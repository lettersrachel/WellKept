/** Types for the plain-ESM constants, so TypeScript consumers (the e2e
 * specs) can import the same module the node scripts do. One module, one
 * value each; a second declaration file would be the copied-constant
 * defect again. */
export const SMOKE_TEST_FIXTURE_ID: string;
export const FERNBROOK_DEMO_ID: string;
export const SMOKE_TEST_FIXTURE_NAME: string;
export const FERNBROOK_DEMO_NAME: string;

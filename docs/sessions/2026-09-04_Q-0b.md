---
status: living
---
# Session Q-0b · 4 September 2026 · intake and merge of the 3 September build package

Instruction: `docs/FOUNDER_RULINGS_2026-09-03_PR282_Q0b.md` Part B (frozen,
manifested). Documents only; no feature code, no migration. Q-1 does not
start until these outputs are approved and PR #282 is merged.

## Built (documents)

1. **The intake freeze.** The founder ruling and all ten package files
   frozen verbatim under `docs/intake/2026-09-03-build-package/` (the
   package CLAUDE.md renamed `PACKAGE_CLAUDE_MD_WITHDRAWN.md` so it can
   never be mistaken for the standing file), every stripped-content
   sha256 verified equal to its Q-0 pin, eleven entries added to the
   FROZEN manifest in `packages/schema/src/frozen-records.test.ts`
   (the register, per the standing pattern; the one non-document file
   touched, as Part B permits).
2. **Part A posted verbatim as the review on PR #282**, per the
   instruction's own first line.
3. **The by-section merge into the standing CLAUDE.md**, twelve
   commits, one per section, each commit message naming the package
   section and whether the standing wording was kept:
   1/12 tier ruling · 2/12 quiet hours · 3/12 Handled invariant ·
   4/12 mode logic and AI release governance · 5/12 fixtures plan ·
   6/12 competitor inputs and not-copied list · 7/12 brand ·
   8/12 Household Zero · 9/12 invariants 16 to 20 ·
   10/12 benchmark adoption record · 11/12 the intake process rule
   (Session discipline) · 12/12 the START_HERE standing instruction
   (Session discipline, standing wording winning).
4. **The register retired what is withdrawn** (`docs/SPEC_REGISTER.md`):
   package CLAUDE.md and RFC-001 `superseded` with pointers to the
   standing CLAUDE.md and the ATTR-01 amendment; the adopted package
   files stamped intaken; the authority caveat resolved with a dated
   note. Frozen copies kept.
5. **RFC-ATTR-01 Amendment 1** (`docs/rfc/RFC-ATTR-01_Field_Attributes.md`):
   the two enums signed off with the ruling quoted as the signature
   (section 7 question 1 closed); the knowing-state mapping table with
   both preserved rules; the nine Ruling 2 §4 consumers and attributes
   each dispositioned as already / partly / absent against the drift
   list, every absent tied to a queue item with a single migration.
6. **The re-cut queue** (`docs/BUILD_QUEUE.md`, replacing the package's
   through the intake pattern): built items stamped `built@<sha>` and
   closed (Q-0 @1db1108, Q-3 @db98f08); partly built items carry their
   true remaining size (Q-1, Q-2, Q-4 @0d863ad, Q-7 @9121429,
   Q-14 @8fa115a, Q-17 @43d1f2b); new items keep the package acceptance
   verbatim; the Ruling 2 §4 migrations split one per session (Q-3b,
   Q-12b-1/2/3, Q-13a, Q-13b, Q-14a); tier labels and the `shadow` flag
   kept; the corporate-task list preserved in full and re-mapped.
7. **Fixtures and mode reconciliation (Part B task 5)** recorded where
   it binds rather than in a separate report: merge 5/12 states that
   Fernbrook, the Smoke Test Fixture and Trainor exist, do not map to
   F-1/2/3, that Trainor is the Synthetic Training Household base, and
   that `training=true` reconciles with the existing `is_fixture` flag
   at build time; queue rows Q-8, Q-15, Q-16 and Q-17 record that no
   workbook importer, mode table or certification table exists in the
   tree, so those rows extend rather than duplicate.

## Verified

- Stripped hashes of all eleven frozen files equal the Q-0 pins
  (`tail -n +4 | sha256sum` against the FROZEN map); the manifest guard
  ran green after the extension.
- The schema package suite (guards manifest, frozen records, the
  document frontmatter lint, all censuses): 15 files, 64 tests, green
  against the merged documents.
- The whole suite uncached: 11 of 11 turbo tasks green
  (`pnpm test --force`, exit 0). One earlier run failed one web test
  seconds after the local database restarted and passed on the two
  consecutive re-runs; environmental, and no document under this
  session's change is in that suite's failing path.
- CLAUDE.md, BUILD_QUEUE.md, the amendment and this log carry zero em
  dashes, checked by pattern.
- Commit list: the intake commit, twelve merge commits, the register
  commit, the amendment commit, the queue commit, this log's commit,
  all on the PR #282 branch.

## Ground truth (every disagreement met while merging; none reconciled silently)

1. **The ruling's "six-table delete in `erase-household.mjs`" is a
   stale count: the repository has NINE documented delete exceptions**,
   CI-enforced and named in the tool's own header. The intent (do not
   convert them to tombstones) is unchanged and merged; the ruling's
   text is not reworded (prohibited); the count stands corrected here.
2. **Q-6, Q-8b and Q-9 build member surfaces while WK-DEV-007 freezes
   the client side at the digest, and both are inside the ruling's
   "what does not change".** Reported, not resolved; the stricter
   reading holds; the rows are FREEZE-GATED in the queue.
3. **Q-1's `mail_outcome` requires a migration though the package row
   said none.** A new table is a migration; the row is corrected in the
   re-cut with the drift noted.
4. **"Household Zero" collides with the repo's earlier use of the term
   for the held temporal-layer field list.** The merged section defines
   the term forward; dated entries keep the older usage as written.
5. **Q-5's stage attribute lost its migration home** when RFC-001's
   Migration B was withdrawn and decomposed; the row now carries its
   own migration under the one-per-session rule.
6. **`latest_safe_start`: the ruling says own migration; the
   repository's standing pattern for exactly this shape is
   computed-never-stored** (flag promotion, time_segment duration).
   Recorded as the queue item's first question; the ruling is not
   reworded.
7. **START_HERE's "one human reviewer minimum" does not match the
   standing Merging law** (verify-then-merge plus branch protection).
   Standing wording wins, stated in merge 12/12.
8. **The ruling names "the three founder tasks that gate the queue";
   the package lists six corporate tasks.** Same list at two grains:
   the three are the queue-gating subset. Preserved in full in the
   queue with the gating three named.
9. **Already stated, no change made (task 1's duplicate log):**
   invariant 17's confirmation rule (the standing Backstage
   AI-suggestion invariant, standing wording); invariant 18
   (activity is not outcome, carried by the Handled invariant merge);
   one migration per session; report-and-stop on disagreement;
   the do-not-unwind rulings; START_HERE rule 9 (external content),
   merged once with invariant 16 rather than twice.
10. **Q-16's cohort record: the package says no migration, and an
    append-only cohort record is a table.** Recorded on the row;
    settled at that session, not silently fixed here.
11. **The withdrawn package CLAUDE.md's invariant 13 ("household logic
    runs in America/New_York") sits against the standing G-116
    true-instant law** (per-row operator zone, UTC instants). Invariant
    13 is not on the Ruling 1 §2 new-law list, so nothing was merged
    and no conflict stands; noted so nobody later reads the frozen
    copy as law.
12. **RFC-001's `confirmed` maps one-to-two** (`asked` or
    `client_written`): the promoted list keeps a distinction RFC-001's
    vocabulary collapsed. Recorded in the amendment's mapping table as
    an honest split, not a lossy rename.
13. **Ruling 2 §5's judgment-free guard extension is code**, which this
    session may not change; it is carried as the Q-4 remainder rather
    than built here.
14. **Four queue specs are not in the repository** (Architecture Review
    §4, INSTRUCTION_UPDATES v2, A128, A129, A130 among them, per the
    register); their rows run on the package's own acceptance criteria
    until a copy is intaken.
15. **Q-2's "Upstash resumed" is a founder-side dashboard fact** this
    repository cannot verify; recorded on the row rather than assumed
    done or not done.

## Part C applied (4 September 2026, same day)

The founder's Part C review
(`docs/FOUNDER_RULINGS_2026-09-04_PartC_Q0b_Review.md`, frozen,
manifested, posted verbatim as the second review on PR #282) accepted
Q-0b and ruled the four named items. Application record:

- **§2.1 (nine, not six):** searched before editing; no merged living
  text asserts the six-table count. It appears only inside frozen
  records and inside the two session logs QUOTING the stale text as the
  disagreement, which are correct as records and stay as written. The
  correction that binds forward is the lesson added to the standing
  intake rule: counts and lists come from the tree, never from a
  document about the tree.
- **§2.2 (the freeze split):** Q-6, Q-8b and Q-9 split in
  `docs/BUILD_QUEUE.md` into freeze-safe halves that proceed and
  FREEZE-GATED member-facing halves waiting on the 25 September
  two-key decision with the COO (that decision's scope: whether the
  digest freeze lifts for exactly those three surfaces at E1, nothing
  else). The queue assumes neither outcome.
- **§2.3 (`mail_outcome`):** already how the re-cut row reads; Q-1's
  single migration.
- **§2.4 (`latest_safe_start` is derived):** the repository holds TWO
  derived-value precedents, not one: stored-with-derivation-stamp
  (`registry_entry.derived_year` beside `derivation_source`) and
  computed-never-stored (flag promotion, `time_segment` duration). The
  ruling's rule matches the stored-with-stamp precedent, so no conflict
  stands; the Q-12 row and Amendment 1 §A1.3 now record the ruled
  shape (materialized, derivation-stamped, worker-recomputed on outbox
  events, read-only, logged corporate-admin recompute) and the open
  question is closed.
- **§3 (the other eleven):** every one of the eleven is marked
  non-blocking with a proposed disposition, and each disposition either
  is a pure record or lives inside a document Part C §1 accepted, so
  all eleven are adopted under §3's first sentence. None was marked
  blocking and none PROPOSES a change touching a two-key adoption, the
  E1 gate, WK-STD-026, or a member-facing surface (items 4, 7 and 11
  BRUSH such subjects but their dispositions change nothing in them:
  a term defined forward, the standing merge law confirmed, a note on
  a withdrawn document). Zero items are therefore listed for
  individual ruling, stated here so the empty list is a checked claim
  rather than an omission.

## Open items

- Founder review of Part B's outputs, then the PR #282 merge (hers; not
  merged by this session, per the instruction).
- The Q-6/Q-8b/Q-9 freeze conflict: a founder ruling, either shape.
- The `latest_safe_start` store-versus-compute shape (ground truth 6).
- The `training=true` versus `is_fixture` flag reconciliation, at Q-17.
- WK-TRN-009 exists only as the unplaced draft (G-110); Q-16 is gated
  in part on the document.
- Ruling 2 §4 could not be settled for nothing: every item resolved to
  already / partly / absent. No Amendment 1 row is left undispositioned.
- Standing outside this session, unchanged: the Railway worker redeploy
  (A2 acceptance's production half), the tz NOT NULL tighten, the
  2026-09-09 a11y gate date with open findings.

## Exact commands

- Guards manifest, frozen records and the document lint against the
  merged files: `pnpm --filter @wellkept/schema test`
- The whole suite, uncached: `pnpm test --force`

---
status: living
---
# Session Q-2 · 4 September 2026 · brand as one configuration value

Queue row: Q-2 of the re-cut `docs/BUILD_QUEUE.md`, authorized by the
founder after Q-1's acceptance, with the precondition met first: the
Railway worker redeploy is CONFIRMED (founder, this session: the PR
#283 deployment ACTIVE on Railway, auto-deployed from main;
repeat:drain-outbox completing in the logs; the fleet board's Outbox
drain line showing a live run). The instruction, verbatim scope:
extract the company name, sending domain, app display name and
credential wording into one configuration value with a CI test that no
member-facing string carries the literal; do not change the value
itself. No migration.

## Plan (written before building)

**Read first (done):** a case-insensitive survey over the root layout,
the sign-in family, /privacy, the (client) route group, and every mail
send path (three independent senders: lib/mail.ts, auth/config.ts's
raw fetch, and packages/mail's sendResendEmail used by the worker).
Twenty-one literal sites found; the case-insensitive pass caught three
UPPERCASE variants ("WELL KEPT" in the root layout h1 and both digest
headers) that a case-sensitive search missed.

**1. `packages/config` (@wellkept/config)**, a new workspace package in
the mail package's build-less shape (main src/index.ts, no runtime
dependencies). Exports:
- `BRAND`: companyName, legalEntityName (the privacy notice's entity
  line), appDisplayName, sendingAddress, and emailFromFallback derived
  from companyName plus sendingAddress, so the display literal exists
  ONCE. All values exactly today's strings; the render is byte
  identical.
- `CREDENTIAL_NAMES`: "Household Operations Manager" and the two
  certified forms, FIXED and independent of the company name per the
  adopted law (merge 7/12); the 25 September name decision does not
  touch them. No member-facing surface renders them today; they live
  here so the one place exists before a surface does.

**2. The sweep, value-identical:** member-facing sites route through
BRAND (root layout title, appleWebApp title and the h1 via
toUpperCase(); signin heading; /privacy metadata and entity prose;
the client playbook line; the magic-link from/subject/body; the
client report footer; the client digest header and footer; every
AUTH_EMAIL_FROM fallback). The staff strings living in the SAME mail
channel files (watch-alert footer, fleet digest header and footer,
staff digest from-fallback) convert too, so every mail file is
literal-free and the guard needs no fragment excusals; staff-only
.tsx pages elsewhere keep their literals, exactly as the law permits.
packages/mail's local style const named BRAND is renamed PALETTE to
free the name (colors only, render unchanged).

**3. The guard** `packages/schema/src/brand-config.test.ts`, both
inputs computed (the inputs doctrine): set A walks apps/web/src/app
for .tsx under the (client) group plus the signin, verify-request and
privacy segments plus the root layout (floor: files); set B walks
apps/, packages/ and services/ for source files matching the
send-channel signature (sendMail( or sendResendEmail( or
api.resend.com), never descending into .next or .turbo (the G-113
lesson) and excluding tests and the config package itself. After
comment-stripping (the client-copy helper's shape), the DISPLAY
literal (/well kept/i, space required so "@wellkept/..." import
specifiers do not match) must appear in NO scanned file; and
BRAND.companyName must equal exactly "Well Kept", which is the
instruction's "do not change the value itself" made structural: the
name decision changes the config and this pin in one reviewed commit.
Proven red both directions (a literal planted back in a scanned file;
the pin against a changed value) and green restored. Joins
guards-manifest (files array and the CLAUDE.md-name list) and the
CLAUDE.md guard table, same PR.

**4. Records:** the WORK_QUEUE state entry closes the Railway-redeploy
owed line on the founder's confirmation (her instruction: "the
redeploy is no longer owed"), which also completes the A2 ruling's
"metric visible in production" acceptance; BUILD_QUEUE Q-2 stamps
built@sha with the Upstash clause honestly recorded as the one
founder-side remainder of the original row.

**Deliberately out of scope, named:** the "Tell Well Kept" feature
name on staff capture surfaces and in the close-flow question carries
the company name and is UNTOUCHED (staff-facing; the law binds
member-facing surfaces, and the e2e journeys pin those labels). When
the name decision lands, that sweep is its own session and the config
is its source. Staff-only pages (visit, mfa, link-device, context)
likewise keep their strings.

## Built

Commit `db0cf18`, exactly the plan:

- **`packages/config` (@wellkept/config)**: BRAND (companyName,
  legalEntityName, appDisplayName, sendingAddress, emailFromFallback
  derived so the display literal exists once) and CREDENTIAL_NAMES
  (the three fixed credential strings, independent of the company name
  per the adopted law). Build-less, dependency-free, single file.
- **The sweep, twenty-one sites, values byte-identical**: root layout
  (title, appleWebApp title, the masthead h1 via toUpperCase()),
  sign-in heading, /privacy title and entity line, the client playbook
  line, the magic-link from/subject/body, the client report and watch
  alert footers, both digest headers and footers, and all four
  AUTH_EMAIL_FROM fallbacks. The mail package's local style const
  BRAND renamed PALETTE (colors only). Workspace dependency added to
  apps/web, packages/mail, services/worker (runtime) and
  packages/schema (dev, for the guard); lockfile updated.
- **`brand-config.test.ts`**: both input sets computed (the
  member-page walk over the (client), signin, verify-request and
  privacy segments plus the root layout; the send-signature scan over
  apps/, packages/ and services/), comment-stripped, the space-required
  case-insensitive literal refused everywhere but the config, and the
  config pinned to today's exact values. Joined guards-manifest (files
  array and the name list) and the CLAUDE.md guard table.

## Verified

- Guard red both directions with each mutation confirmed landed first:
  a literal planted back into the sign-in page failed naming
  `apps/web/src/app/signin/page.tsx`; the config value changed to
  "Kept Well" failed the pin (twice over, since the derived
  from-fallback moved with it); restored, green.
- The guard's own first run corrected a hand-guessed floor: six
  member-facing pages were assumed, five exist (layout, signin,
  verify-request, privacy, the one client page); the floor now states
  the derived count with its derivation date.
- Typecheck 13 of 13 tasks (the new package included); full suite
  `pnpm test --force` 11 of 11 uncached, green. An earlier red run was
  the local database down after idle hours (ECONNREFUSED on every
  integration file), green on restart; not a code failure.
- Zero em dashes in every new file.

## Ground truth

1. The case-insensitive survey found three UPPERCASE literals ("WELL
   KEPT" in the masthead h1 and both digest headers) that the first
   case-sensitive pass missed. Method fact worth keeping: a brand
   sweep that greps one casing undercounts.
2. "Tell Well Kept" (the feature name) appears on staff capture
   surfaces, in the close-flow question, and in e2e label pins, and is
   DELIBERATELY untouched: the law binds member-facing surfaces, and
   renaming a feature is the name decision's own sweep, for which the
   config is now the source. Staff-only pages likewise keep their
   strings, as the law permits.
3. The privacy page's entity line ("Well Kept Home Operations
   Management LLC") now renders from BRAND.legalEntityName; the master
   doc `docs/legal/privacy-notice.md` still carries the words as
   prose, correctly: documents are not code strings and stay on the
   same-PR rule. The guard's not-covered column says so.
4. During the red proof, `git checkout` on the not-yet-committed
   sign-in page restored the PRE-SWEEP file rather than the sweep: on
   uncommitted work, a checkout IS a mutation, and the guard itself
   caught the regression it created (a second, unplanned red-green
   cycle). Recorded because the restore idiom quietly assumes a
   committed baseline.
5. Q-2's remaining original clause: the Upstash state is a founder
   dashboard fact this repository cannot read; recorded on the queue
   row, not assumed either way. The sweeps clause closed on the
   founder's Railway confirmation recorded in WORK_QUEUE.

## Open items

- Founder-side: the Upstash check (the queue row's one remainder).
- The next deploy carries Q-1's 0062 (migrations 62 to 63) and this
  session's member-surface changes; renders are byte-identical, so the
  only visible delta of Q-2 is none, which is the point.
- Standing: the tz NOT NULL tighten, the 2026-09-09 a11y gate
  findings, the 25 September two-key freeze decision, and the name
  decision itself, which now lands as a reviewed edit to
  `packages/config/src/index.ts` and the guard's pin together.

## Exact command to run the acceptance test

- The guard alone: `pnpm --filter @wellkept/schema test -- brand-config`
- Everything: `pnpm test --force`

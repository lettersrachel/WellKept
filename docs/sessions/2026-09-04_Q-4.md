---
status: living
---
# Session Q-4 · 4 September 2026 · substrate guards and the ruled outbox FK (0064)

Queue row: Q-4 of the re-cut `docs/BUILD_QUEUE.md`, authorized by the
founder at the values-intake acceptance: one migration carrying the
ruled FK with the production census and test parent rows, plan first,
red proofs on the guards, report and stop. Authority: RFC-ATTR-01 §2.8
and Amendment 1 §A1.1/§A1.4 (RFC-001 §4 withdrawn); the 4 September FK
ruling; the values-intake adoption ruling 2 (the guard bars the
inference and permits the `load_concern_raised` record).

## Plan (written before building)

**Read first (done):** the Q-3b readers' maps still current (tables.ts
event_outbox, the drain, every emit site); the local orphan census run
fresh (0 orphans across 61 event rows); the test survey (exactly one
suite inserts events under household ids with no parent row:
`event-outbox.integration.test.ts`; the Q-3b causation suite already
creates parents).

**1. Migration 0064** (the session's one): the additive FK
`event_outbox.household_id -> household(id)`, per the 4 September
ruling with its erasure-order condition answered at the intake
(households are renamed, never deleted; children delete first, which
an FK never objects to). No new column: the ALTERED column's producers
are unchanged (every emit site passes a real household id). The header
carries the precondition (local census 0/61) and the PRODUCTION census
SQL for the founder to run before the deploy applies it; the failure
mode is Postgres's atomic refusal at ADD CONSTRAINT (the 0059
precedent).

**2. Test parent rows**: `event-outbox.integration.test.ts` gains real
household parents for its two random household ids, with cleanup, the
shape the causation suite already uses.

**3. The judgment-free guard** (`packages/schema/src/judgment-free.ts`
holding the founder-editable pattern list with a written WHY per
entry; `judgment-free.test.ts` the guard): a census over every column
name in tables.ts refusing matches against the Ruling 2 §5 list
(ranking constructs: rank, leaderboard; stress and emotion inference:
stress, emotion, mood, burnout, anxiety; the standing cognitive-load
bar; health inference: health_score, health_risk, diagnosis;
social-content inference: social_ prefixed columns) plus RFC-001 §4's
person-characterizing words (temperament, personality), with an
allowlist-with-written-reasons hatch, EMPTY at birth. And the
TWO-HOUSEHOLD refusal: no table may define more than one
household-named column (RFC-001 §4's second clause; today every table
defines at most one, and the composite same-household FKs reuse that
one column, which the census counts by definition, not by usage).
Adoption ruling 2 recorded in the module: `load_concern_raised` with
required `reported_by` is a PERMITTED human record (it matches no
inference pattern by design, and if a future pattern would catch it,
the allowlist entry cites the ruling); the barred thing is the
inference. Proven red two ways (a planted `stress_score` column; a
planted second household column) and green restored; joins the guards
manifest and the CLAUDE.md table.

**4. The signed enums promoted**: `field-attributes.ts` exports
`MATERIALITY_VALUES` (safety_access, money_legal, convenience) and
`CONSEQUENCE_CLASS_VALUES` (editorial, behavioral, high_consequence)
per Amendment 1 §A1.1, ending their deliberate unexport;
`field-attributes.test.ts` gains the exact-list assertions, proven red
on a removed value and green.

**Deliberately not done, named**: no CHECK or column ships for
materiality or consequence class (RFC-ATTR-01 §6: attribute columns
arrive on the next primitive that needs them, never as a speculative
batch); no `load_concern_raised` column ships (its adoption is Q-15/16
work; the guard is ready for it); the guard reads COLUMN NAMES, not
free text or values, stated in its not-covered column.

## Built

Commit `e117087`, exactly the plan: migration 0064 (the ruled FK, one
statement); the tables.ts reference; parent rows in
`event-outbox.integration.test.ts`; `judgment-free.ts` (the
founder-editable pattern list, a written WHY per entry, the
written-exception hatch empty at birth) and `judgment-free.test.ts`
(the column census with a 300-column floor and the two-household
census with a 30-table floor); `MATERIALITY_VALUES`,
`HARD_STOP_MATERIALITIES` and `CONSEQUENCE_CLASS_VALUES` promoted in
`field-attributes.ts` with exact-list pins in its guard; manifest and
CLAUDE.md guard table extended.

## Verified

- **The FK's red proof arrived unplanned and real**: the full suite
  went red BEFORE the test fix because the constraint refused the
  orphan-seeding suite's random household ids, which is the refusing
  direction firing on real inserts; green after the parent rows.
  Applied locally against the fresh 0-orphan census (61 rows).
- **The judgment-free guard red two ways by name**, each mutation
  confirmed landed first: a planted `stress_score` failed citing the
  stress/emotion pattern and its written WHY; a planted
  `other_household_id` failed naming `event_outbox (household_id,
  other_household_id)`. Green restored both times. The false-positive
  scan ran BEFORE the pattern list was finalized: zero matches across
  today's schema, so the guard shipped green on the known-good case by
  measurement, not hope.
- **The enum pins red on a removed value** (`high_consequence` dropped
  failed the exact-list test), green restored.
- Typecheck 13/13; `pnpm test --force` 11/11 uncached, green; schema
  suite 73 tests. Zero em dashes in new files.
- One working-practice note: a `git checkout` restore during red proof
  1 wiped the session's uncommitted FK edit along with the mutation
  (the Q-2 lesson recurring); the FK edit was re-applied and verified
  present, and red proof 2 restored by removing the planted line
  instead of by checkout.

## Ground truth

1. The values-intake adoption ruling's boundary is encoded where the
   guard will meet it: `load_concern_raised` matches no inference
   pattern BY DESIGN, and the module says so with the ruling cited; if
   a future pattern would catch it, the allowlist entry cites the
   ruling. The guard bars the inference, permits the human record.
2. The field-attributes step-1 stamp (built@0d863ad) left the Q-4
   queue row when the row closed; it remains recorded in WORK_QUEUE's
   B1 entry and the RFC. Noted so the stamp is not read as lost.
3. RFC-001 §4's remaining clauses (defineAction as prerequisite;
   derived-field recompute job) resolved elsewhere: the action gate is
   Q-1's guard in the standing shape, and recompute arrives with the
   first derived-field consumer (Q-12), not here.

## Open items

- Owed at the next deploy: 0062, 0063, 0064 apply together (migrations
  62 to 65); the founder runs 0064's header census against production
  first (expected 0; a non-zero count refuses atomically).
- The materiality and consequence-class COLUMNS await their first
  needing primitive (RFC §6); `load_concern_raised` awaits Q-15/16.
- Standing, unchanged: the tz NOT NULL tighten, the 2026-09-09 a11y
  gate findings, the 25 September two-key decisions.

## Exact commands

- The new guard: `pnpm --filter @wellkept/schema test -- judgment-free`
- The FK and outbox suites:
  `pnpm --filter @wellkept/web test -- event-outbox` (needs
  DATABASE_URL against a migrated database)
- Everything: `pnpm test --force`

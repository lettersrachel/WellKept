---
status: living
---
# Session D input: draft categories mapped onto the 258-field seed

2026-07-28. Mechanical mapping of CONCERNS_MINOR_DRAFT.md's categories
onto the actual template (tooling/seed/fernbrook_template_seed.json),
which the draft's author had not seen. **Candidates for the founder's
redline, not a chosen list.** The test applied: the field's DEFINITION
guarantees the value concerns a minor.

## Clear matches (pass the test on their definition)

All in Section 3 unless noted; draft category in brackets.

- "Each child: name, age, school and grade, weekly schedule" [1,2,3]
- "Each child: room rules and boundaries (enter? knock? never?)" [11]
- "Each child: what they notice, need, or are proud of" [1]
- "Child-related rules: screens, snacks, homework, what HM may/may not do" [1]
- "Sizes registry per child" [5] (playbook twin of the registry CHECK)
- "School communication channels: which portal/app/email..." [2,12]
- "CADENCE REGISTRY, children [S2]: well-child, dental, vision,
  vaccination/school-form deadlines, sizing check, activity gear and
  registration windows" [3,4,9]
- "CUSTODY SCHEDULE [S2]: which weeks/days each child is home; co-parent
  handoff; INFORMATION BOUNDARY with co-parent" [7] — note the seed
  already answers the draft's co-parent design question structurally:
  the co-parent boundary lives INSIDE the custody field, so marking the
  field marks it, with no separate decision needed.
- s21 "VISITING-CHILD KIT" [13] — carries design question 2.

## Borderline (mixed definitions; founder decides which side)

- s5 "REGULAR VISITOR ROSTER: playdate/carpool children (names + ALLERGY
  CROSS-CHECK)..." — children by name, but the same field holds adult
  visitors and vendors. Mark, split, or leave to policy.
- s17 "Transportation safety protocol (if transporting child/client)" —
  conditional on "child/client"; car-seat config is child data, the
  protocol itself is not.
- s1 "Allergies: every person, every severity" [10] — definitionally
  every person, not definitionally a minor. The draft's category 10
  likely stays POLICY here unless a child-specific field is split out.

## Draft categories with NO definitional field in the seed today

6 (transport/pickup authorization beyond the custody field), 8 (childcare
providers as a child-scoped field - s5's staff roster is adult-defined),
and 1's identity basics beyond the Section 3 rows. If Phase 0 adds them,
they arrive classified (the CHILD_DATA.md rule).

## Not matched, correctly

Pets (s4 "Walker/sitter" is the dog walker), HVAC/vacuum "sizes",
firearms/cannabis fields that mention children as a risk factor - a
hazard NEAR children is not data ABOUT a child.

## What the founder decides (then session D builds)

1. Redline the two lists above (cut/add/split).
2. Design question 1: marker means under-18 or child-role.
3. Design question 2: visiting-child material's consent posture
   (connects to packet section 6(d)).
4. Question 3 (retention) is now IN the packet: section 6(g) asks
   whether child data needs its own retention rule.

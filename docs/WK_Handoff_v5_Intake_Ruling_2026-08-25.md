---
status: frozen
---
# Handoff v5 Intake Ruling · 25 August 2026
Paste-ready for the development session (register draft A577). Figure-free; enters docs/ beside the directives. Governs the intake of Well_Kept_Software_Developer_Implementation_Handoff_v5_2026-08-25 and the Full-Chat Consistency Audit v2, both filed same date (terminology corrected on intake: two retired-role-title hits; the one pilot mention is the drift-risk register's own warning and stands).

## 1. Precedence, stated once and bindingly
Handoff v5 is accepted as the CURRENT CONSOLIDATED TIER C REFERENCE, superseding the 24 August handoff as the reading copy. It changes nothing about what governs: the controlled requirement set (WK-DEV-001 through 005 as merged, REQ-070..085), the adopted directives WK-DEV-006 through 010 v1.1, the rulings of 24 August, and the register entries A553-A576 remain the law of the build. The audit's own precedence rule agrees ("where controlled requirements, ADRs, current authority or a security control conflict, stop and record"), and this ruling makes the application concrete: WHERE V5 IS SILENT ON SOMETHING THE DIRECTIVES ADOPTED, THE SILENCE IS NOT SUPERSESSION. The 24 August handoff remains filed as lineage; its section 24 is the recorded origin of REQ-083..085.

## 2. The adopted layer v5 does not contain (verified absent; all remain in force)
Because v5 was consolidated in a parallel workstream that had not received the directives, it carries none of the following, and the development session continues to enforce every one: REQ-083 covenant events (visit_arrival, visit_departure, household_departure + cause_code; the monthly covenant report as a pure function of events); REQ-084 property-data enrichment prohibition with its CI dependency guard; REQ-085 the software cost gate and run-rate reporting; Ruling 1's capacity-vs-performance boundary and its permission-matrix enforcement; the D7 client-surface time-quantity ban and its lint; the shadow-mode doctrine and A0 cap for the anticipation engine; the Tier D/M split and the Tier M gate of WK-DEV-009 v1.1; the vendor-slice rulings (internal simulation until the pen test covers the link surface; digest as the client landing); Household Green, Lauren Green's tester provisioning, and the tester flag; the Task Inventory provisional-source flag; and the segments-derived-from-taps rule with manual per-segment timing prohibited in v1. Nothing in this list is reopened by v5's arrival.

## 3. What v5 and the audit genuinely add, adopted as follows
1. **Section 40's product-development test** (functionality plus outcomes plus maintenance tax plus operating leverage, superseding the burden-only test) is CONVERGENT with WK-DEV-010 v1.1 section 1's seven-question RFC law; the seven-question form remains the working template and now cites v5 section 40 as concordant.
2. **The audit's C-05 batch-confirmation correction** (only known-normal deterministic work compactly confirmable; every AI-created fact individually confirmed) is verbatim the 24 August ruling as unified in WK-DEV-009 v1.1 section 2.3. Convergence from two independent workstreams is recorded as strong evidence the rule is right; no change.
3. **Capacity doctrine precision (C-06).** The 3-to-5 households-per-HOM band is accepted as current workforce operating doctrine, stored as VERSIONED CONFIGURATION never hard-code, exactly as the audit directs, with one estate-side precision the audit could not know: the CAP value of 5 is covenant-relevant (the lender capacity math and hiring gates of the model of record are built on it), so the configuration encodes cap=5 and band=3..5, and any change to the cap is a two-key model change before it is a config change. The stale 15-to-20 assumptions are dead everywhere, as C-06 requires.
4. **The nine recovered candidate concepts** (SituationEvent, CaseHierarchy, ZoneState, FunctionalStateTarget, OrganizationPreferenceProfile, WorkCognitiveLoadProfile, RelationshipMomentRecord, VendorProviderProfile, AIHumanHandoff) enter the CAND ledger under the 24.7 forcing function: owner, forum, and date assigned at the next housekeeping pass or they close. Two immediate notes: SituationEvent merges with the Situation/Event Bundle already authorized in WK-DEV-009 (one concept, not two), and WorkCognitiveLoadProfile carries Ruling 1's bar explicitly (planning input, never ranking), which its own disposition already states.
5. **The security package v3 set** (Threat Model v2, WK-SEC-001 Addendum A1, authorization architecture, projection registry, vendor-link substrate, physical-access, offline/mobile, privileged-access, recurring assurance, Data Lifecycle Matrix, abuse-case suite) is named by the audit but not yet in the library. Founder action: deliver the package v3 files for filing beside WK-SEC-001; developer action: the external pen-test scope aligns to Threat Model v2 plus the abuse suite when they land, and the abuse-case workbook's NOT RUN/BLOCKED status is honest and stays until tests execute.
6. **Zone State's privacy shape** (project do_not_disturb without the private reason) and **CaseHierarchy's closure rule** (a parent case closes only on verified overall outcome, never on dispatch) are adopted now as design rules for their future primitives, since both restate walls the estate already holds (operational-instruction transform; vendor-complete never implies resolved).

## 4. Open gates the audit lists, mapped to their owners
Repository verification: DONE (Gate 0, PR #119) and continuing per unit. Retention periods and deletion behavior in blocked lifecycle rows: counsel plus founder, already queued with the privacy notice. Abuse-case execution and independent retest: the scheduled external security engagement. Physical-access provider: evidence-gated per WK-DEV-010 section 12. AI/RAG provider with zero-training evidence: the Tier M gate entry, unchanged. Later candidates (process mining, predictive inventory, smart-home, autonomous scheduling): the standing never-yet list and their evidence gates.

## 5. Transfer and reading order
The development session's reading order becomes: 00_CURRENT_AUTHORITY (reference-only) → WK-DEV-006 → the 24 August rulings → this ruling → Handoff v5 (the consolidated reference) → WK-DEV-007, 008, 009 v1.1, 010 v1.1 (the governing implementations) → the audit v2 (context). The 24 August handoff moves to lineage. Nothing in this intake pauses any running work: Gate 1 continues, the HG sprint continues, and the only new founder items are the security package v3 delivery and the capacity-config note above.

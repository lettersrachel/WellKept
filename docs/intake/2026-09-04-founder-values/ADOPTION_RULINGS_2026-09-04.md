---
status: frozen
---
# Adoption rulings on the values-intake ground truths · 4 September 2026

Founder rulings at the values-intake acceptance, applied as this
adoption note per her instruction: the frozen package files are NEVER
edited; this note governs how their rows are read. Recorded verbatim in
substance, numbered as ruled.

1. **Two quantities, both real.** `capacity_gate` 5 is the OPERATING
   cap the software enforces (the 0055 knob, two-key change control);
   `household_cap_weekly` 6 is the PHYSICAL hard maximum in workforce
   doctrine. Lifting 5 to 6 is an E4 two-key decision. The CSV row is
   read as SPLIT accordingly: its value feeds workforce doctrine, never
   the shipped knob.
2. **`emotional_load_flag` is renamed `load_concern_raised`** at
   adoption: human-recorded ONLY, with a required `reported_by` (self
   or senior); no code path ever sets it. The Q-4 judgment-free guard
   BARS the inference and PERMITS the record: when the column arrives,
   it enters through the guard's written-exception path citing this
   ruling.
3. **Status tags map to the shipped hyphenated keys** (`LIFE-EVENT`,
   `ONBOARDING-90`, `RENEWAL-WINDOW`); no rename. The enums file's
   underscore forms are read as display-layer identifiers of the same
   six values.
4. **Dead-letter attempts stay at the shipped 10.** The CSV's 8 does
   not adopt; the drain's bound is unchanged.
5. **The two additional reconciliation patterns**
   (`seasonal_service_not_scheduled`,
   `subscription_renewed_unexpectedly`) **are adopted as Q-12b data
   BEHIND the six launch patterns**: they ship with the consumer, after
   the six, not among them.

The other five ground truths in the session log carried proposed
readings inside documents the founder accepted with the intake and
stand as recorded there.

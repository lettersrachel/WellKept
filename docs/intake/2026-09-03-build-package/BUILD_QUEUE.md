---
status: frozen
---
# Build queue · sequenced sessions with acceptance criteria

One item per session, in this order. `R` items are reconciliation sessions. Every item names its stamped spec, its single migration (if any), and acceptance criteria an agent can test. Year-two items carry the `shadow` flag until E2. The launch-critical tier ends at Q-11.

| Id | Item | Spec | Migration | Acceptance (Given / When / Then) |
|---|---|---|---|---|
| Q-0 (R) | Reconciliation and ground truth | all | none | Given the repo at HEAD, when every spec in SPEC_REGISTER.md is read against it, then each carries `verified@sha`, `plan-of-record` or `superseded`, the AQ run report is in `docs/reports/`, and drift is listed in the session log. |
| Q-1 | Phase 0 hardening: `defineAction` wrapper and lint rule; deliverability webhooks (bounce, complaint) to an `mail_outcome` table; alert on webhook silence | Architecture Review §4 | none | Given an action file without `defineAction`, when CI runs, then lint fails. Given a bounced message, when the webhook posts, then `mail_outcome` holds it and corporate sees it within one worker cycle. |
| Q-2 | Housekeeping: REQ-078 to 080 appended to WK-DEV-001; `BRAND` config extracted; Upstash resumed; daily trigger sweeps verified running | INSTRUCTION_UPDATES v2 | none | Given a fresh deploy, when the sweep is due, then it runs and logs; given `BRAND` changed, when the app renders, then no member-facing string carries the old value (grep test in CI). |
| Q-3 | RFC-001 Migration A: `field_event` outbox | RFC-001 §2 | yes | Given any field or registry write, when the transaction commits, then exactly one `field_event` row exists with matching hashes and the worker consumes it idempotently; a failed consumer retries and dead-letters at 8. |
| Q-4 | RFC-001 Migration B: attributes and CI guards | RFC-001 §3, §4 | yes | Given a PR adding a column named from the judgment-free list or a two-household table, when CI runs, then it fails. Given a derived field, when its anchor changes, then it recomputes and a hand-edit returns a typed rejection. |
| Q-5 | Pipeline state machine and stage enum on triggers, prompt packs and queue items | Four-Stage spec §pipeline | none (uses B) | Given a trigger fires, when routed, then it lands in `anticipate`; below-threshold items auto-execute and log fully; above-threshold items reach the decision inbox as `decide`. |
| Q-6 | Decision inbox (member), Commitment Ledger with the Handled invariant, M-25; Decision Rights authored in plain language and mapped to entries with provenance | Four-Stage spec §inbox; BENCHMARK_ADOPTION §2 | none | Given a household's Decision Rights block, when a week closes, then M-25 is computed, the inbox contains only `decide` items batched at the household's cadence, nothing is delivered 21:00 to 07:00, and every ledger item shows one of: needs you / handled / approaching / done or changed, where `handled` requires the invariant (owner exists, no unresolved member decision, follow-up exists where external completion is pending, verification satisfied or explicitly pending). Given a member types a standing instruction, when saved, then it becomes a Decision Rights entry with provenance and a preview of what it will auto-execute. |
| Q-7 | Pre-populated record, tiers 1 and 2, with knowing-states and conditional visibility; per-household capture address and share-sheet target through the capture pipeline (quarantine, source identity, canonical match, AI proposal, human confirmation); anticipation opt-in switch per household at intake | Pre-Populated Intake Spec (A129); RFC-001 §3a; COMPETITIVE_FEATURE_INPUTS §1, §7 | yes (`capture_artifact`) | Given an address and anchor answers, when a household is created, then tier-1 facts land `estimated`, tier-2 `expected`, none `confirmed`; the verification walk flips states with provenance; a client session never sees `conditional_visibility` rules. Given a forwarded flyer or PDF, when captured, then it passes quarantine, lands `proposed` in the HOM review queue, never writes the record directly, and a hostile instruction inside it produces no action. Given anticipation opted out, when triggers fire, then nothing surfaces to the member. |
| Q-8 | Importer parity and fixtures: the three fixture households load through REQ-016 with dry run | FIXTURES.md, REQ-016 | none | Given the three fixture workbooks, when imported, then all fields land with sensitivity (fail loudly if missing), render in all three portals, and the airplane, payload and suppression tests pass against them. |
| Q-8b | Provider-independent export: canonical household JSON plus document and media manifest, vendor, asset, work and outcome history, preferences and standing rules, access history, audit metadata; member-initiated | REQ-017; BENCHMARK_ADOPTION §2 (portability) | none | Given a member requests export, when generated, then the archive restores a complete household in a clean instance through the importer with fixture parity, and S3 values are included only under a separate member-authorized reveal. |
| Q-9 | Inbound routing and auto-acknowledgment (REQ-078, 079) | INSTRUCTION_UPDATES v2 | none | Given an inbound member SMS or email, when received, then it routes to a corporate surface, no HOM personal contact is exposed, and a channel-aware acknowledgment naming the follow-up time and the emergency line is sent (exempt from quiet hours). Prerequisite: A2P registration complete (corporate task). |
| Q-10 | Response-time capture and quarterly report (REQ-080, M-23) | WK-STD-028 | none | Given a quarter of inbound messages, when the report runs, then per-channel response times compare against the commitment table. |
| Q-11 (R) | Reconciliation; E1 launch-critical review; security-test handoff package (WK-SEC-001) | all | none | Given the queue through Q-10, when reconciled, then stamps are current, the E1 checklist in the model's Milestones tab is mapped item by item to evidence, and the assessor has the scope. |
| Q-12 | Cascade implication sets and intersections; `latest_safe_start` computed on commitments; collision detection across household calendars promoted from plan-of-record (`shadow`) | Inference Cascade Spec §1, §2; WK-APP-004; RFC-001 §3 | none | Given a fact confirmed, when the cascade runs, then implied facts land `expected`, predicted events and staged preparations are logged to the corporate view only, every item is dismissible, and nothing is recorded as `confirmed` by the system. Given two facts that intersect, then the intersection anticipation exists and neither single fact alone produces it. Given two commitments collide, when either is created or moved, then the collision is detected before it reaches the member and surfaces once, as one decision. |
| Q-12b | Reconciliation consumer: `expected_event` patterns and `changeset` propagation; the six launch patterns (vendor visit without invoice, registration without confirmation, cancellation confirmed then charged, return without refund, promised estimate overdue, school-cycle packet missing); `fallback_plan` on repetitive choices (`shadow`) | BENCHMARK_ADOPTION §2, §3; RFC-001 §3a | yes (`expected_event`, `changeset`) | Given an expected event's window passes with no matching signal, when the sweep runs, then a `missing_expected` result opens with candidate decisions routed by materiality, and the member is never asked to check. Given a source change (soccer moves Saturday to Sunday), when propagated, then dependent work is recomputed, stale work is retired, safe changes apply automatically, and exactly one genuine tradeoff reaches the member. Given the preferred option fails, when the fallback runs, then the approved substitute is used within authority before anyone is asked. |
| Q-13 | Verification with materiality, hard-stops and just-culture analytics, M-26; `validity_class` runtime behavior (freshness and intentional forgetting); Household Operational Pressure from operational evidence only (`shadow`) | Verification spec (A130); RFC-001 §3; BENCHMARK_ADOPTION §3 | none | Given a `safety_access` field past its verification interval, when the sweep runs, then a hard-stop appears on the HOM briefing; `convenience` fields never hard-stop; a real finding honestly surfaced scores as strongly as a clean audit. Given a `life_stage_bounded` fact past its context, when read, then it is flagged stale before reuse. Given a heavy week by open loops, deadlines and collisions, when pressure is computed, then optional prompts defer, decisions batch, and no stress, emotion or health is inferred (CI guard). |
| Q-14 | Task stacking within the day; portfolio load sheet as a computed view; `dueness` on appliances and pars once close-flow condition inputs exist; recall matching (REQ-047 promoted) against the appliance registry; weather and freeze layer on the environment feed (`shadow`) | Task Stacking spec (A128), Portfolio and Team Ops §5; COMPETITIVE_FEATURE_INPUTS §4, §5 | none | Given a HOM's book, when the week is planned, then date-certain anchors, flexible pool and ballast queue are computed; the capture budget is measured per household and shown to corporate; cross-household batching does not exist (CI guard). Given a CPSC recall matching a registered nameplate, when the daily job runs, then a corporate alert opens with the household and asset. Given a freeze forecast for the household's ZIP, when the sweep runs, then the seasonal trigger fires in window. |
| Q-15 | Guided / Normal / Expert mode logic and mode history | SPEC_MODE_LOGIC.md | yes (mode history table) | Given a HOM's evidence on a household, when promotion criteria are met, then the mode advances with provenance and both signatures; demotion on a high-consequence error is automatic and logged; briefing density follows mode. |
| Q-16 | Cohort record, scenario evaluation form, doctrine-change propagation by consequence class | SPEC_MODE_LOGIC.md §4, WK-TRN-009 | none | Given a doctrine change tagged `high_consequence`, when published, then affected HOMs receive a re-demonstration gate; `editorial` changes are silent; every certification check is recorded the day it happens by the observer who watched it. |
| Q-17 | Scenario delivery in-product (doubles as the AI evaluation harness, FIXTURES §5): the Synthetic Training Household as a simulated household with scripted events, trainer controls, scoring capture | FIXTURES.md §3 | none | Given a trainee account, when a scenario runs, then events fire on schedule, the trainee's actions are captured against the five-item evaluation form, safety scenarios allow no partial credit, and nothing from a training household reaches a member surface. |
| Q-17b | `ai_behavior_version`, evidence envelope, reversibility classes and handoff packet contract; release pipeline through Household Zero | SPEC_MODE_LOGIC §4a | yes (`ai_behavior_version`) | Given an AI behavior change, when proposed for promotion, then it carries a version record, passes the scenario suite and abuse cases with no partial credit, runs on Household Zero first, and can be rolled back in one action; every proposal carries its evidence envelope. |
| Q-18 (R) | Reconciliation; shadow-mode audit; E2 promotion plan; M-series review (necessary member touches, decisions required, process minutes per verified outcome, expected-event misses caught, ownership trace M-27) | all | none | Given all `shadow` features, when audited, then each has a promotion criterion tied to the E2 test and a measured baseline of process minutes. |
| Q-19 | Trigger library conversion consumed: `docs/triggers/*.yaml` loaded into the versioned library with stage tags and suppression classes | WK-APP-002 (converted) | none | Given the yaml, when loaded, then every trigger validates against the schema, the three launch cascades (kindergarten, meds day, occasion radar) fire on fixtures, and corporate can edit dates in-app. |
| Q-20 (R) | Hardening and pen-test remediation; managed KMS migration when funded; final E1 reconciliation | WK-SEC-001, Tech Schedule | none | Given the assessor's report, when remediated, then the retest passes and thirty stable worker days are logged. |

Corporate tasks outside any session, started now: SMS/voice provider selection and A2P/10DLC registration (gates Q-9); Apple Developer Program; managed KMS choice; trigger table conversion (founder; gates Q-19); fixture content (founder and COO; gates Q-8); RFC-001 enum sign-off (done by the ruling); security assessor engagement.

---
status: frozen
---
# Preparation batch, 4 September 2026

Author: Rachel Letters, CEO. Paste to Claude Code after the two ruling files. This runs alongside the build queue, not instead of it: the build continues under the standing authorization, and these land as they are ready. Items 1 to 3 come first because they are diagnostic and may change what I do next week.

Standing constraints for the whole batch: document-only unless an item says otherwise; invent no doctrine and no household facts; mark every blank as needing me or the COO by name; report each item as it lands rather than batching the reports.

## 1. E1 readiness, as one live page
The gate's conditions are scattered across the v7.0 model, WK-SEC-001, WK-FIN-012 and the queue: security test passed and retested, code and accounts in the company's name, insurance bound, payroll live, thirty stable days, a real member account on the digest for a month, and whatever else the sources actually name rather than what I have just listed from memory. Produce one page mapping each condition to its evidence, its current state and its owner, and keep it current as items land. Today nobody can answer "how close is E1" without reading four documents.

## 2. A dependency map from today to E1
Every queue item, founder task and corporate task that gates another, with lead times attached where they exist: A2P registration at two to four weeks, insurance binding, assessor scheduling, formation. Output the critical path explicitly. I have been reconstructing it by hand and am probably wrong in places; tell me where.

## 3. Seed-evidence instrumentation check
E2 and E4 are measured from time records, retention with reason codes, referral events and process minutes. For each measurement the milestones name, say whether a producer exists today, is queued, or is assumed. Discovering in 2028 that referral share was never recorded would be expensive; discovering it this week is free.

## 4. The security-test handoff package
Assemble what WK-SEC-001's scope implies an assessor will want: system description, architecture, data-flow, the guard inventory, the erasure and vault design, the permission model. All of it exists in pieces. Assembling it now means engaging an assessor the week the entity exists rather than preparing for two weeks after.

## 5. WK-APP-002 extraction
Extract the seven life-domain sections into the trigger loader's YAML shape, one file per section. Fill trigger source, cascade steps and tier behaviour from the document. Leave lead time, stage, consequence class, suppression class and materiality blank for me. Where an IF names a human description rather than a field or registry, resolve it against the schema and say so; where it cannot be resolved, mark it. Report the row count per section, and say whether section 3.7 is rows or doctrine that belongs in the loader's header.

## 6. Three fixture household workbooks
Structure complete, every value marked invented, traps placed per FIXTURES.md, archetypes per The First Ninety Days. The COO and I correct rather than compose.

## 7. An empty Household Green workbook
In the exact import shape, every field labelled and every registry sectioned, so filling it is answering questions rather than deciding what the questions are. Pre-fill anything derivable from public sources (parcel record, school assignment, county service calendar) marked `estimated` for me to confirm. Invent nothing about the household itself.

## 8. Decision Rights confirm-or-amend sheet
Render the seeded tier rows as a one-page sheet per tier, so the COO's 25 September review is a page of yes or no rather than a database query.

## 9. Draft evaluation forms and scenario stubs
Turn the twelve rubric domains and their anchors into the evaluation form's actual shape, and draft thirty scenario stubs from the WK-TRN-007 categories A to G. Leave the judgment to the COO: which failure modes matter, what carries no partial credit.

## 10. Freeze packet item A, laid out
Three shapes for whether a visit's hours reach a member archive, each with the exact D7 language that would need amending under it, so the COO and I rule in a sentence rather than reconstructing the question in the room.

## 11. G-register triage
Mark every entry blocks-E1, defer, or stale, with one line each. There are 122 and some are certainly stale; one may be a gate nobody has looked at since August.

## 12. Two queue rows to open (build, not documents)
- **Foundation Reset workflow.** Every household begins with a Reset, it carries its own certification under WK-SVC-004, and no queue item builds it. Open the row with acceptance criteria; place it where the queue's logic puts it.
- **Staff-side member-view render.** Read-only, corporate-scoped, so the COO can judge what a member would see without a member surface existing. Scope it as a corporate render. If it cannot be built without crossing the WK-DEV-007 freeze, say so plainly and do not build it.

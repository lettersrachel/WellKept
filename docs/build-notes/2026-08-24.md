---
status: living
---
# Weekly build note · week of 24 August 2026

The first note under WK-DEV-006 section 6. One page: phase, acceptance
lines passed, run-rate, blockers with age. Living until the Friday
close of the Household One sprint fills in the dry-run result and the
go/no-go; the register transcription (draft A569) freezes the outcome.

## Phase

Phase 0 (Truth) is closing: the delta report is delivered and frozen,
its two section 8 escalations resolved as ADR-007 and ADR-008, the
system-of-record map is delivered, and the CAND ledger is CLOSED with
every candidate assigned. Remaining Phase 0 items are founder-side:
the run-rate statement and the staging dashboard standup. The build now
runs under WK-DEV-007 (expanded internal build) and WK-DEV-008
(workload forecasting incorporated), with the Household One sprint as
this week's frame.

## Acceptance lines passed this week (dev side)

- Sprint Day 1-2 security lines, code half: permission matrix coverage,
  tenant isolation checks, S2/S3 ACL spot checks, audit-log write
  verification, secrets tree scan, and the erase-household dry run
  against a synthetic household, all green.
- Fifteen CI guards green, including the two added this week:
  client-duration (the D7 staffing wall) and telemetry-discipline
  (PRIV-01, a Phase 1 line, landed before any real data).
- Substrate landed: REL-01 feature flags and kill switches, OUTBOX-01
  (migration 0037, the generalized event outbox with data carry
  proven), and the shadow engine scaffolding with the A0 cap enforced
  in code and every rail proven in both directions.
- Input spine: visit-page captures are offline-first commands, wizard
  drafts survive a reload, EXIF strips at the photo boundary, and the
  airplane drill now covers capture as well as the close flow.
- SYN-01 journeys grown: consent (recorded and refused, with its audit
  row) and permissions (client walls, tenant isolation at the page
  layer) run in a real browser alongside the airplane spec.
- The client weekly digest is built and DARK behind its flag; the
  sample went to the founder today for tone and content review.

## Dry run, dev half (24 August; the founder's go/no-go stays Day 5)

The rehearsal ran on a synthetic twin, end to end through the real
mechanisms, no shortcuts:

- Intake: a synthetic transcription workbook in the WK_PLAY_002 shape
  went through wk_import.py (real-import path, explicit sensitivity on
  every row) and load-seed into the local database. Eight fields landed
  with sensitivities, flags, and provenance intact; the s3 row carried
  no value, per doctrine.
- Visit: applied through the real sink. The time entry, the two
  covenant events (personless payloads), and the close-flow deferral
  all landed in one transaction, correctly associated.
- Digest: composed from the twin's real rows; carries the deferral
  under the client card language and names no staff.
- Correction: captured and then corrected through the real intake
  surface; the audit trail carries both writes as field_write rows
  with value hashes, actor stamped. The s3 rail proved live in the
  browser: reclassifying a valued field to s3 clears its plaintext.
  All of this is now a repeatable SYN-01 intake journey in CI, not a
  one-time drill.

One defect found and fixed by the rehearsal: load-seed.ts attached the
three demo identities to EVERY household it loaded, so the real HO
import would have granted demo accounts roles on a real household, and
a second house_manager assignment makes the field-surface resolution
order-dependent. Demo accounts now attach only to the bare db:seed
default; an explicit seed path must opt in with --with-demo-accounts.

- Stranger mode and vault behavior, data layer: the mobile briefing
  API against the twin's shape. The s3 field appears nowhere in any
  payload (its value lives only in the vault path); the s2 CRITICAL
  allergies field reaches both staff projections, which is correct
  since a covering stranger needs it; the stranger flag flips for
  backup_hm; a client session is refused outright. The on-device half
  (the Expo app itself) stays a manual founder check.

Still founder-side for the real Day 5: the HZ workbook, HO selection
and consent, the digest approval, the on-device mobile walkthrough,
and the go/no-go itself.

## The shadow session (24 August, evening; WK-DEV-007 section 3 landed)

The anticipation engine now runs in shadow mode end to end:

- Migration 0038: the shadow_log table. Every CHECK proven red and
  green in SQL: confidence bounded, authority class known, the score
  whole or absent (the house resolution pattern), and one row per
  distinct input state per trigger per household (the unique index, so
  identical re-evaluations never add scoring noise). The next
  production deploy's three-way migration count reads 39.
- The table sink, the first real trigger (condition-decline over the
  W-5 look series; v1 parameters are marked proposals for the scoring
  loop to calibrate), and the worker's hourly shadow pass, kill
  switches read from the REL-01 flags each run. Proven on the real
  database: recorded once, deduped on replay, silenced by the kill
  switch, and a new row when the inputs actually change.
- The scoring surface on the oversight drill-in, visible to
  corporate_admin and cfo_readonly ONLY (corporate_ops never receives
  the rows; the query is role-gated, not the render). Scoring is the
  founder's: one click writes the whole score with its audit row;
  corrections re-record, the consent pattern. Proven in a real
  browser end to end.
- The Cockpit SIGNALS panel, empty until a per-trigger promotion flag
  flips, and even then gated by the A0 cap in code.
- The support work the session forced: shadow_log joined the erasure
  tool's DELETE exceptions with its written reason (the guard proven
  red first), the payload guard gained the shadow-row signature both
  directions, and the staff-disclosure guard detected scored_by on its
  own and carries the written allowlist reason.

## The evening authorizations (proceed with recommendations)

The founder's evening instruction resolved the open rulings; the frozen
record is FOUNDER_INPUTS_2026-08-24_EVENING.md. In brief: stranger mode
is ruling (c), the per-field marker, BUILT the same evening (migration
0039, the permission-core overlay at 100 percent coverage, the
server-side stranger projection with a one-gesture toggle and split
offline cache, the capture checkbox; proven live in four directions on
the twin). The day's route stays the assigned-households list at
training scale, evidence-gated. The cause-code taxonomy v1 is adopted
as a section 8 default (six codes; build rides the next migration
session). Task Inventory resolves to reconstruction-as-v1.4, so WL
Gate 0 unblocks. The digest sample is approved; the production flag
flip is the founder's knob write. The go/no-go recommendation stands
as the directive's own structure and currently resolves to the
fallback unless the founder-side Phase 1 lines turn green by Friday;
the decision itself remains Friday's, made by the founder.

## Blockers update (evening)

Items 1, 4, and 5 of the blocker list above are resolved or reduced by
the evening authorizations; items 2 and 3 (the HZ workbook and the
staging clicks) and the run-rate line remain founder-side, physically.

## Run-rate vs budget

Founder-side by design; figures never enter this repo. The statement
travels beside this note, not inside it.

## Blockers, with age

All filed 24 August (age 0 days at writing); anything still open next
Friday gets its register-visible note per section 6.

1. Task Inventory v1.3 locate-or-reconstruct ruling (founder). Blocks
   WL Gate 0 and the forecasting segments.
2. Household Zero transcription workbook copy (founder). Blocks the
   Day 2-3 intake slice verification and the importer round-trip.
3. The six staging dashboard clicks per STAGING_RUNBOOK.md (founder).
   Blocks the staging repo-side half; URLs come back to the build.
4. Digest sample approval (founder). Gates the client_weekly_digest
   flag; nothing sends until it is set.
5. The WK-DEV-007 section 2 media-release-at-capture line conflicts
   with the REQ-006/071 reuse-gating design; no household-level media
   flag exists. Reported as a document conflict, both readings held;
   a ruling, not a default, resolves it.

## Questions carried to the founder (section 8)

The G-61 date-render fix awaits authorization (UTC render now, or the
date column at the Temporal window; recommended, not chosen). The
Approve-control relabel from G-55's close-out. The two input-spine
DECISION items: the five-interaction flag form against the W-5 design,
and where the flag-not-block validation line sits.

## 25 August, overnight: Household Green provisioning (the tester doc's first deliverable)

Per WK_Tester_Provisioning_Household_Green section 6, reporting the
three lines the next weekly note owes:

1. **The account path is built and proven.** Migration 0047 adds
   `auth_user.is_tester` (the single exclusion filter the doc
   requires; access stays the role's, exclusion is the flag's, and
   every future covenant, payroll, or learning computation owes it a
   filter). `pnpm db:hg --email <tester's address>` provisions the
   pseudonymized Household Green tenant (codename as name, no
   address, no contacts, s3 empty, NOT a fixture: the exclusion rides
   the user flag per the doc), the HOM-role tester assignment
   (house_manager, never trainee, no corporate role by default), and
   the starter set (4 fields including two room overviews, 3 assets,
   1 rhythm item, 1 open loop, every row marked fixture-origin in its
   own note). Proven locally: refuses without --email (her address
   never enters the repo), idempotent on re-run. The founder handed
   the session her sign-in address on 25 August (held out of the
   repo per the provisioning rule); the PRODUCTION run now waits
   only on this migration reaching production: the next deploy
   applies 0037 through 0047, and the founder then runs the db:hg
   one-liner from her own machine against the production store, the
   knob-flip pattern (this container has no production access by
   design).
2. **Isolation is green against a tester-shaped account, in CI.** The
   tester journey joined SYN-01 as release-blocking: a tester-flagged
   HOM reads only their one household on /visit, another tenant's
   drill-in and briefing API refuse, the corporate boards (where
   every Ruling 1 surface lives) refuse even for their own household,
   and the exclusion flag reads true.
3. **Device path chosen: Expo Go as the interim, web Cockpit as
   fallback.** The dependency check ran: no custom native modules;
   every native dependency is an Expo-Go-bundled SDK module, so the
   doc's better-interim condition holds. Caveats recorded in
   apps/hm-mobile/README.md (permission copy falls back in Expo Go;
   no push; AsyncStorage offline persistence works). EAS iOS internal
   distribution is already configured (internal profiles, bundle id
   com.wellkept.hm); the day the Apple Developer enrollment clears
   (founder-side, the pacing item), credentials and her device UDID
   land and the native build ships, then the airplane drill re-runs
   on it.

Adopted the same night, frozen with manifest hashes:
WK_Tester_Provisioning_Household_Green_2026-08-24.md and
WK_Sprint_Household_Green_HG_2026-08-24.md (register draft A576 and
the A569 revision; Household One naming is superseded by Household
Green throughout the open work, dated records keeping their wording
per Ruling 3). The zip's other documents hash-match the copies
already adopted; the Phase0-Close copy differs only where the repo
copy deliberately documents the run-rate template instead of carrying
its blank amount lines, the standing figures-never-enter-source
adaptation, recorded at adoption.

## 25 August, the full day: deploy, sitting, directives executed (the WK-DEV-011 section 6 contents)

**Phase and acceptance-line status.** Production serves `50ecd0f`, the
tenth clean run; the DEPLOY section 4 sitting ran COMPLETE against it
the same day and its record is frozen (SECTION4_SITTING_2026-08-25.md,
superseding 6 August; every standing check has now run against a live
build, and the erasure tool was proven in both directions in one
sitting). PRs #152 through #177 merged. Migrations 0056 (SITUATIONS
bundling: composite tenant FK, manual bundling only, grouped brief
delivery) and 0057 (the PreferenceRule primitive: explicit-only
creation, confidence whole-or-absent by CHECK, retirement the only
mutation) landed after WL Gate 1 closed (0049-0054) and the versioned
capacity configuration (0055). Incidents G-63 and G-64 were filed AND
structurally fixed same-day; check 15 (the invisible-household census)
and the legal-census guard (the fifteenth) joined CI, each proven red
first.

**The A581/A582/A584 execution, same day.** Ruling 1 amended to option
(b) with CLAUDE.md citing A581 in the same PR as the board's
founder/CFO capacity section, the gate enforced in the permission
matrix and proven by role-based retrieval tests in both directions.
The eight CAND assignments transcribed verbatim into the ledger, both
holds standing (RelationshipMomentRecord schema-free pending the
founder's definition; AIHumanHandoff with the Tier M gate).
**Task Inventory status:** v1.3 FOUND and amended to v1.3.1 by
register (A584: T-345..348 appended, T-206 retired; repo extract
pinned by sha256 d71e27e0...); v1.4 rev2 DELIVERED founder-side with
the finished MAPS_TO join (64 rows: one-to-many mappings, 11 NO-MAP
with written reasons after the amendment closed four, 8 UNCERTAIN for
the founder); adoption waits on the combined review block, and the
freeze/flip/loader/Gate-2 sequence is set behind it.

**The grant audit reference (A581 item 1): DELIVERED.** `pnpm
db:grant` was built and proven (five refusals red, accepting shape
with ADR-006 token and required reason), and the founder RAN it in
production the same evening: `lettersrachel+ftc-admin@gmail.com` to
corporate_admin on Field Test Home, ndaApproved false, subject token
32a207a2 (kind=email), `role_assigned` at 18:34:35 carrying
`provisionedVia: db:grant` and the register citation in its reason
field. The census then read no household without a corporate holder,
so the check-15 allowlist came OUT in the same day's work and is empty
again: the first exception was disposed of rather than carried. One
unaudited write is recorded rather than claimed as covered (the
grantee's auth_user row, inserted directly because db:grant refuses to
create people and auth_user has no household to attach an audit row
to). The grant surfaced G-65 and confirmed a month-old invisible
tenant; both are in the register and the inspection document. Two follow-up actions were
attempted the same evening on the now-reachable drill-in as the
ftc-admin identity, and this note originally recorded both as done.
CORRECTED: the field-role REVOKE DID NOT WRITE (zero role_revoked
rows on that household; the assignment stands), and the capture
dismissal is UNVERIFIED at the time of writing. Both were reported
clean by the operator; the trail disagreed on the one we checked.
Retry-with-verification is the next action, and no claim is made here
until the audit rows say so. Check 15 still passes on the corporate
assignment either way; G-65 stays OPEN and untouched.

**Verification outputs:** the section 2 verification ran complete
against production; the verbatim outputs are preserved in the frozen
sitting record rather than duplicated here, with the instruction's
priority finding expressly clean: the erasure dry-run plan enumerated
fixture-household rows ONLY.

**Input-spine metrics:** airplane and journey drills green in CI
(24 e2e); live close-flow interaction times await the tester's first
sessions (instructions delivered; invite armed on her signed
agreement).

**Confirmations owed by the brief:** the provisional-source flag
confirmed by query 25 Aug morning (task_definition: 4 rows, 4
provisional, 0 canonical); the recall job confirmed code-side (exists,
weekly, deduped, tested), Railway execution check still founder-side.

**Run-rate versus the modeled line:** the founder-side statement is
still pending (figures never enter this repo); no new spend was
committed dev-side. One cost-adjacent observation: the dormant
well-kept-web Vercel project burned its 100-per-day free-tier build
quota on today's PR volume; disconnecting its git integration (after
the A571 inspect-and-rotate) stops the burn.

**Dependency-ledger delta:** none. No new dependencies; the pinned
stack holds.

**Blockers, with age:** the combined review block (floor rows + WKT
verdicts + 8 UNCERTAIN + 11 header-row rulings), new today · staging
dashboard URLs, 1 day · the run-rate statement, 1 day · Apple
Developer enrollment (paces the native tester build), 1 day · Lauren's
signed agreement, new today. CLEARED today: the Field Test Home grant
run (done, above). NEW, reported not chosen: G-65's /visit
household-resolution rule, a founder decision between three shapes.

**Deliberately NOT built, standing:** the optimizer, ambient inference
until the Tier M gate, native vendor app, vendor marketplace,
autonomous scheduling, custom predictive ML, broad smart-home
ingestion; productivity ranking recorded as never, not yet.

## 25 August, late evening: G-68, the operator's other half

Filed and fixed the same night, out of the founder's one-line question
after the corrected G-67 record: clicking Revoke, "it says nothing when
revoked, its gone". The saying nothing was the finding.

**What the census found.** All 57 exported server actions write. Thirty
confirmed with a green line naming what landed; twenty-seven ended at
`revalidatePath` and said nothing at all. The silent list is the
ordinary work of the product: assignRole, revokeRole, setStatusTag,
reviewEdit, setVaultValue, recordHouseholdConsent, resolveIncident,
both exclusion actions, forceSignOut, resetTotp, captureField,
setMonthlyRate, the gesture trio, the photo toggles, scoreShadowSignal.
G-29 (27 July) made every refusal visible on the reasoning that an
operator cannot tell "declined" from "down"; the same reasoning was
never run in the success direction, and that is precisely why the G-67
evening could not be settled at the screen.

**Built.** `recorded(householdId, what)` beside `refuse(householdId,
reason)`; every silent action on it, the two hand-built redirects
normalized onto the helper, so all 57 confirm. `RecordedBanner` as a
component in place of four copied inline blocks, now rendered on the
five confirmation surfaces that had none (fleet board, economics,
triggers, /intake, the client /playbook). The sixteenth guard,
`success-visibility.test.ts`: every state-changing action confirms or
is excused in writing (allowlist EMPTY), every confirmation target
renders the banner bound to the redirect's own param, both inputs
computed with floors, proven red five ways before being trusted.

**Two riders from the proving, not the writing.** The red-proof found a
hole in the ELEVENTH guard: refusal-visibility read literal arguments
only, so an action pinning its surface in a local const (the three task
actions) was invisible to it and `/oversight/tasks` could have lost its
refusal banner with CI green. Both guards now resolve local consts, and
the extension was proven by deleting that page's banner and watching
the older guard finally fail. And G-29's conversion is complete on
every staff surface; `proposeEdit` is the deliberate exception, since
the refusal vocabulary is staff-voiced and a member's refusal copy is
the founder's call.

**Stated plainly: G-68 does not close G-67.** A confirmation proves the
code path ran to its end, never that the row committed, and nothing at
all about a POST that never leaves the browser. What changes is that
the same evening would now be legible: no green line means it did not
run. The network-panel click is still owed.

**Copy:** every message is a proposal, one string each (the AG
precedent). The one rule that is not copy: a message names WHAT was
recorded and never a value, because it rides in a URL and lands in
browser history; the guard enforces that for the value-hashing actions.

**Suite:** 17 web test files, 48 tests, full turbo suite green;
twenty-five e2e (the new journey drives assign and revoke on the real
controls and reads the audit row behind each banner). Migration count
unchanged: this unit adds no schema, no data category, and therefore no
legal-document obligation.

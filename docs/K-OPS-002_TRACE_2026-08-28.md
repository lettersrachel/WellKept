---
status: living
---
# WK-OPS-002 platform capability trace, run against the monorepo

28 August 2026. The document is being re-pointed from Jobber to the monorepo
per D1. Rows supplied by the founder verbatim from the library archive; this
is the trace run against the code.

**Method, and it is the whole value of the exercise.** Verified is read from
the code and nothing else. Not from the SOP, not from the row's own
expectation, not from what the interim control assumes. All three describe
intent; only the code describes behaviour. Every verdict below carries a file
and a symbol, and where a one-line expectation is not enough to trace
against, the row says so rather than guessing what the SOP meant.

**Vocabulary, fixed before the first verdict so it cannot drift row to row:**

- **yes**, the behaviour exists and holds.
- **partial**, the mechanism exists and something material about it does not
  hold, and the note says which. Partial without a named gap is a yes wearing
  a hedge.
- **no**, the subject is absent from the code.
- **unverifiable** is reserved for a row whose SUBJECT lies outside the
  repository (a paper process, a device behaviour, a vendor). It is not a
  softer `no`. Collapsing the two makes the trace read better or worse than
  it is depending on which way the collapse runs.

---

## The four ruled rows, recorded rather than assessed

Recorded as given. The code note is only there so the record shows what each
interim control is standing on.

| # | Row | Ruled | Interim control as ruled | What the code shows |
|---|---|---|---|---|
| 1 | Geofenced or location-stamped clock-in | **partial** (R21) | The existing HOM-entered times | Two `datetime-local` fields the HOM types (`VisitWizard.tsx:398-399`). No geofence, no location stamp, anywhere. REQ-031 is amended today to say HOM-entered times; REQ-036 keeps the geofenced version at P1 |
| 2 | Encrypted secure fields, visible only on assignment | **partial** (R20) | Real s3 values on paper until WK-SEC-001 clears. **No third-party vault product** | The vault is built and works: `vault_item`, the audit-before-decrypt invariant (`api/reveal/route.ts:59-70`), crypto-shred erasure, boot-time KEK validation, a proven fixture round-trip. The ruling governs which VALUES may enter, not whether the mechanism runs. Note that the interim as WRITTEN named a dedicated password manager; the ruling replaces that with paper |
| 7 | ACH bank debit as the default auto-pay rail | **no** (R19) | Signed paper mandate at intake, collection by member-initiated bank transfer. **The card interim is VOID under D5 and 24.6** | No payments code of any kind: no processor, no abstraction, no mandate object, no webhook receiver. The D5 shortlist is delivered and the selection clock is running |
| 12 | Training and certification checklists gating solo scheduling | **no** (R22) | **CORRECTED, see section 11.** The interim as written is TWO things: the readiness checklist as a controlled document, AND the CEO enforcing the scheduling gate manually. This row first recorded only the second. D6's trainee role is sequenced on the third HOM or the first cohort the founder does not personally train | No checklist object, no per-item sign-off, no completion tracking. `TrainingState` is stubbed BY NAME in the training-household seed header, unbuilt. The trainee role has zero references outside two comments saying it does not apply |

---

## The ten rows verified

### Row 3. Per-user access logs: who viewed or used which household's access data, and when

**Verified: partial.** Two different halves, and only one is software.

**Viewed is logged, and logged well.** `api/reveal/route.ts:59-70` writes an
`audit_event` carrying `householdId`, `actorUser`, `actorRole`, `fieldId`,
`kind` (`s3_reveal` or `s3_corporate_view`) and `detail: { field, at }`,
**before** the value is decrypted, and a failed audit write refuses the reveal
(`route.ts:68-70`). Who, which household, which field, when, and under which
role. That is the row's question answered directly.

**The gap is one the code already documents against itself (G-53).** The row
records that a reveal was AUTHORIZED and ATTEMPTED, not that a secret reached
anyone. On 2026-07-29 three identical rows covered one real exposure and two
failures. So the log answers "who was permitted to see this and asked for it",
which is not quite "who viewed it".

**"Or used" has no representation at all.** Key sign-out, physical custody, a
code actually being entered at a door: none of that is in the system, and the
row's own interim control puts it on paper ("key sign-out records satisfy the
chain of custody"). Correctly scoped as paper, and named here so the trace
does not read as if the software covers it.

### Row 4. Photo capture inside the app that bypasses the device camera roll

**Verified: partial, and the two surfaces differ, which the row does not
anticipate.**

- **Native**: `ImagePicker.launchCameraAsync` after an explicit camera
  permission request (`hm-mobile/App.tsx:314-316`). That opens the camera
  directly rather than the photo library, which is the behaviour the row is
  asking for.
- **Web**: `<input type="file" accept="image/*" capture="environment">`
  (`VisitWizard.tsx:416-417`). `capture` is a HINT to the browser. On iOS
  Safari the picker can still offer the library, and the row's claim cannot be
  made for this surface from the source.

**Whether the captured image lands in the camera roll is an OS decision in
both cases**, and nothing in this repository asserts or controls it. Adjacent
and real: `jpeg-strip.ts` removes metadata before upload, which is a different
protection than the one this row asks for.

**The row's expectation is not traceable to a verdict from source alone.** It
says "verify capture path on iOS and Android", which is a device test, not a
code read. Named as a device test rather than an SOP ambiguity, because those
are different kinds of insufficiency and the remedy differs: this one needs a
phone, not a document.

### Row 5. One-tap client approvals for add-on quotes and over-limit purchases, logged

**Verified: no.** No quote object, no approval flow, no spending-limit concept
anywhere in the schema.

**What exists and is NOT this, named because it is close enough to be
mistaken for it:** client playbook edits land in a review state and merge only
on corporate approval (`actions.ts:195`, audit detail
`via: "client_edit_approval"` at `:225`). That is corporate approving a
client's data edit. This row is a client approving a corporate spend. Same
word, opposite direction, and no money in it.

**Spending authority exists only as free text**, in a Fernbrook demo playbook
field ("Sixty dollars per order without asking... above that Lisa approves").
A household fact in prose, not a limit the system can enforce or compare
against.

**One note for whoever scopes this:** an approval RECORD is not an invoice, so
this is buildable inside ADR-004's boundary. The app would capture the client's
yes; QuickBooks would still bill it.

**WK-FIN-005 makes this row four things rather than one.** Re-traced in
section 8 below.

### Row 6. Automated dunning: retry, notify, escalate on the Day 0/3/7/14/30 clock

**Verified: no.** Nothing. No payment retry, no reminder schedule, no
escalation ladder, and no payments layer for them to attach to.

**Worth separating two reasons it is absent**, because they have different
futures. The RETRY half is a processor behaviour and belongs to whichever
option D5 selects. The **notify and escalate** half is a schedule the app
could own under ADR-004 without originating a charge, and if it is ever built
it goes through `emitOutboxEvent` and the notification firewall like
everything else, not as a feature with its own send path.

### Row 8. Visit report with checklist, photos, and exceptions delivered to the client on completion

**Verified: partial. Delivery happens; the content is a subset.**

On visit close the client report email sends to `client.email`
(`api/visit-commands/route.ts:44-45`), and it ran in production on 27 August
with the three sentences, the photo count and correct escaping. So the
"delivered on completion" half is real and proven.

**The projection is `CLIENT_REPORT_KEYS = ["report", "photoCount"]`**
(`client-report.ts:24`). The client receives the HOM's three sentences and a
NUMBER OF PHOTOS. Not the photos. Not the checklist. Not exceptions as a
structured section.

**The exceptions exist and stop one screen short of the client.** The drafted
close composes "N of N expected outcomes completed as planned" plus itemized
exceptions, and renders it on the HOM's submit screen. It is not in the client
email.

**The row's own test could not be traced when this was written.** It says the
client-facing format must "meet the QA five-dimension review". **The founder
read WK-SOP-005 and WK-QA-000 on 28 August and that review does not exist
under that name** (G-108). The re-trace against what the SOP actually
requires is in section 8 below, and it supersedes this paragraph's verdict.

### Row 9. Backup HOM assignment, coverage activation, and client notification of substitutions

**Verified: partial. Three clauses, three different answers.**

**Assignment: yes, and more than asked.** `backup_hm` is a first-class role
gating every field surface (`visit/page.tsx:28`, the visit-commands sink,
discard, mobile upload). Assignment and revocation go through audited actions
carrying ADR-006 subject tokens. Beyond the row: stranger mode gives a backup
the amplified first-visit projection (`visit/page.tsx:114-128`), which is the
substantive thing a covering HOM actually needs.

**Coverage activation: no, by boundary.** There is no scheduling in this
system; ADR-004 puts it in Jobber. A role assignment is not an activation, and
building one here would cross the boundary.

**Client notification of substitution: no.** Nothing notifies a member that a
different person is coming. The notification machinery exists and this is not
wired into it. Of the three clauses this is the one with no reason behind its
absence, and it is the one a member would notice.

### Row 10. Pause state: billing stopped, auto-resume on date, records preserved

**Verified: partial, and the middle clause is the sharp one.**

**Pause is a real, audited state change.** `pause` and `resume` are
`membership_event` kinds (`actions.ts:1119`), corporate_admin only, each
requiring an effective date, written in one transaction with the audit row.
They deliberately carry no tier and must not touch `household.tier`, asserted
in `actions.membership.test.ts:86`.

**Records preserved: yes.** Nothing hard-deletes; tombstone plus append-only
audit is the pattern throughout.

**Billing stopped: not represented, correctly.** There is no billing in this
app (ADR-004). The pause records that the state changed; money stops in
QuickBooks.

**Auto-resume on date: NO, and this is the row's real finding.** `resume` is a
second event a person records by hand. Nothing reads `effectiveOn` and fires.
An operator recording a pause with a future effective date could reasonably
believe she has scheduled the resume, and she has not. That gap is invisible
until the date passes and nothing happens.

### Row 11. Incident, near-miss, and feedback modules with categories and logs

**Verified: partial. Two of the three modules exist; the third does not.**

**Incident and near-miss: yes.** `incident_report` (`tables.ts:463-483`)
carries a kind enum of `complaint | breakage | injury | near_miss | other`
(`actions.ts:825`), severity, `occurredAt`, `reportedBy`, `reportedVia`,
a description, an open/resolved status with the resolution triple, and the
engine back-link (`preventableByPrompt`, `relatedRuleId`, `relatedPromptId`)
answered by whoever RESOLVES rather than inferred at creation. Categories and
logs, as asked.

**Feedback: no.** `complaint` is an incident kind, which is feedback only in
the sense that a fire is weather. There is no satisfaction capture, no
solicited feedback surface, and the brief for one is explicitly gated on 60
days of a real household.

**One word in the row was doing a lot of work**, and the founder's SOP read of
28 August settled it: "feedback" is WK-SOP-019's STAFF product-friction
channel, not a client surface. The three modules are three different things
with three different audiences. Re-traced in section 8 below, which supersedes
this entry's two-of-three verdict.

### Row 13. Client-visible billing history, itemized purchases, and pass-through detail

**Verified: no.** The client side has exactly one route, `(client)/playbook`.
`monthlyRateCents` renders only on corporate surfaces (the exhibit page and
`/oversight/economics`). No billing history, no itemization, no pass-through
detail reaches a member.

**Two reasons, and only one of them is a decision.** The client side is FROZEN
at the digest under WK-DEV-007, which is deliberate. And there is no billing
data to show, because payments do not exist. **D7 does not bar this row**:
D7 bars durations, service-hour totals and staffing ratios on client surfaces,
not money. A billing history is unbuilt and frozen, not prohibited.

### Row 14. Emergency contact one tap from every screen; driving mode deferring notifications

**Verified: no, on both clauses, and they fail differently.**

**Emergency contact: the DATA exists, the AFFORDANCE does not.** Emergency
contacts are playbook content in section 9, Safety and Emergency Readiness,
and the demo household carries several. Nothing puts them one tap from any
screen; there is no persistent control on any surface, web or native. A HOM in
a hurry would navigate to the playbook and read a field.

**Driving mode: nothing, and the row itself says why that may be right.** Its
interim names an OS-level driving focus mode on work profiles, which is a
device policy rather than software. Adjacent and NOT this: outbound
notifications are governed by a 9pm-to-7am rule under REQ-079's doctrine, which
is time-of-day, not motion.

---

## What the ten rows amount to

Stated because a row-by-row table hides it: **the pattern is not "unbuilt".**
Six of the ten have a working mechanism with a specific clause missing, and in
four of those the missing clause is the one a member or an operator would
actually notice: the substitution notice nobody sends (row 9), the resume
nobody schedules (row 10), the photos the client is counted but not shown
(row 8), the emergency contact that is data rather than a button (row 14).

Each of those is small. None is blocked by a boundary or a ruling. They are
the residue of building the record first and the moments around it later.

---

## The seven v1.1 additions: which are recoverable, and which need A213

The founder's note suggested three might be recoverable from documents already
held. **Two are, and the third is not, and the reason is worth having.**

| # | Addition | Recoverable? | From what |
|---|---|---|---|
| 15 | Deviations table | **No. Needs A213** | Zero substance in the repository. The only hits for "deviation" are unrelated senses (a STOP instruction in a verification script; the ServiceEvent lifecycle's escalation clause) |
| 16 | Consents arm jobs | **YES, recovered** | WK-SEC-001 test area 4, quoted below |
| 17 | Dwellings | **No. Needs A213** | Zero hits. Multi-property households appear in WK-APP-006 group F, which is a bundle brief not in this repository, and guessing that is the same concept would be inventing the row |
| 18 | Decision-Rights assertion | **No, despite being adopted.** Needs A213 or INSTRUCTION_UPDATES v2 | The CONCEPT is named twice (handoff line 388, "current decision rights that may apply", in a briefing contents list; WK-DEV-010:42, "Decision Rights linked automatically", in the vendor slice). Both are references, neither is a definition. INSTRUCTION_UPDATES_2026-08-05_v2 is deliberately held out of the repository, so the adoption text is not readable here |
| 19 | Member channels | **No, with a candidate named** | REQ-078 covers inbound routing (member-facing phone, SMS, shared inbox routing to corporate surfaces) and came from the same 5 August adoption. That is a plausible match and it is NOT a recovery: matching a subject line to a requirement by topic is exactly the inference this trace is written to avoid. One line of A213 confirms or kills it |
| 20 | Restricted-access class | **YES, recovered** | WK-SEC-001 test area 3, quoted below |
| 21 | Body-adjacent flag | **No. Needs A213** | Zero hits anywhere. Adjacent in spirit to the child-data classification work (`child-data-kinds.test.ts`, CHILD_DATA.md), but nothing names this |

### The two that are recovered, with their expectations

**Row 20, restricted-access class**, from WK-SEC-001 test area 3, verbatim:

> the restricted-access class (do-not-admit, child-pickup, welfare notes)
> enforced server-side with visit-sheet-only visibility and access logging,
> not as UI courtesy an API call walks around.

That is a traceable expectation with four testable clauses. **Not traced
here**, because it arrived inside this task rather than as one of the ten, and
tracing it properly means walking the permission core against three named
data kinds. Named as its own next step.

**Row 16, consents arm jobs**, from WK-SEC-001 test area 4, verbatim:

> Consents are switches arming capabilities; verify server-side checks.
> Photo-processing consent off means the processing path refuses. Clearance
> mode means retention is actually shortened. Portable export is scoped to the
> requesting household only.

Also traceable, also not traced here, same reason.

### The correction that made those two recoverable

**WK-SEC-001 IS in this repository**, at
`docs/library/WK-SEC-001_Application_Security_Audit_Scope.docx`, listed in
`docs/library/` and NOT in `LIBRARY_INDEX.md`, which names eight files while
ten are on disk (G-107). It carries all eight test areas and the pass
criteria. Three
documents produced this session said it was absent. That was wrong, it is
corrected in each of them, and it is filed as **G-106**.

**WK-QA-004 and WK-SPEC-002 are genuinely absent**, confirmed against the full
`docs/library/` listing rather than against a grep.

---

## The PAPER-PARITY acceptance test

**Its text is not in this repository.** Confirmed by search: zero matches for
`PAPER-PARITY`, `paper parity`, or `A228` anywhere in the tree, including
`docs/library/`.

The flag stands and is now sharper, because the trace has run. **Three of the
four ruled rows resolve to paper as the interim control**, and rows 5, 6, 13
and 14 have paper or manual answers behind them too. If the test asks whether
the software matches what the paper process does today, those rows pass by
definition, since the paper process IS the control. If it asks whether the
software can replace the paper, they all fail.

**That is not a close call between two readings; it is opposite verdicts on
roughly half the document.** Its text should be read before anything is
assessed against it.

---

## Rows where one line was not enough

Recorded separately so the founder can pull exactly what is needed rather than
a stack of SOPs.

| Row | What is missing | What would settle it |
|---|---|---|
| 8 | The QA five-dimension review's dimensions | WK-SOP-005, or whichever QA document defines the five |
| 11 | What "feedback module" means as distinct from complaint | WK-SOP-018 or WK-SOP-019 |
| 5 | Whether the purchase limit is a system field or a household fact | WK-SOP-015, WK-SOP-016, or WK-FIN-005 |
| 4 | Nothing documentary. **This one needs a device**, not an SOP | An iOS and an Android capture test |
| 15, 17, 18, 19, 21 | The expectation text itself | A213 (18 alternatively from INSTRUCTION_UPDATES v2) |

---

## 8. Rows 8, 11 and 5, re-traced against the SOPs

Added 28 August 2026, after the founder read WK-SOP-005, WK-SOP-018,
WK-SOP-019 and WK-FIN-005 and supplied their requirements. **These verdicts
supersede the three in section 2 above**, which were traced against a one-line
expectation and were correct as far as that line went.

**Each of the three turned out to be several rows wearing one number**, which
is the strongest argument for pulling the SOPs before re-running the trace as
a backlog: a row that is really four requirements cannot be closed, only
partly done.

### Row 8, re-traced: eight things, not one

**One of the three is a citation defect and it is recorded rather than
resolved.** The row's acceptance test names a "QA five-dimension review".
**That review does not exist under that name**: the founder searched
WK-SOP-005 and WK-QA-000 and the phrase appears in neither. WK-SOP-005 has
THREE verification levels (an end-of-visit HOM self-check, a same-day CEO
review of every report, and a quarterly formal review with the client), and
the most likely referent is **Level 2's five required elements**, which are
the client-facing report's contents and therefore the row's actual subject.
Traced against those. Filed as **G-108**.

**The five required elements of Level 2:**

| # | Required element | Verified | Evidence |
|---|---|---|---|
| 1 | Task completion checklist | **partial** | Captured: the close flow tracks `requiredTaskIds` against `completedTaskIds` and refuses to submit with tasks incomplete (`close-flow/index.ts`). **Not delivered**: the projection is `["report", "photoCount"]`, so no checklist reaches the client |
| 2 | One photo per room in scope | **no** | Photos exist (`visit_photo`) and are counted. **There is no room concept on a photo, and no scope concept at all.** A photo carries household, bytes, uploader, retention and reuse flags, and nothing that says which room it is of, so per-room coverage is not merely undelivered, it is unrepresentable |
| 3 | Expenses and receipts | **partial** | `cost_entry` is real and good: category, `amountCents`, `incurredOn`, `recordedBy`, optional `note`, and `receiptPhotoId` pointing at a `visit_photo` so a receipt shares one photo lifecycle. **Not delivered**: no cost or receipt reaches the client report |
| 4 | Any flags or follow-up items | **partial** | Rich capture: `condition_flag`, deferrals with their client-readable reasons, dots, paused decisions. The DEFERRAL half does reach the client, on the playbook page under "Noticed, and planned for later". **The visit report itself carries none of it** |
| 5 | Playbook update notes | **partial** | `changesNoticed` is a required close-flow step with "none" as the valid quiet answer, and field changes are captured and audited. **Not delivered** in the report |

**The pattern across all five is one gap, not five.** Every element is
CAPTURED, four of them well. **The projection is the wall**: `CLIENT_REPORT_KEYS`
is two keys, and G-78's payload-shape guard means adding a key is a deliberate
act with a declared list, not an oversight. So this row is not "unbuilt"; it
is one decision about what the client report contains, and the capture behind
every element is already there.

**The two delivery requirements the row also carries:**

| Requirement | Verified | Evidence |
|---|---|---|
| Report submitted before the HOM leaves the driveway, or within thirty minutes | **no** | `submittedAt` is stamped at submission (`close-flow/index.ts:287`) and nothing compares it to anything. No timing rule, no warning, no measurement. Note the tension worth flagging rather than solving: an offline-first close flow queues on the device and drains on reconnect, so "submitted" has two possible meanings (the HOM pressed submit; the server accepted it) and a thirty-minute rule has to say which one it means |
| Push notification to the client automatically on submission | **no** | Push exists and is staff-only. `push_subscription` rows are registered from the HOM visit surface (`PushRegister.tsx`) for the signed-in staff user, and `notification` rows are keyed to `auth_user`. **The client gets an email, not a push.** No client push path exists at any layer |

**Row 8 overall: partial.** Delivery on completion is real and proven in
production. Of the SOP's seven testable requirements, one is met, five are
captured-but-not-delivered, and one (per-room photos) is unrepresentable
without a schema change.

### Row 11, re-traced: three modules, three audiences, three answers

The founder's SOP read settles the ambiguity flagged above. **These are not
three flavours of one thing.**

| Module | Source | Audience | Verified | Evidence |
|---|---|---|---|---|
| Incident | WK-SOP-008 | client-facing | **yes** | `incident_report` with kind, severity, `occurredAt`, `reportedBy`, `reportedVia`, description, open/resolved with the resolution triple, plus the engine back-link answered by whoever resolves |
| Near-miss | WK-SOP-018 | staff, reportable and celebrated rather than punished, **monthly CEO pattern review** | **partial** | `near_miss` is one of five `incident_kind` values, so the RECORD exists and is capturable. **The surrounding practice does not**: there is no monthly review surface anywhere (searched; the only "monthly" surfaces are the covenant preview and the monthly rate), no pattern view, and nothing that distinguishes a near-miss from an incident in how it is treated. Filing a near-miss through the incident form is filing it as an incident, which is the opposite of the SOP's celebrated-not-punished framing |
| Feedback | WK-SOP-019 | **staff, about the SOFTWARE**: friction, bugs, missing features, workaround temptations, reviewed monthly | **no** | Nothing captures this |

**The third is the interesting one, and the reason is structural rather than a
matter of priority.** Tell Well Kept is close in shape: a one-box capture on
the visit surface, the HOM's words verbatim, a corporate router queue, filing
or dismissal with a reason. **It cannot serve this row**, because
`capture_artifact.household_id` is `NOT NULL` and foreign-keyed to `household`.
Every capture is ABOUT a household. A HOM who wants to say "the photo upload
fails on this screen" has nowhere to put it that is not attached to a member's
record, and attaching it there would be wrong twice: it pollutes a household's
record with software complaints, and it makes staff product friction subject
to household erasure.

**So the nearest existing mechanism is not a partial implementation of this
row; it is a different mechanism that happens to look like one.** Recorded
because the cheap read is "Tell Well Kept covers it", and the tenancy column
says otherwise.

**Row 11 overall: partial**, one of three modules built, one capturable
without its practice, one absent and not reachable from the closest thing.

### Row 5, re-traced: four things

WK-FIN-005 makes this concrete where the one-line expectation was not.

| # | Requirement | Verified | Evidence |
|---|---|---|---|
| 1 | Default limits as configuration: 300 dollars per transaction, 2000 per card per month | **no** | No limit exists in any form. `app_setting` holds `capacity_gate`, `photo_retention`, `visit_reconciliation`, `flag_promotion` and the feature flags; there is no spend key. `cost_entry` records what was spent, after the fact, and compares it to nothing |
| 2 | Per-household override, above the default, requiring documented client authorization on file | **no** | No override field, and no authorization-on-file concept anywhere. The closest thing is free text: Fernbrook's demo playbook carries "Sixty dollars per order without asking... above that Lisa approves", which is a household fact in prose that nothing can read |
| 3 | Approval as a LOGGED TRANSACTION, explicitly not a text message or a verbal okay | **no** | No approval object, no request-and-response, no client-side approval surface. The client side is frozen at the digest, so there is currently no member surface an approval could be requested on |
| 4 | Vendor thresholds: quotes over 300 need written client approval, emergency repairs over 150 need same-day authorization | **no** | No quote object, no vendor approval path, no threshold. The WK-DEV-010 vendor slice is the authorized future home and is internal-simulation-only until the pen test covers the surface |

**Row 5 overall: no, on all four**, and it is the row with the least of it
built. Two observations for whoever scopes it:

- **Requirement 3 is the load-bearing one and it is also the SOP's own
  emphasis.** "A logged transaction and not a text message" is a statement
  about evidence, and it is the same posture as the audit invariant: the
  record is what makes the act real. Building 1 and 2 without 3 would produce
  limits nobody can prove were honoured.
- **None of the four crosses ADR-004.** An authorization record is not an
  invoice and a limit is not a charge. QuickBooks still bills; the app would
  hold the permission and its evidence.

### What the three re-traces change about the trace as a whole

**Nothing about the ten verdicts, and a good deal about their size.** Rows 8,
11 and 5 were recorded as partial, partial and no, and they remain partial,
partial and no. What changed is that they resolve into **seven, three and four
separately testable requirements**, of which the software meets one, one and
zero.

**That is the argument for pulling the remaining SOPs before the trace becomes
a backlog**, rather than after: a row costed from its one-line expectation is
costed against roughly a fifth of what it asks for.

---

## 9. A213: not a document, and probably not digital

The founder's finding, recorded here because it closes the question rather
than leaving it open: **A213 is a RULING, not a document**, and it is not a
file in the library. It most likely lives in WK-QA-018, which the authority
page says is current through A552 **on paper**.

**If it is paper-only, the five remaining additions cannot be traced from any
digital source, and that is the finding rather than a blocker.** Rows 15
(Deviations table), 17 (Dwellings), 18 (Decision-Rights assertion), 19 (Member
channels) and 21 (Body-adjacent flag) stay untraceable until someone reads
A213 on paper and writes down what each expects.

**Two of the seven are already recovered** from WK-SEC-001's test areas 3 and
4 and do not wait on this. So the paper dependency is five rows, not seven.

**Worth stating so the next session does not re-litigate it:** the correct
response to an untraceable row is to leave it untraced. Rows 15, 17, 19 and 21
are single words. Inventing expectations for them would produce four verdicts
that read exactly like the seventeen real ones and rest on nothing.

---

## 10. Rows 1, 9 and 10, re-traced against the SOPs

Added 28 August 2026 from the founder's extract of WK-SOP-017, WK-SOP-014 and
WK-SOP-010. **The pattern from section 8 holds and row 1 breaks it upward.**

### Row 1, re-traced: seven items, and three of them are not software

**R21 ruled this row partial with HOM-entered times as the interim control.
That ruling holds for items 1 to 3 and does not reach items 4, 5 and 7.** The
founder's recommendation to amend R21 is recorded at the end of this section
and is **not applied here**, because amending a ruling is hers.

| # | WK-SOP-017 requirement | Verified | Evidence |
|---|---|---|---|
| 1 | Clock in and out through the app, geotagged at client locations; **the app is the time system of record** | **partial** | The app IS the system of record for service time: `time_entry` rows are written from the visit close (`visit-command-store.ts:208`) and from a manual `logTime` action (`actions.ts:1038`). **No geotag exists**, and see the structural note below |
| 2 | Continuous workday: paid from first client home through last, **all travel between client homes paid** | **partial** | `travel` is one of five `time_category` values and can be logged. **Nothing computes a workday**: entries are independent rows with no day grouping, no first or last stop, and no continuity rule |
| 3 | Home-to-first and last-to-home commute unpaid | **no** | No commute concept, and no first or last stop to anchor one |
| 4 | Training, team meetings, Playbook maintenance, onboarding and shadow visits **coded to their categories in the app** | **partial, and structurally limited** | The vocabulary is good: `delivery, travel, intake, admin, training`. **But `time_entry.household_id` is NOT NULL.** A team meeting is not about a household, and neither is Playbook maintenance across a route. Non-household paid time has no home in this schema |
| 5 | Missed punches corrected within 24 hours via an **app correction request, CEO-approved** | **no** | `time_entry` has two insert paths and no update path, no correction request, no approval gate, and no amendment concept. Nothing distinguishes a corrected entry from an original one |
| 6 | Meal breaks of 30+ minutes, fully relieved, unpaid and clocked; an interrupted break paid in full | **no** | One interval type. A break is a second interval class with its own paid or unpaid state, which the capture does not hold |
| 7 | Records retained **at least four years**; **employees can view their full punch and pay history in the app** | **no, on both halves** | No retention policy covers `time_entry`; the only retention knob is `photo_retention`. And **no HOM-facing time surface exists at all**: `timeEntry` is read by the corporate board and the corporate drill-in and by nothing a HOM can reach. A HOM cannot see her own hours |

**The structural finding, and it is the same shape as row 11's.**
`time_entry.household_id` is NOT NULL. **Every minute this system records is a
minute spent on a household.** A punch is not household-scoped: clocking in at
the start of a day, a team meeting, a training session, and travel from home
to the first stop are all real paid time that has no household to attach to.
So items 2, 3, 4 and 6 are not four independent gaps; they share one cause,
and it is a column constraint rather than a missing feature.

**One tension worth naming before it becomes an incident.** Item 7 requires
four-year retention of wage records. `erase-household.mjs` keeps `time_entry`
rows by default for exactly that reason, and its header says so: "employer and
business records, hours worked and money spent outlive the household data they
served". **But `--erase-time-and-costs` deletes those rows if counsel
directs.** The default is right; the flag is the tension, and whoever
authorizes it needs to know it can delete a wage record inside its retention
window.

**The founder's recommendation, recorded and not applied.** R21 should be
amended to trace this row as seven sub-requirements with separate verdicts,
and **items 4, 5 and 7 should be surfaced to the CFO rather than sitting in a
software trace**, because WK-SOP-017 is a wage compliance document citing the
FLSA and Virginia wage law, and the interim control for a wage rule is a
payroll process rather than a field. **Item 6 needs a founder decision before
it is built**, since clocked unpaid breaks add a second interval type to a
capture that currently holds one.

### Row 9, re-traced: two protocols, three channels, and a capacity rule the trace omits

| Requirement | Verified | Evidence |
|---|---|---|
| **Planned absence**: approved absence triggers **automatic backup assignment in the app** | **no** | Assignment is a manual corporate action (`assignRole`). No absence concept exists, so nothing can trigger from one |
| Affected clients notified **at least 7 days ahead, with the backup's name and photo** | **no** | No client notification of substitution at all, and no staff photo anywhere in the schema |
| Dedicated HOM confirms Playbooks current; backup reviews and raises questions **before** the coverage week | **no** | No pre-coverage review step. Adjacent and better than nothing: stranger mode gives a `backup_hm` the amplified first-visit projection at visit time, which is help DURING rather than before |
| **Same-day absence**: HOM notifies CEO by phone or app alert by 6:30 a.m. | **no** | No absence alert path |
| CEO activates the trained backup for that route | **no** | No route concept, no activation, no trained-for-this-route state |
| **Client notified before the arrival window opens** | **no** | No arrival window exists, and no client notification |
| No-backup fallback: Concierge and time-critical first, routine rescheduled within 48 hours, **every affected client gets a personal call** | **no** | None of it. The personal call is correctly outside software, but nothing records that it happened |

**The correction to what section 2 reported.** Section 2 said "nothing
notifies a member that a different person is coming". The SOP requires **two
different notifications on two different clocks**, one carrying a photo, plus
**a third channel entirely** (a personal call) when no backup exists. One
absent notification was an understatement of three.

**The capacity rule, which the trace row does not mention and which is the
live finding.**

WK-SOP-014 sets the hiring trigger at **above 85% for four weeks**. The
corporate board already renders a hiring-trigger state, computed from the
`capacity_gate` knob. **They are not the same rule, and they are not
convertible into each other.**

| | Board (`board/page.tsx:114-119`) | WK-SOP-014 |
|---|---|---|
| Metric | `households / HOMs` | a utilization percentage |
| Unit | households per HOM | percent |
| Threshold | `cap = 5`, band `3..5` | 85% |
| Window | **none: one reading trips it** | **four consecutive weeks** |

Converting one into the other needs a stated assumption about hours per
household and available hours per HOM, and no such assumption is written
anywhere. **This is not two thresholds for one decision; it is two different
measurements both called the hiring trigger**, and one of them is live on a
founder-visible surface. Filed as **G-109**.

### Row 10, re-traced: seven items, and item 7 has no substrate at all

| # | WK-SOP-010 requirement | Verified | Evidence |
|---|---|---|---|
| 1 | Duration 2 to 12 weeks, **set as a specific resume date at approval** | **no** | `recordMembershipEvent` takes `kind` and `effectiveOn` and nothing else. No duration, no resume date, no bounds. A pause is one dated event |
| 2 | Under 2 weeks is a WK-SOP-014 schedule change; over 12 is a WK-SOP-006 cancellation conversation | **no** | Follows from 1: with no duration there is nothing to route on |
| 3 | Weekly billing stops at pause start and **resumes automatically** with the first service week back | **no, and correctly outside the boundary** | ADR-004 puts billing in QuickBooks. The pause records that state changed; money stops elsewhere |
| 4 | No pause fee at launch; a future hold fee is a CEO and CFO pricing decision with 30 days notice | **not applicable today** | No fee concept exists to configure |
| 5 | Eligibility limited to members in good standing | **no** | Any `corporate_admin` can record a pause on any household. No standing concept and no eligibility check |
| 6 | Access items stay in the key safe per WK-SOP-012, or are returned **against a signed receipt** and re-collected at resume | **no** | No key custody object exists anywhere (the same absence row 3 found for "or used"). **And this is a WK-SOP-012 requirement sitting inside a WK-SOP-010 row**, which the row does not cite |
| 7 | **A pause suspends, not shortens, the six-month minimum commitment** | **no, and the substrate is absent** | See below |

**Item 7 needs a sharper statement than "it is not computed".** The founder's
note is that a paused member's commitment date would be wrong and nobody would
notice until it mattered. **The stronger version: there is no commitment date
to be wrong.** Searched the schema: `household` carries
`founding_rate_lock_until` (a RATE lock, not a term) and a `membership_terms`
jsonb that holds `monthlyRateCents` and nothing else. No minimum term, no
commitment start, no commitment end.

So the six-month commitment lives entirely on paper, in the agreement. A pause
recorded in the system has nothing to reach. **The failure mode is not a
miscomputation; it is that the two facts never meet**, which is worse in one
specific way: a miscomputed date is visible on a screen and can be checked,
and an absent one cannot be looked at at all.

### What rows 1, 9 and 10 add to the picture

**Row 1 is the outlier and it should be treated as one.** It is the only row of
the fourteen whose SOP is a compliance document, and three of its seven items
are legal obligations rather than product decisions. A software trace is the
wrong instrument for those, and leaving them in it makes them look like
backlog.

**Two rows now share one structural cause.** Row 11's feedback module and row
1's non-household paid time both fail because a NOT NULL `household_id` makes
every record be about a household. That is the correct default for a household
operations system and it is exactly right for the member-facing data. It is
also, twice now, the thing standing between the system and a staff-facing
obligation. Worth a decision as a decision, rather than being met a third time
by accident.

**And the growth factor is confirmed at five to seven per row.** Fourteen rows
of one line each have so far produced, across the six re-traced, thirty-one
sub-requirements. Extrapolating the remaining eight at the same rate puts the
completed trace near eighty, which is the founder's own estimate and is a
materially different planning input from fourteen.

---

## 11. Rows 3, 4, 6, 12, 13 and 14, re-traced. The trace is complete

Added 28 August 2026 from the founder's extract of WK-SOP-012, WK-SOP-019,
WK-SOP-016, WK-SOP-013 and the WK-TRN-009 finding. **With this section every
one of the fourteen original rows has been traced against its SOP rather than
against its one-line expectation.**

### Row 3, re-traced: eight items, and the SOP's own governing sentence is the test

WK-SOP-012's standard, in one sentence: **at any moment the company can state
exactly what access it holds for every household, where each item is, and who
has used it.** Today the system can answer none of those three.

| # | Requirement | Verified | Evidence |
|---|---|---|---|
| 1 | Every issuance, transfer, use and return logged; **the app's access log is the single source of truth and the audit trail** | **partial, and narrowly** | What exists is the VAULT reveal log: `audit_event` rows written before decryption, carrying who, which household, which field, when (`api/reveal/route.ts:59-70`). That covers viewing a stored credential. **There is no access-item register at all**: no key, no code, no fob, no location, no holder. Issuance, transfer and return have nothing to write to |
| 2 | **Clients notified whenever access changes hands**, and can revoke at any time through the app | **no** | No client notification of any access event, and no client-facing revocation. The client side is frozen at the digest |
| 3 | Each physical key photographed against a neutral background, logged with its household code | **no** | No key object to photograph |
| 4 | Client recommended to create a Well Kept specific alarm and lock code | **not software** | A recommendation made at onboarding; nothing to trace |
| 5 | Client signs WK-LEG-005 recording granted methods and restrictions | **no** | `household` records consent for the service agreement (`consent_signed_at`, `consent_doc_version`, `consent_recorded_by`), and there is no second consent slot for the access authorization or its restrictions |
| 6 | Keys returned and logged when an assignment ends, a client pauses, a client offboards, or the HOM departs | **no, and the doctrine is not missing** | Four named triggers, all four of which are events the system already records (`role_revoked`, `membership_event` pause and cancel). **The trigger exists; the thing it would trigger does not** |
| 7 | Only the client may authorize duplication, in writing | **no** | No duplication concept |
| 8 | Quarterly access audit, owned by the CEO | **no** | No audit cadence, no surface |

**The finding that matters most is item 6**, and it corrects the shape of a
gap noted earlier today. The access register was reported as lacking a
revocation trigger. **It is not missing from the doctrine**: WK-SOP-012 names
four events that end a grant, and the system already emits all four. What is
absent is the register itself, so there is nothing for those events to reach.

### Row 4, re-traced: six items, of which the device settles only one

| # | Requirement | Verified | Evidence |
|---|---|---|---|
| 1 | All client content captured natively in an app that **does not write to the camera roll**; personal cloud, rolls and notes apps prohibited | **partial, device-dependent** | Native opens the camera directly (`ImagePicker.launchCameraAsync`); the web uses `capture="environment"`, which is a hint a browser may ignore. Whether the OS writes the shot to the roll is not controlled from here. **The device test settles this item and no other** |
| 2 | An always-photograph and never-photograph table | **no** | No such table, in the schema or in the capture surface |
| 3 | **If a person appears unexpectedly in frame, retake. The app's review step exists to catch this** | **partial** | Photos render as a thumbnail strip after upload and can be removed, so review is POSSIBLE. There is no required review STEP in the close-flow state machine and no retake prompt. The SOP asserts a step the software does not have |
| 4 | Photos shared only through the platform, to client and corporate; forwarding outside is a terminable breach | **partial** | Enforced where the software can: `reuseAllowed` defaults false, is corporate-set, and is barred on NDA households; the client report carries a photo COUNT, not the images. Forwarding by a person is a policy matter |
| 5 | Photo consent and any restrictions captured at onboarding and **stored on the Playbook** | **no** | `reuseAllowed` is a per-photo media-reuse flag under REQ-006, set after the fact by corporate. That is not a consent scope captured at onboarding, and **the capture path reads no consent at all** |
| 6 | v1.1: processing follows declared consent scope with an ephemeral option, clearance mode shortens retention, **no-capture zones are absolute** | **no** | No consent scope, no ephemeral mode, no clearance mode, no zone concept. This is WK-SEC-001 test area 4's subject and is one of the two additions recovered in section 6 |

**Items 3, 5 and 6 are code reads, not device questions**, which corrects the
earlier framing of this row as blocked on a phone. The phone settles item 1.

### Row 6, re-traced: the sequence, and its collision with R19

The SOP's clock: **Day 0** charge fails, cards retry automatically within 24
hours, **ACH branches by return code** (insufficient funds retries once after
three banking days; a second such return moves to card fallback; closed or
invalid account skips retry and goes to Day 3), client notified with a one-tap
link to update the method. **Day 3** second retry plus a personal email from
the CEO. **Day 7** phone call from the CEO. Plus disputes acknowledged within
one business day and resolved within five on the evidence of itemized visit and
purchase records, chargebacks answered within the network deadline, and monthly
metrics to the CFO.

**Verified: no, on all of it.** No payments layer, no retry, no schedule, no
dispute record, no metrics.

**The finding is not the absence; it is that this sequence cannot be built as
written.** R19 replaced card auto-pay with a signed paper mandate and
member-initiated bank transfer, and D5 bars a card requirement. **The SOP's
second-insufficient-funds branch falls back to card**, which the adopted
decisions prohibit, and the whole sequence assumes an automatic rail that R19's
interim does not provide.

**So this row needs its own interim under R19 rather than inheriting the row's
CFO calendar**, and the branch that fails over to card needs a founder ruling
before anything is built against it. Recorded, not resolved.

**One note for the D5 shortlist, which is already delivered and stands:**
return-code handling is processor behaviour. Criterion 3 (webhook events for
returns and failures) covers the delivery of those events; **it does not cover
which codes each vendor exposes or how they classify them**, and the Day 0
branch depends on exactly that. Worth adding to the questions already drafted
rather than re-opening the shortlist.

### Row 12, re-traced: the source document does not exist

**The founder's finding, recorded: there is no WK-TRN-009.** What exists is
`WK-TRN-009a_Onboarding_Checklist_DRAFT`, marked **UNPLACED, code to be
confirmed, Draft v0.1, July 2026**, effective on counsel sign-off of the
WK-SOP-011 screening questions. **The draft that carries the number disclaims
the number.**

**This is the trace's second citation defect and it is worse than the first.**
Row 8 points at a real document with a phrase that does not appear in it. Row
12 points at a document that has never been written, and the number has been
referenced across the library index since v3.1. Filed as **G-110**.

**R22 needs splitting, and the first version of this entry got it wrong.**
It said "R22 is unaffected and is now better supported". **Half of that is
false**, and the half that is false is the half that matters operationally.

R22 accepted the row's interim control **as written**, and as written that
interim is TWO things:

1. **the readiness checklist as a controlled document**, and
2. **the CEO enforcing the scheduling gate manually.**

This entry recorded only the second, and then reasoned from it. **G-110 shows
the first does not exist.** So R22 accepted an interim control whose first
clause depends on a document that has never been written, and what is actually
in force today is a CEO enforcing a gate **from her own judgment, with no
controlled document behind it**. That is a materially weaker control than the
one the ruling accepted, and nothing recorded it as weaker until now.

**Split cleanly:**

- **R22's SEQUENCING of the D6 trainee role is unaffected and is better
  supported.** With no certification document written there is nothing for
  software to enforce, and building a role to gate against an unwritten
  checklist would be building against nothing.
- **R22's ACCEPTANCE of the interim control is NOT unaffected.** One of its two
  clauses is missing, and the missing one is the document that would make the
  manual gate reviewable rather than personal.

**Row 12's blocker is a document, not a feature**, and the correction sharpens
what that costs: it is not only that software has no specification to build
against. It is that **the interim control is running at half strength right
now**, and it will keep doing so until the checklist is written and placed.
That is a founder or COO item, and it is more urgent than "unbuilt feature"
suggested.

**How the error was made, since it is today's own catalogued shape.** "Interim
control accepted as written" was read as "the manual CEO gate is accepted", and
the clause that was dropped turned out to be the one that fails. A two-part
control summarized to its second part reads as intact, because the summary
contains no trace of what it left out.

### Row 13, re-traced: four items, and the row is in tension with a decision

| # | Requirement | Verified |
|---|---|---|
| 1 | Every charge generates an **itemized receipt in the app the same day** | **no** |
| 2 | Clients see their **full billing history at any time**; billing transparency is **a product feature** | **no** |
| 3 | Company-card purchases passed through at cost with no markup, itemized, on the next weekly cycle | **no** |
| 4 | Reimbursements and credits on the next weekly cycle with an itemized invoice | **no** |

**The row's real content is a conflict, not a gap.** WK-SOP-016 calls billing
transparency **a product feature** and requires same-day receipts and
always-available history. The client side is **frozen at the digest** by
WK-DEV-007, and the only client route is `(client)/playbook`.

**Reported and not resolved, per the standing doctrine.** Either the freeze
admits a billing view, or the SOP's transparency commitment is met by the
monthly statement and the SOP should say so. **The stricter reading holds
meanwhile**, which here means the freeze: no client billing surface is built.

Worth stating so the resolution is not mis-scoped: **D7 does not bar this.** D7
bars durations, service-hour totals and staffing ratios on client surfaces.
Money is not a time quantity.

### Row 14, re-traced: six items, and the row conflates two affordances

| # | Requirement | Verified | Evidence |
|---|---|---|---|
| 1 | **The app surfaces the corporate on-call number one tap from every screen** | **no** | No persistent control on any surface, web or native |
| 2 | Each HOM carries a wallet card with the universal sequence | **not software** | Correctly outside the system |
| 3 | The Playbook holds, **verified at quarterly review**: shutoffs, alarm details, utility references, primary and secondary client contacts, preferred emergency vendors, nearest hospital, vet | **partial** | The DATA is Playbook content and the demo household carries it (section 9, Safety and Emergency Readiness). **No quarterly verification cadence exists**, and no view assembles this subset |
| 4 | Per-scenario call orders, which differ; the intrusion branch requires **providing police the access log from the app on request** | **no** | No scenario guidance anywhere, and the access log that branch requires is row 3's absent register |
| 5 | Medical: 911, then the **client's emergency contact from the Playbook**, then the CEO | **no** | The contact is in the record; nothing surfaces it as a call target |
| 6 | Annual tabletop drill, two scenarios per HOM, recorded in the training file | **no** | No drill record, and no training file (row 12) |

**The correction to the earlier reading.** Section 2 reported emergency
contacts as Playbook data with no affordance. The SOP asks for **two different
affordances**: item 1 is the CORPORATE on-call number, one tap from every
screen, and item 5 is the CLIENT's emergency contact inside a scenario. The row
conflates them; they are different data, different audiences and different
placements.

**And item 4 ties row 14 to row 3**: the intrusion branch cannot be honoured
without the access register row 3 found absent.

---

## 12. The completed trace, read as a whole

**Two citation defects.** Row 8 points at a phrase that does not appear in its
source (G-108). Row 12 points at a document that has never been written
(G-110). Both were found by reading the source rather than by working the row.

**Three SOPs assert that the software is the system of record for something it
does not fully hold**: WK-SOP-017 on time, WK-SOP-012 on access, WK-SOP-019 on
capture. That is a distinct class from a missing feature, because the SOP is
making a claim to the reader about where truth lives, and a person acting on
that claim would look in the app and find part of an answer.

**Two rows are in tension with adopted decisions rather than merely unbuilt.**
Row 6's dunning sequence falls back to card, which R19 and D5 prohibit. Row 13's
always-available billing history sits against the frozen client surface. Both
are reported and neither is resolved.

**One row's blocker is a document.** Row 12 needs the certification checklist
written and placed before software has anything to gate against.

**One structural cause appears three times.** A NOT NULL `household_id` is why
row 11's staff feedback has nowhere to go, why row 1's non-household paid time
cannot be recorded, and why row 3's access register would need to be
household-scoped when a key is held by a person. Correct as a default,
load-bearing for member data, and now met three times at the boundary with
staff-facing obligations.

**The growth factor, stated the way the conventions require.** No total is
written here, because the sub-requirements are enumerated in the tables above
and a hand-carried total would drift from them the moment a row is re-read.
What can be stated per row, each figure being the row count of that row's own
table:

| Row | Sub-requirements | Met |
|---|---|---|
| 1 (time) | 7 | 0 whole, 3 partial |
| 3 (access) | 8 | 0 whole, 1 partial |
| 4 (photo) | 6 | 0 whole, 3 partial |
| 5 (approvals) | 4 | 0 |
| 8 (visit report) | 7 | 1 |
| 9 (coverage) | 7 | 0 |
| 10 (pause) | 7 | 0 |
| 11 (incident, near-miss, feedback) | 3 | 1 |
| 13 (billing transparency) | 4 | 0 |
| 14 (emergency) | 6 | 0, 1 partial |

Row 6 is deliberately not tabulated: its sequence cannot be traced as written
until the R19 collision is ruled, so counting its steps would imply they are
buildable. Rows 2, 7 and 12 are the ruled rows plus the document blocker.

**The unit is sub-requirements, not features**, and several of them are
process obligations that should leave the software trace entirely (row 1's
items 4, 5 and 7). **A backlog built from fourteen one-line rows would be
costed against roughly a fifth of what the document asks**, which is the
finding, and it does not need a precise total to hold.

---

## 13. Four rulings, 28 August 2026

The founder's rulings on what the completed trace surfaced. Recorded here as
given; each names what it changes and what it deliberately does not.

### R23. R21 is amended: row 1 splits into seven, and three items leave the trace

**Row 1 is traced as seven sub-requirements with separate verdicts**, replacing
the single partial verdict R21 gave it.

| Items | Disposition |
|---|---|
| 1, 2, 3 (clock in and out, continuous workday, commute boundary) | **Keep the typed-fields interim control.** R21 as originally written holds for these |
| 4, 5, 7 (paid-category coding, CEO-approved missed-punch correction, four-year retention with employee self-access) | **LEAVE THE SOFTWARE TRACE.** They go to Kelly as wage compliance obligations, because the interim control for an FLSA rule is a payroll process rather than a field |
| 6 (clocked unpaid meal breaks) | **HELD as a founder decision before it is built.** Clocked unpaid breaks add a second interval type to a capture that holds one |

**Why the split matters more than the verdicts.** Items 4, 5 and 7 sitting in a
software trace would be estimated like features and scheduled like features,
and their actual remedy is a payroll process that exists whether or not
software ever changes. **A compliance obligation parked in a build backlog is
an obligation nobody owns**, because the backlog's owner cannot discharge it.

The retention tension recorded in section 10 travels with item 7: WK-SOP-017
requires four-year retention, `erase-household.mjs` keeps `time_entry` by
default for exactly that reason, and `--erase-time-and-costs` deletes it if
counsel directs. Whoever authorizes that flag should know it can reach inside a
retention window.

### R24. Row 6 gets its own interim under R19

**The SOP's sequence cannot be the interim**, because it assumes an automatic
rail with return codes and falls back to card on a second insufficient-funds
return, and R19 and D5 have voided both.

**The interim, as ruled:** the CFO-owned dunning calendar runs the Day 0, Day 3
and Day 7 contacts **manually** against the paper mandate. **No automatic retry
at any stage. No card fallback at any stage.**

**The return-code branching is reclassified.** It is no longer an interim
control that is failing to exist; it is **a requirement on the processor
integration**, to be checked against whichever processor D5 selects. Added to
the processor questions rather than re-opening the shortlist, since criterion 3
already covers webhook delivery of returns and failures and this is about which
codes a vendor exposes and how it classifies them.

### R25. Row 13: the freeze holds and WK-SOP-016 is amended

**The monthly itemized statement satisfies the transparency commitment for the
launch cohort.**

**The reason is doctrine, not cost**, and it should be recorded as doctrine so
it is not re-litigated as a budget question: an always-available billing
history is a client surface that invites checking, and the whole client-side
position is that **a member should not have to check**. A surface that rewards
vigilance asks for vigilance.

**The amendment is library-side.** WK-SOP-016 is not in this repository (the
28 August census confirmed it), so the text below is the amendment ready to
apply rather than an edit made here:

> Billing transparency is delivered by the monthly itemized statement for the
> launch cohort. The always-available in-app billing history is DEFERRED, not
> promised, and its return is a founder decision about the client surface
> rather than a scheduling question.

**The D7 note travels with the amendment**, because it prevents the wrong
reason being recorded later: **D7 bars time quantities on client surfaces, and
money is not a time quantity.** So this is a freeze question, not a
prohibition, and the day the freeze lifts nothing in D7 stands in the way.

### R26. G-109: the SOP wins, and the board says what it measures

**WK-SOP-014's rule is the adopted hiring trigger**: utilization above 85
percent for four consecutive weeks, in a CFO-reviewed document. The board
should compute that.

**Until it does, the board says what it actually measures.** Applied in this
change (`oversight/board/page.tsx`):

- The block is labelled **"Fleet load against the covenant band"**, not
  "Hiring-trigger state".
- Every state string now carries its **unit**: `households per HOM`, never a
  bare ratio.
- The unset case reads **BAND UNSET** rather than GATE UNSET.
- A standing line disclaims the trigger in both knob states: this is not the
  hiring trigger, the adopted one is WK-SOP-014's, and this board does not
  compute it.
- The board journey asserts the disclaimer in **both** states and asserts the
  unit is rendered, so the label cannot silently revert.

**What is deliberately not done: computing the SOP's rule.** It needs a
utilization denominator this system does not hold (available hours per HOM),
and inventing one would be choosing a threshold's inputs, which is the thing
the conventions forbid. That is its own session with its own founder input.

**The general shape, worth keeping beside the count rule.** A threshold stated
in two places drifts, and **the one on a screen wins by default**, because it
is the one somebody reads without going looking. The remedy that worked here
was not picking a winner in code; it was making the visible number state its
own unit and disclaim what it is not.

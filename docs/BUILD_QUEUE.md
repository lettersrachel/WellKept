---
status: living
---
# Build queue · re-cut 4 September 2026 (Q-0b, Ruling 1 section 3)

The 3 September package's queue, re-cut against the Q-0 drift list per
the founder rulings on PR #282: items already built are stamped
`built@<sha>` and closed, partly built items carry their true remaining
size, new items keep the package's acceptance criteria verbatim.
Nothing is built twice. The frozen original is
`docs/intake/2026-09-03-build-package/BUILD_QUEUE.md` (superseded).

**Relationship to WORK_QUEUE.md:** WORK_QUEUE.md stays the index of the
standing state and its dated records; THIS file is the forward build
sequence. A session takes its item from here and reports into both.

**Rules (Ruling 1 and the standing Session discipline):** one item per
session, in this order, never reordered, skipped, merged or added; at
most one migration per session; `R` items are reconciliation sessions;
year-two items carry the `shadow` flag until promoted at E2 (they
compute, log and show in the corporate portal only); the launch-critical
tier ends at Q-11; year-three waits for after E4. Where Ruling 2
section 4 requires a consumer or attribute to land as its own
migration, the package row is split below with the ruling cited; those
are Part A items, not additions.

**The digest-freeze conflict is RULED (Part C §2.2, 4 September 2026):
the WK-DEV-007 freeze GOVERNS.** It was adopted under two keys and a
single-founder review does not lift it. Q-6, Q-8b and Q-9 are SPLIT,
not deferred: their freeze-safe halves proceed in their queue
positions, and any member-facing surface carries the `FREEZE-GATED`
label and waits for the two-key decision scheduled with the COO for
the 25 September session, whose scope is exactly whether the freeze
lifts for these three surfaces at E1 and nothing else. Until then the
queue assumes neither outcome. The split is written into each row
below.

**The standing authorization of 4 September 2026 (Q-6 through Q-17b)
governs which items may be worked, and THE QUEUE'S ORDER GOVERNS THE
SEQUENCE** (founder ruling the same day, correcting her own list): the
authorization's Q-10-before-Q-8b was arbitrary, and its `Q-13, Q-13a`
and `Q-14, Q-14a` were natural-language ordering rather than a
resequencing. Read the sequence from this table, top to bottom.

**BLOCKED, not to be started or worked around:** Q-8, Q-9, Q-11, Q-17,
Q-19 and Q-20, each on a founder or corporate task (fixture content, the
SMS provider and A2P registration, the security assessor, the
trigger-table conversion). **AND Q-7**, added to this list 4 September
2026: it was in neither the authorized nor the blocked list because it
was omitted, and it stays unauthorized because **its spec, the
Pre-Populated Intake Spec (A129), is not in the repository**. Nothing
outside the repository is a build authority until it has been intaken,
so Q-7 needs an intake before it can be a session input at all, which
is the standing report-and-stop rule rather than a new one.

| Id | Item | Spec | Migration | Standing (re-cut 4 Sep 2026) | Acceptance (Given / When / Then) |
|---|---|---|---|---|---|
| Q-0 (R) | Reconciliation and ground truth | all | none | **built@1db1108, CLOSED** (PR #282; Q-0b at this branch completes the intake half) | Met: register stamped, AQ report resolved as already-in-repo, drift in the Q-0 session log. |
| Q-1 | Phase 0 hardening: action-permission enforcement; deliverability webhooks to `mail_outcome`; alert on webhook silence | Architecture Review §4 (not in repository; row ran on its acceptance alone) | 0062 (`mail_outcome`; the package said none, corrected per drift: a new table is a migration; ruled Part C §2.3) | **built@1c641b5, CLOSED** (4 September 2026, session Q-1). The lint clause is the `action-permissions.test.ts` census guard, proven red and green; `mail_outcome` plus the Svix-verified webhook route plus the fleet board card with the founder-set silence knob (quiet while null). The defineAction WRAPPER is deliberately not built: the guard enforces the rule the wrapper would, recorded in the session log as the settled shape, not a remainder. Founder-side to activate: the Resend webhook endpoint and RESEND_WEBHOOK_SECRET. | Given an action file without a declared permission, when CI runs, then lint fails. Given a bounced message, when the webhook posts, then `mail_outcome` holds it and corporate sees it within one worker cycle. |
| Q-2 | Housekeeping: REQ appends; `BRAND` config extracted; Upstash resumed; daily trigger sweeps verified running | INSTRUCTION_UPDATES v2 (not in repository) | none | **built@db0cf18, CLOSED except the one founder-side clause** (4 September 2026, session Q-2). REQ-078..082 appended 5 August; the sweeps verified running in production by the founder's Railway confirmation (the PR #283 deployment ACTIVE, drain completing, board line live); `@wellkept/config` extracted with the `brand-config.test.ts` guard (values pinned byte-identical, red-and-green proven). The Upstash clause closed 4 September on the founder's confirmation (active, sweeps ran); **the row is now fully CLOSED.** | Given a fresh deploy, when the sweep is due, then it runs and logs; given `BRAND` changed, when the app renders, then no member-facing string carries the old value (grep test in CI). |
| Q-3 | Outbox | RFC-ATTR-01 Amendment 1 (RFC-001 §2 withdrawn) | 0046 | **built@db98f08, CLOSED.** The outbox is `event_outbox` with the s4 envelope and the one emit path, not `field_event`; the drain is registered-kinds with the A2 metric. The differences RFC-001 wanted are Q-3b. | Met in the standing shape: same-transaction emit, idempotent drain, dead-letter, envelope integration tests. |
| Q-3b | Domain-event catalog completion: `causation_id` on the envelope; the family catalog as registered kinds | RFC-ATTR-01 Amendment 1 §A1.3 (Ruling 2 §4) | 0063 | **built@dd39e67, CLOSED** (4 September 2026, session Q-3b). causation_id with the composite self-FK (cross-tenant causation unrepresentable, proven refused in SQL); emitOutboxEvent plumbed (no production non-null producer yet, per-column stated; the Q-12b consumers are first); the eleven RFC-001 §3a families verbatim in event-catalog.ts, a forward namespace with the legacy s4 kinds deliberately outside it; the acceptance's drain clause proven end to end with the causation chain read back by SELECT. | Given any emitted event caused by another, when written, then it carries `causation_id`; given a family-catalog kind, when emitted, then the drain consumes it as a registered kind. |
| Q-4 | Substrate guards: judgment-free schema introspection; two-household-FK refusal; the signed enums promoted into `field-attributes.ts`; PLUS the ruled additive FK `event_outbox.household_id -> household(id)` | RFC-ATTR-01 §2.8, Amendment 1 §A1.1 and §A1.4 (RFC-001 §4 withdrawn); founder ruling 4 Sep 2026 (the Q-3b acceptance): the FK lands inside Q-4's migration IF the erasure order permits, else its own row reported first. ANSWERED at the values-intake session, documents-only: the order PERMITS it (household rows are never deleted, only renamed and archived, so the parent side never vanishes; event_outbox children delete first, which an FK never objects to). | 0064 | **built@e117087, CLOSED** (4 September 2026, session Q-4). The FK applied against a 0-orphan local census with the production census SQL in the header; the one orphan-seeding suite gained parent rows; the judgment-free guard (founder-editable pattern list, two-household refusal) proven red two ways and green, with the load_concern_raised adoption boundary in the module; the signed 2.5/2.6 vocabularies promoted per A1.1, pinned by exact-list tests. Attribute COLUMNS still arrive on the next primitive that needs them (RFC §6). | Given a PR adding a column named from the judgment-free list or a two-household table, when CI runs, then it fails, and the guard is proven red and green before it is trusted. |
| Q-5 | Pipeline state machine and stage enum (`anticipate`, `identify`, `decide`, `monitor`) on triggers, prompt packs and queue items | Four-Stage Application Spec (docs/library) | **yes** (the stage enum; the package said "none (uses B)" and Migration B no longer exists as one batch, so this row now carries its own migration per Ruling 2 §4's one-per-session rule) | **NEW.** Internal only; never displayed to members. | **built@PENDING, PARTLY.** (4 September 2026, session Q-5.) BUILT: migration 0065, the `pipeline_stage` enum with the spec's four values verbatim, and one column on each of the three carriers this row names, each with its producer NAMED PER COLUMN in the migration header (G-85): `prompt_pack_item.stage` is written by the trigger engine at both insert sites, stamping `anticipate`; `trigger_rule.stage` and `work_item.stage` SHIP INERT with no producer, said in the header rather than left to be discovered. **AMENDED BEFORE APPLYING, founder ruling 4 September 2026: ALL THREE columns are nullable with no default and the nine existing prompt rows stay NULL.** The first version defaulted `prompt_pack_item.stage` to `anticipate` to backfill them; the ruling is that a stage on a row predating the stage is a claim nobody made, NULL is the true statement, and all three carriers then behave the same way. The amendment also surfaced a THIRD writer the producer table had not named (the demo seed's raw INSERT at `demo-hom-view.ts:179`), which the default had been silently stamping and which now correctly writes NULL. The migration was REGENERATED rather than hand-edited so SQL, snapshot and schema cannot disagree, and the local database was rolled back to pre-0065 so the amended file was genuinely applied. The vocabulary module `pipeline-stage.ts` is pinned against the pgEnum both directions. The internal-only guardrail (spec section 5) is enforced POSITIVELY in both client-payload mechanisms: a single-key stage clause in `assertNoAnticipationRows` (every other clause there is a key pair, which relies on the companion) and a `FORBIDDEN_CLIENT_KEYS` set in `assertDeclaredClientKeys` that throws even when the key is DECLARED, so the reviewed-list hatch cannot publish one. Four red proofs, each confirmed landed before the result was read. NOT BUILT, gated not skipped: the routing half of the acceptance criterion below. Below-threshold auto-execution and the decision inbox read against the Decision Rights block, which is Q-6; no threshold exists in this tree and choosing one is barred. The row closes when Q-6 lands the routing table. **The roadmap test is NOT this column** (founder, 4 September 2026, agreeing with the session's report): "anything tagged `decide` receives the returned-choice design review before it ships" governs FEATURES, which live in the requirements document and this queue. The column and the roadmap test share a vocabulary and not a mechanism, and reading the column as the roadmap test would be the G-109 shape (two measurements sharing one name). The routing half of this row's acceptance criterion moved to Q-6 by the same ruling, so it is not carried on a closed row. |
| Q-6 | Commitment Ledger with the Handled invariant, M-25, plain-language Decision Rights mapping; the member decision-inbox VIEW split off | Four-Stage spec; intake BENCHMARK_ADOPTION §2 | **CORRECTED: `none` was WRONG. TWO migrations, one per session** (founder ruling 4 September 2026, on a report made before building). The census that settled it: no table carries a Commitment Ledger; `decision_record` is one routed choice with no accountable-owner, member-decision or verification state, so the Handled invariant's four clauses had nowhere to live; and NO table carried a spend ceiling of any kind, the only money columns in the schema being records of what something COST rather than limits on what may be spent without asking. The original claim is stated here rather than deleted so the next reader sees the correction instead of the claim. **SETTLED 4 September 2026 (founder rulings Part One, `docs/FOUNDER_RULINGS_2026-09-04_Unblocking.md`), so Q-6-2 does not stop for them.** (1) THE 17 DECISION RIGHTS TIER ROWS STAND as recommended defaults and Q-6-2 routes on them NOW: they keep `recommended` with their authority string, a household's own confirmation supersedes them by the design already built, and the COO's 25 September confirm-or-amend does not gate this build. This ruling supersedes the no-dependency document's own not-ruled list, which named the tier rows; the later document drops them from its list, so the supersession is the founder's own and is recorded rather than reconciled. (2) `work_item.stage` IS ANSWERED: a work item carries the stage of the DECISION that produced it, and NULL when it was HOM-captured or corporate-authored with no upstream decision. It becomes writable when Q-6-2 lands and stays inert until then, which is the 0065 producer table's "NO PRODUCER YET" answered by name rather than left open. (3) A PRE-COMMITTED TIEBREAK on the reserved decide-item decision: bring both options with the semantics change spelled out as planned, and IF THEY ARE CLOSE ON MERIT take the NEW LEDGER STATE rather than a semantics change to `decision_record`, because a shipped primitive whose client audience is deliberately absent is already doing a job and two meanings in one table is the cost that is hardest to reverse. | **NEW, SPLIT per Part C §2.2.** Freeze-safe, proceeds here: Commitment Ledger state and the Handled invariant as server-side state; M-25 computation; the plain-language Decision Rights mapping with provenance (authored corporate-side). FREEZE-GATED, waits for the 25 September two-key decision: the member-facing decision inbox view and any member delivery. The Handled invariant itself is adopted law already (CLAUDE.md). **SPLIT into Q-6-1 (the Decision Rights block, migration 0066) and Q-6-2 (the ledger, the Handled invariant, M-25, the routing), consecutive sessions, founder ruling 4 September 2026.** **Q-6-1 is BUILT (0066); Q-6-2 IS BUILT (0067, 5 September 2026)** and holds its queue position ahead of Q-6g. It carries one decision the founder reserved: whether a `decide` item lands as a new ledger state or reuses `decision_record`, brought to her as two options with the semantics change spelled out, and not decided alone. Recorded here so a session working further down the list does not read Q-6 as closed. **Q-6-2 BUILT: migration 0067 `commitment_ledger_item`, one migration, no split needed.** THE HANDLED INVARIANT IS A CHECK, not a rendering rule: a row cannot be closed while an accountable owner is missing, a required member decision is unresolved, an external completion is pending with no follow-up, or verification is neither satisfied nor explicitly pending. Nine SQL refusals by name and four accepting shapes, with the constraints printed from `pg_constraint` first and all four clauses proven refusing INDIVIDUALLY. `handled`, the five display states and M-25 are COMPUTED and never stored (`lib/commitment-ledger.ts`, 16 tests); routing reads the 0066 rows (`lib/decision-routing.ts`, 7 tests) with THE NULL-THRESHOLD CASE FIRST per the founder's instruction, so a household with no right on record proposes and never auto-executes. Seven corporate actions, the drill-in ledger card, the s4 envelope on every change, ADR-006 tokens on the owner audit row, the twenty-seventh journey. **THE RESERVED DECISION WAS ANSWERED AS OPTION 2 under the pre-committed tiebreak** (a new ledger, not a new audience on `decision_record`), with both options in the session log; the sharpest fact behind it is that `decision_record.authority_class` is the COMPANY's A0..A5 ladder and a household deciding its own affairs is not on a rung of it. `decision_record` is untouched. **Both reported items RULED the same day and applied.** (1) `approaching` is RENAMED `in_progress`, keeping the open-and-not-yet-handled reading: a state named approaching will be read as temporal by every future reader whatever the definition says, and renaming it now leaves the temporal state available later under a name that describes it, once a threshold exists to define one. (2) A FIFTH STATE, `unowned`, because four left an open unowned item describable only as not-handled, which is the ledger holding a row it cannot describe; an unowned commitment is precisely what the Handled invariant exists to stop from being forgotten, so it is visible rather than absent. The five are asserted TOTAL and every one reachable, with a test that nothing is named `approaching` so the temporal name stays free. Precedence reported: an item both unowned and awaiting the member reads `needs you`, and its missing owner is still named on the row by the unmet-clause line. The member vocabulary is still the queue row's four; `unowned` is the company's word for its own failure and whether a member ever sees it belongs to the freeze-gated inbox. (3) `blocked` is recorded as a VOCABULARY TERM AWAITING A PRODUCER in the same form the migration headers use for an inert column (G-85), so its absence reads as a build fact; a test asserts nothing produces it, which is where the note stops being true if a producer is added without updating it. **Still open on this row: the freeze-gated member decision inbox**, waiting on the 25 September two-key decision. **DEPLOYED 5 September 2026 as the twenty-seventh clean run (`c972b2f`), and NOT closing more than it did** (recorded by founder instruction): the Handled invariant refused a close with no accountable owner in PRODUCTION, by constraint name, inside a transaction that was rolled back to zero rows, and the founder is exercising the other three clauses the same way. **`decision_right.confirmed_at` and `confirmed_by` remain PRODUCERLESS**, because confirming a right is the member-facing half's act; 0067 gave the ledger every column a writer and gave 0066's two nothing. Neither half routes yet. **THE FOUR ROUTING PROOFS ARE BUILT, 5 September 2026** (founder instruction, the same day: they became buildable once `candidate_routing` and `decision_right` both existed, and they land here rather than on the closed Q-5 row). `routePromptItem` (`packages/trigger-engine/src/prompt-routing.ts`) reads the household's OWN `decision_right` rows and routes on them. **What this adds over the pure function is the whole point:** `routeByDecisionRights` had unit tests from Q-6-2 and had never once been run against a row that was really in the database, only against an array a test built, which is precisely the gate-exercised-against-real-inputs rule. Four directions, **in the founder's order with the null-threshold case FIRST**, each asserted by reading the stage back from Postgres rather than from the return value: (1) no right on record proposes and stamps `decide`, with the precondition that the household really has zero rights asserted before the case runs; (2) a right recorded in WORDS proposes; (3) at or below a real ceiling permits acting, boundary inclusive; (4) one cent above the same real ceiling proposes. A fifth test proves every decision is logged whichever way it went, including the one that acted without asking. **ONLY `decide` IS STAMPED and the asymmetry is reported rather than resolved:** the acceptance clause names `decide` for the proposing side and there is NO value for the other, because the Four-Stage spec's flow sentence names FIVE movements with `execution` absent from its own four-value tag (reported on Q-5, still unresolved, and not fixed here by inventing a value or borrowing `monitor`). **AND NOTHING EXECUTES:** auto_execute states that the household's rights PERMIT acting without asking; no execution engine exists in this tree, so the outcome is recorded and logged and no act follows it, stated because the word reads like something happened. Remaining on this row: M-25 wired to a surface, and the member decision inbox, which stays FREEZE-GATED. **THE FINDING, recorded on the row by founder instruction rather than left in a session log.** `routeByDecisionRights` shipped with Q-6-2 on 5 September and carried SEVEN unit tests, all green, for the week it stood. **Every one of them passed an array the test had built, and not one read a `decision_right` row.** The tests were green and UNATTACHED: they proved the arithmetic of a ceiling and said nothing about whether the function could find a household's rights, whether the column names matched, or whether a real row's shape fit the type it was handed. A guard, a function and a table can each be correct while nothing connects them, and a green suite reads identically in both cases. **The remedy generalises and is promoted to CLAUDE.md: where a function READS A TABLE, at least one proof READS THE TABLE.** Not every proof, and not integration tests for their own sake; one, so the connection itself is exercised. **AND `auto_execute` IS RENAMED `permitted_without_asking` (migration 0071, founder instruction the same day), at the point a counter would read it rather than only in a header.** The value states that the rights PERMIT acting without asking and nothing executes, so a count of it is a count of PERMISSIONS GRANTED rather than work done, and **a leverage claim built on the old name would have measured the wrong thing.** Fourteen code sites and one stored enum; the rename is data-preserving (`ALTER TYPE ... RENAME VALUE`) and was taken at the cheap moment, with the precondition READ rather than assumed: `expected_event` carries zero rows in production and locally, so no row and no already-emitted outbox payload carries the old word. **The generated migration was replaced by hand**: drizzle-kit emitted cast-to-text, DROP TYPE, CREATE TYPE, cast back, which rewrites the column and would refuse mid-deploy against a database that had rows.  | Given a household's Decision Rights block, when a week closes, then M-25 is computed, the inbox contains only `decide` items batched at the household's cadence, nothing is delivered 21:00 to 07:00, and every ledger item shows one of: needs you / handled / approaching / done or changed, where `handled` requires the invariant. **AMENDED 5 September 2026 by founder ruling: `approaching` is RENAMED `in_progress` and a FIFTH state `unowned` is added, so this criterion's four are now five. The original wording stays as written and this is the amendment beside it rather than an edit of the record.** Given a member types a standing instruction, when saved, then it becomes a Decision Rights entry with provenance and a preview of what it will auto-execute. (Member-facing clauses run only if the freeze lifts.) **AND Q-5's routing acceptance clause lands HERE, moved by founder ruling 4 September 2026 rather than left on a closed Q-5 row.** Q-5 shipped the stage tag; the routing it implies was never buildable there, because "below-threshold items auto-execute and log fully; above-threshold items reach the decision inbox as `decide`" reads against the Decision Rights block, which is this row. The clause in full, so it is not re-derived: a trigger fires and lands in `anticipate` (BUILT, Q-5, the trigger engine stamps it at both insert sites); below-threshold items auto-execute and log fully; above-threshold items reach the decision inbox as `decide`. The stage column exists and nothing routes on it; wiring the routing table to it is this row's work. The freeze split still governs the member-facing inbox VIEW. |
| Q-6g | A treatment described in a comment and written nowhere must not pass `erasure-coverage` | G-40; the finding in the Q-6-1 session log | none expected | **NARROWED to the surviving finding by founder ruling 4 September 2026, and the wider question is CLOSED.** The floor STAYS as designed: naming and deciding is the property worth having, and a guard demanding a particular SQL shape would fail on every legitimate no-op treatment and be allowlisted into silence, which is the failure mode CLAUDE.md already names as the reason the per-column producer rule cannot become a guard. That decision is recorded in the guard's own header beside the sentence it settles, so the next reader meets the answer rather than re-deriving the question. **What remains is one claim, not the original one**: a table whose treatment exists ONLY as prose and nowhere as an executed statement should fail. Note the shape this must NOT take: it cannot demand a particular SQL shape, so it detects the absence of ANY statement touching the table rather than the presence of a specific one. The id is POSITIONAL and renameable; this is not a third part of Q-6. **BUILT 5 September 2026 (session Q-6g), no migration.** A second test in `erasure-coverage.test.ts`: the tool's source is stripped of comments and every household-referencing table must appear in what is left, detected by the ABSENCE of any statement rather than by the presence of a shape, so a deliberate no-op recorded as code passes. The first test is UNTOUCHED and its naming floor stands; the two allowlists are separate so a reason for one cannot excuse the other. **The red proof is the finding's own instance**: removing the executable `UPDATE decision_right ...` and leaving the header prose left the FIRST test green and turned the new one red naming `decision_right`. Green on the no-op shape; and with the strip disabled the test fails on its own precondition (37616 to 37616) rather than passing vacuously. Both mutations confirmed landed first. **The audit clause is satisfied by the guard itself**: 45 household-referencing tables checked, zero prose-only, computed at test time with a floor so a broken census fails instead of reporting a clean zero. CLAUDE.md's guard table row extended in the same change. | Given a table named only in a comment and touched by no statement, when CI runs, then the guard FAILS naming it. Given a table with a real treatment statement of any shape, including a deliberate no-op recorded as one, then it passes. Both directions proven with the mutation confirmed landed. AND every table currently passing is audited for the comment-only shape, with each finding either given a real treatment or allowlisted with a written reason. |
| Q-7 | Capture pipeline completion: pre-populated record tiers 1 and 2 with knowing-states and conditional visibility; per-household capture address and share-sheet target; pipeline states; anticipation opt-in per household | Pre-Populated Intake Spec (A129, not in repository); RFC-ATTR-01 Amendment 1 §A1.3; intake COMPETITIVE_FEATURE_INPUTS §1, §7 | **yes** (the `capture_artifact` pipeline extension: states, source identity, authority class, quarantine result) **THE BLOCKING CONDITION IS NAMED (founder ruling Part One item 6, 4 September 2026): Q-7 unblocks when the Pre-Populated Intake Spec (A129) reaches this repository through intake. **OWNED AND DATED 5 September 2026: RETRIEVAL, the founder's, this week, with the standing caveat she attached: if A129 turns out to be PAPER-ONLY she will say so and the row stays blocked with that recorded as its condition rather than as an absence.** The distinction matters because a paper-only ruling is a permanent state of this row, not a delay in it.** Recorded on the row rather than left as an absence, so the question is not rediscovered three items later. | **PARTLY.** `capture_artifact` is built@9121429 (0044: text capture, router queue, filing whole-or-absent) and the close-flow answer files through it. Remaining: the pipeline states (`processing`, `proposed`, `confirmed`, `routed`, `failed`, `needs_review`; the `filed`/`dismissed` mapping is a stated design question), the capture address and share-sheet ingress, the pre-populated tiers, and the opt-in switch. `expected` is never recorded as a knowing state (Amendment 1 §A1.2): tier-2 prepopulation lands as suggestions. **CORRECTED 5 September 2026 under the standing queue-row authority, found by Q-11q's guard on its first run. THE ORIGINAL STATUS READ SIMPLY `PARTLY.` and is left visible above as incomplete rather than deleted: the row listed remaining work as if it were buildable and never said its spec is missing.** **BLOCKED for the pre-populated half.** The Pre-Populated Intake Spec (A129) is absent, checked the way the library census was: filenames across the tree, inside every XML part of the ten `.docx` in `docs/library/`, and the standards store. The hits for `A129` are CITATIONS, and a citation is not a document (G-107). **This row was reported blocked when it came up in queue order on 5 September and the report never reached the row**, which is the G-133 shape committed by the session that filed G-133. What stays buildable without the spec: the pipeline states, whose vocabulary is already written in this cell. What does NOT: the pre-populated TIERS, which are the spec's own content and would be a taxonomy chosen here, and the opt-in switch's scope, which follows from them. | Given an address and anchor answers, when a household is created, then tier-1 facts land estimated (derived class, unconfirmed), tier-2 as suggestions, none confirmed; the verification walk flips states with provenance; a client session never sees `conditional_visibility` rules. Given a forwarded flyer or PDF, when captured, then it passes quarantine, lands `proposed` in the HOM review queue, never writes the record directly, and a hostile instruction inside it produces no action. Given anticipation opted out, when triggers fire, then nothing surfaces to the member. |
| Q-8 | Importer parity and fixtures: the three fixture households load through the importer with dry run | intake FIXTURES.md; REQ-016 | none | **NEW, gated on founder fixture content.** The tree's importer writes seed JSON rather than ingesting workbooks (28 Aug finding), and the existing fixtures (Fernbrook, Smoke Test Fixture, Trainor) do not map to F-1/2/3; this row extends, never replaces, them. | Given the three fixture workbooks, when imported, then all fields land with sensitivity (fail loudly if missing), render in all three portals, and the airplane, payload and suppression tests pass against them. |
| Q-8b | Provider-independent export; the member-facing control split off | intake BENCHMARK_ADOPTION section 2 (portability), the authority for this row. **REQ-017 REMOVED from this cell 4 September 2026 (G-122): it is a DIFFERENT deliverable** and citing it here made this row look like it delivers a requirement it does not touch. The frozen intake copy carries the original citation and is never edited. | none | **NEW, SPLIT per Part C §2.2.** Freeze-safe, proceeds here: the export archive and its importer round-trip. FREEZE-GATED, waits for the 25 September two-key decision: the member-facing export control. **REQ-017 IS NOT DELIVERED BY THIS ROW** and is unqueued anywhere: its branded client Playbook PDF and internal S1+S2 coverage-binder PDF have no row and no code (zero PDF references in `apps` or `packages`, checked 4 September 2026). See G-122. | Given an export is generated, then the archive restores a complete household in a clean instance through the importer with fixture parity, and S3 values are included only under a separate member-authorized reveal. (Member initiation runs only if the freeze lifts; until then generation is a corporate act on the member's request.) **BUILT 5 September 2026, and the three founder rulings of that day are applied:** people travel PSEUDONYMISED (id plus the role held on this household, never a name or an address, restored under the RFC 2606 `.invalid` TLD); `task_definition` joins the archive, referenced rows only; and the export takes a REQUIRED `--scope corporate\|member` with no default. The two scopes run their defaults in opposite directions on purpose: corporate is everything-in with a written deny list, member is nothing-in with a written allow list naming the portability phrase that admits each table. The round trip is proven in both scopes into clean instances, 41 tables compared with zero row-count mismatches. **The corporate-complete finding is carried to `docs/FREEZE_DECISION_PACKET_2026-09-25.md`**, which also records the four portability categories the member scope leaves EMPTY and what blocks each. |
| Q-9 | Inbound routing; the member-facing auto-acknowledgment split off (REQ-078, 079) | INSTRUCTION_UPDATES v2 (not in repository; REQ text is in WK-DEV-001) | none | **NEW, SPLIT per Part C §2.2, and gated on A2P registration (corporate task).** Freeze-safe, proceeds here: inbound routing into the corporate surface with no member-facing change, no HOM personal contact exposed. FREEZE-GATED, waits for the 25 September two-key decision: the auto-acknowledgment copy the member receives. | Given an inbound member SMS or email, when received, then it routes to a corporate surface and no HOM personal contact is exposed. (The channel-aware acknowledgment naming the follow-up time and the emergency line, exempt from quiet hours, sends only if the freeze lifts.) |
| Q-10 | Response-time capture and quarterly report (REQ-080, M-23) | WK-STD-028 (not in repository) | none | **CORRECTED 5 September 2026 under the standing queue-row authority. THE ORIGINAL STATUS READ SIMPLY `NEW.`, and it is left visible in that sentence as WRONG rather than deleted.** **BLOCKED, and on something the row did not say.** Three facts, each read from the tree rather than inferred: (1) **NO INBOUND-MESSAGE TABLE EXISTS**, so there is nothing whose response time could be captured; a census of `pgTable("...")` for message, inbound, sms, conversation or reply shapes returns none. (2) **REQ-080's own text carries a gate this row omitted**: `docs/WK-DEV-001_Requirements.md:109` ends `GATE: with REQ-078`, and REQ-078 is the inbound-routing requirement Q-9 builds. (3) The report is defined as comparing against the **WK-STD-028 commitment table**, and while `WK-STD-028_Response_Architecture_2026-08-02.docx` IS in `docs/library/`, the standards STORE holds STD-000, 002, 004, 005, 006, 014, 016 and 026 and no STD-028, so the table it compares against is not loaded as data. **The row's own two halves already disagreed**: the prerequisite column said WK-STD-028 was not in the repository while the status column said `NEW.` beside it. **So Q-10 is gated on Q-9**, itself gated on A2P registration, a corporate task. Reported rather than built around, per the rule that a missing prerequisite is reported and the founders decide. | Given a quarter of inbound messages, when the report runs, then per-channel response times compare against the commitment table. |
| Q-11 (R) | Reconciliation; E1 launch-critical review; security-test handoff package | WK-SEC-001 | none | **NEW.** WK-SEC-001 is present; staging (ADR-007, its ruled venue) is not stood up and gates the handoff. | Given the queue through Q-10, when reconciled, then stamps are current, the E1 checklist is mapped item by item to evidence, and the assessor has the scope. |
| Q-11p | REQ-017: the branded S1 client Playbook PDF and the S1+S2 internal coverage-binder PDF | REQ-017 (`docs/WK-DEV-001_Requirements.md:52`) | none expected | **NEW and UNSCHEDULED, at the back of the launch tier by founder ruling 4 September 2026 (G-122).** It exists because REQ-017 was cited on Q-8b and delivered by nothing: zero PDF references in `apps` or `packages`, no row anywhere. **The dependency decision is RULED so the row can be sized: browser print-to-PDF of a dedicated print route, NOT a PDF library.** Three reasons, recorded so the trade is not re-litigated by whoever picks it up: the stack is pinned and this adds no dependency; the artifact is a rendering of the same record the portals already render, so the house style lives in ONE place rather than two; and a PDF library would put document layout in the critical path of every future style change. **The known cost, named rather than discovered:** fidelity control and a print-specific stylesheet, which is a design task rather than an engineering risk, and the decision is REVERSIBLE if fidelity proves inadequate. Two artifacts from ONE route at different scopes. The id is POSITIONAL and renameable; this is NOT a sub-item of Q-11. **Q-8b's archive does not satisfy this row**, so the requirement is not closed when the export lands. | Given the client scope, then the route renders the branded S1 Playbook and a member could be handed it. Given the internal scope, then it renders S1+S2 for a coverage binder AND **respects the vault rule: no S3 value renders, and its absence is VISIBLE rather than silent**, so a reader of the binder can tell a secured value exists and was withheld rather than reading a gap as no-such-value. Given either scope, the payload guards apply as on any other surface. |
| Q-11r | The WK-SEC-001 restricted-access class | founder ruling, 5 September 2026, opened NOW rather than on demand; WK-SEC-001 test area 3, which specifies it; `docs/THREAT_MODEL_HOUSEHOLD_2026-09-05.md`, which found it missing in three scenarios | **yes**, unallocated | **NEW and UNSCHEDULED, and the founder's reason for opening it early is the row's own justification: the scenario that needs it is the scenario where it cannot be built in time.** A do-not-admit instruction, a child-pickup restriction or a welfare note is a fact somebody discloses under pressure, and a household in that position cannot wait a sprint. **Census, 5 September 2026: the tree has NO mechanism.** Searching the schema and the source finds the phrases only in demo fill data and in the decline-class taxonomy, which answers a different question. So such a fact today is an ordinary `playbook_field` row governed by sensitivity like a linen preference, and **if it is `s1` it is visible to every client identity on the household, including the person it is about, if they are a member**. **Not a design problem: WK-SEC-001 test area 3 already specifies the shape** (server-side enforcement, visit-sheet-only visibility, access logging), so this row implements a specification rather than inventing one, and the standing rule against choosing a taxonomy is not engaged. **Three things it must NOT become, named at birth because each is the easy wrong turn.** (1) It must not be a per-person view bolted onto the household unit; the household-in-conflict gap is real and larger than this row, and solving half of it here would make the other half harder to see. (2) It must not require a disability, age, diagnosis or vulnerability flag, which the standing Backstage invariant bars outright: the class is about the ACCESS RULE, never about a characteristic of a person. (3) It must not surface its own existence to a member, since a member who can see that a restricted note exists has been told something. **Read beside the threat model's conclusion**: this is the single change that moves the most scenario rows, and the model deliberately does not rule on whether it lands before launch. | Given a fact classified restricted, then it is enforced SERVER-SIDE rather than by any surface's own filtering, it reaches the visit sheet and no other surface, every access to it writes an audit row before the value is returned (the vault ordering, since the consequence class is the same), and it is absent from every member projection AND from the member archive at every scope; proven in both directions on a real household, with the failing direction exercised against a client session and against a staff session that lacks the grant, and with the RSC payload asserted rather than the rendered page, per the boundary rule. |
| Q-11a | The API-route authorization guard: `action-permissions`'s twin for routes | the 5 September security self-audit (`docs/SECURITY_SELF_AUDIT_2026-09-05.md`), finding 12; `action-permissions.test.ts`'s own not-covered column | none expected | **NEW and UNSCHEDULED, opened as a CLASS finding rather than an instance**, which is the security instruction's own preferred shape. `action-permissions.test.ts` computes every exported server action and demands a sanctioned gate, and its not-covered column already reads "API routes, whose gates live in-route". The 5 September audit walked all seventeen by hand and found every one correct, so this guard would go green on its first run and hold a property currently held by nobody. Two routes are the reason it must compute rather than grep: `api/exhibits/fleet` and `api/mobile/briefing` gate through different symbols than the rest and look ungated to a naive scan. | Given the computed set of files under `apps/web/src/app/api`, then every route handler either calls a sanctioned gate or carries a written allowlist reason, with floors on files AND handlers; proven red on a planted gate-less route and red again on a route whose gate is present but unreachable, and green on the tree as it stands. |
| Q-11n | An audited identity-creation path, for anyone | the 5 September external-certification report (`docs/EXTERNAL_CERTIFICATION_FEASIBILITY.md`), which surfaced it; the Field Test Home grant record, which met it first | **possibly**, unallocated until the shape is designed | **NEW and UNSCHEDULED, opened by founder ruling 5 September 2026.** **There is no audited way to create a person in this system, for employees either.** `db:grant` refuses to create people by design, and `auth_user` has no household to attach an audit row to, so the one time a non-app account was needed it went in as an UNAUDITED DIRECT INSERT, recorded honestly at the Field Test Home grant rather than papered over. Every other identity act IS audited: `role_assigned` carries role, ndaApproved and an ADR-006 subject token, and `role_revoked` has since gained the same (G-69). **So the gap is precisely at the beginning: the system can prove who was given a role and who lost one, and cannot prove who was created.** Read beside G-66, which found that role assignments predating 25 August carry no audit history at all and where a backfill was REFUSED because it would have to invent an actor: this row is what stops that class recurring at the point of creation. It is also the first thing an assessor asks after seeing the audit trail. | Given a corporate act that creates a person, then the row and its audit land together or neither does, the audit names the actor and the reason in words, the subject is an ADR-006 token rather than an address, and creating a person by any other path is not possible from the application; proven in both directions, including that the unaudited direct insert the Field Test Home grant used is no longer a path the tooling offers. |
| Q-11c | The weekly contact ceiling of three, counted across every member-reaching channel | founder ruling, comprehensive instruction Part One item 2; `docs/DOCTRINE_CLIENT_AND_HOM.md` | **likely yes** (a send ledger), unallocated | **NEW and UNSCHEDULED. The largest live contradiction the doctrine pass found.** TWO member-reaching channels exist today and NEITHER is counted: the weekly digest (`services/worker/src/client-digest.ts:76`) and the client report email sent on EVERY applied visit (`apps/web/src/app/api/visit-commands/route.ts:138`). **A household with two visits in a week already receives three messages before any decision exists**, and the third is not a decision. The ruling's own remedy clause has nothing to run on: no mechanism could say "something upstream is wrong" because nothing is watching. **RULED 5 September 2026 (founder), so this row is no longer blocked.** A per-visit report is EXEMPT from the ceiling as the record of a thing that happened, and the exemption is CONDITIONAL: a report containing any request, question, decision or call to action counts. So the exemption is a CLASSIFICATION of a send, never an absence of measurement, and the counter is built over every member-reaching channel regardless of how each send classifies. That ordering is the load-bearing half: a channel that is exempt but uncounted cannot be re-classified later without first building the thing this row exists to build, and the remedy clause would again have nothing to run on. Member-facing throughout, so never pre-authorized. | Given a household and a calendar week, then EVERY member-reaching send is counted before it is sent, exempt ones included, and each carries its classification; an urgent send under the materiality rules is exempt and says so; a per-visit report is exempt only while it carries no request, question, decision or call to action, and one that does counts, proven in BOTH directions on a real report; and a send that would breach the ceiling is REFUSED and raises corporate attention rather than being delivered, with the refusal visible to an operator rather than a silence. |
| Q-11f | Dissent as a first-class action, beyond prompts | founder ruling, comprehensive instruction Part Two item 3; `docs/DOCTRINE_CLIENT_AND_HOM.md` | **likely yes**, unallocated | **NEW and UNSCHEDULED, and PART OF IT ALREADY WORKS**, which is the argument for the rest rather than against it. `prompt_outcome` lets a HOM set a prompt aside with WHY in the outcome vocabulary itself, append-only with no update or delete path, and it reaches corporate at `/oversight/triggers`. **What is missing: dissent exists for PROMPTS and for nothing else.** There is no way to say "this is wrong for this household" about a decision, a standard, or a work item, and the ranked error list the ruling names does not exist. The founder's reason travels with the row because it is the design constraint: a system that records only compliance trains its operators to stop noticing. **RULED 5 September 2026 (founder): this row GENERALISES `prompt_outcome` to every proposal rather than adding a second dissent mechanism beside it, and the RANKED ERROR LIST is its consumer**, which is what makes the dissent countable rather than merely recorded. The append-only, no-update-path property is carried forward as a REQUIREMENT of the generalisation, not as an incidental of the table it grew from: a dissent that can be edited afterwards is a dissent the system can be talked out of. | Given a HOM meeting any software proposal she believes wrong for that household, then she can say so in one gesture, the dissent persists append-only with her reason in her own words and NO update or delete path exists (proven by attempting both, not by reading the service layer), it reaches a corporate surface, and the ranked error list is computed from it; proven on at least two proposal kinds so the mechanism is general rather than a second prompt-shaped special case, and proven to cover the EXISTING prompt dissent rather than running beside it. |
| Q-11h | The HOM's own record: export on leaving, and what is retained after | founder ruling, comprehensive instruction Part Two item 6; `docs/DOCTRINE_CLIENT_AND_HOM.md`; WK-SOP-017 | **likely yes** (retention state), unallocated | **NEW and UNSCHEDULED. ONE THIRD IS BUILT:** `/my-time` is the self-access half for time, with the WHERE clause as the wall (the page takes no person parameter, so it can only ever show its reader themselves). **The other two thirds do not exist**: no export-on-leaving, and no retention rule for what stays afterwards. **The retention half's conflict is RULED, 5 September 2026 (founder): WK-SOP-017's four-year wage retention WINS on `time_entry`**, because it is a legal obligation and "only what continuity requires" is a promise the company made itself; a self-imposed promise does not override a statute-backed one. The clause is in `docs/DOCTRINE_CLIENT_AND_HOM.md` so the two rules stop pointing opposite ways on one table. What is still the founder's, and what this row reports and stops on if unruled when it runs: every table OTHER than `time_entry`, where no retention obligation speaks and the continuity reading governs alone. | Given a departing HOM, then she can export everything the system holds about her work in a form she keeps, and what remains afterwards is exactly the founder-ruled continuity set and nothing else EXCEPT wage records, which are retained four years under WK-SOP-017 whatever continuity requires, proven by a census of what survives rather than by a claim and with the wage rows named in it as a deliberate survivor rather than an oversight; and given any staff member at any time, then the self-access view covers every table that holds her work rather than time alone. |
| Q-8c | The member projection of `client_edit`: the member's own submitted text alone | founder ruling A, `docs/FOUNDER_RULINGS_2026-09-04_NoDependency.md` | none expected | **NEW and UNSCHEDULED**, opened by ruling A. `client_edit` stays corporate-only as a TABLE, because half the row is the member's own words and half is an internal review disposition, and a row carrying both would ship a staff judgment about the member's edit under the guise of returning their own text. The member half is a separate build. **Placement is UNSCHEDULED because the ruling named none**, and it sits here with Q-11p rather than beside Q-8b so the id names its kin and not its position; the queue's order still governs the sequence. | Given a member-scope export, then the archive carries the member's own submitted edit text and NO review disposition and no staff-authored field, with the projection ASSERTED in the shape the exporter already uses (name what is dropped, never a keep-list) rather than hand-listed, so a column added to `client_edit` tomorrow cannot fall silently into or out of it. |
| Q-8v | Vendor engagement history: the shape that does not exist | founder ruling C, `docs/FOUNDER_RULINGS_2026-09-04_NoDependency.md`; the portability line's "vendor history" (intake BENCHMARK_ADOPTION section 2) | **likely yes**, unallocated until the shape is designed | **NEW and UNSCHEDULED**, opened by ruling C. The portability line names vendor history; the schema holds vendors as `registry_entry` kinds and holds NO record of vendor ENGAGEMENTS (who came, when, for what, at what outcome). **Ruled a build gap rather than a disclosure question**, in the founder's own words "a build gap wearing the costume of a disclosure decision", and removed from the freeze packet for that reason. Nothing here is designed yet and no taxonomy is chosen; the row exists so the absence is queued rather than rediscovered. | Given a household with vendor activity, then the record holds what the portability line means by vendor history, and a member-scope export carries it under that category instead of leaving the category empty. |
| Q-11s | Shadow enablement: turn every shadow-mode feature on for the test households from the day it ships | founder addition, 5 September 2026; the 3 September tier ruling (CLAUDE.md, adopted law) | none expected | **NEW and UNSCHEDULED. The founder's rule is adopted; the MECHANISM IT NEEDS DOES NOT EXIST, and that is this row.** The tier ruling says year-two features ship behind a `shadow` flag: compute, log, visible in the corporate portal, not surfacing to members or altering HOM briefings until E2. **Census, 5 September 2026: there is no shadow flag in this tree.** `shadow_log` is the anticipation engine's own output and runs household-wide under the A0 cap; `app_setting` holds four keys and none of them is a shadow switch. So "on for the three test households from ship day" has nothing to be turned on. **SETTLED 5 September 2026 (founder, `FOUNDER_RULINGS_2026-09-05_Blockers.md` decision 1): FERNBROOK DEMO, THE TRAINING HOUSEHOLD, and HOUSEHOLD GREEN.** The REASONS travel with the names by her instruction, so a later reader cannot swap one for another on the assumption that any three would do: Fernbrook because it is the richest record and the cascade needs something to reason from; Training because it is where the COO works; Green because it is the founder's own house and the one she can judge fastest. **Smoke Test Fixture is excluded DELIBERATELY**, not by omission: it is the deploy-checklist target and shadow output would confuse it. HO Twin and Shadow Test are not selected. **What remains is engineering only**: no shadow-enablement mechanism exists in this tree, which is what this row builds. Superseded, kept visible as the original claim:  The tree holds six (Fernbrook Demo, HO Twin, The Training Household, Household Green, Smoke Test Fixture, and a Shadow Test row), and the intake FIXTURES.md names F-1, F-2 and F-3, which do not exist and are gated on founder fixture content (Q-8). Naming them is hers; guessing would put a founder ruling on a household nobody chose. | Given a shadow-mode feature and a household named in the enablement set, when the feature ships, then it computes and logs for that household from the first run and surfaces to no member and alters no HOM briefing, proven in both directions: enabled household accumulates, non-enabled household does not, and neither reaches a member surface. |
| Q-11d | The scripted five-minute demonstration path over a fixture household, with a reset | founder addition, 5 September 2026 | none expected | **NEW and UNSCHEDULED, and it is worth more than a demo.** It walks one fixture household from FACT to CASCADE to DECISION to RECONCILIATION FINDING as one sequence, and RESETS for the next run, the `pnpm db:training` posture. **The reason it is load-bearing rather than cosmetic: the WL Gate 1 objects have never held a row outside a test.** Queried 5 September 2026 against the seeded local database: `work_requirement` 0 rows, `task_occurrence` 0 rows, `time_segment` 0 rows. All six objects are built, migrated and guarded, and the seeds bypass every producer, so the demo household shows visits and hours with nothing behind them. This row is what makes the pipeline actually run, which is also what makes two of the three leverage metrics computable over something other than zero (`docs/LEVERAGE_METRICS_STATUS.md`). | Given a fixture household and the scripted path, when it is run, then a fact enters, a cascade fires, a decision is routed and recorded, a reconciliation finding results, and each step is visible on a corporate surface with its real rows behind it; and when the path is re-run, the household RESETS to its opening state, proven by mutating every step and re-seeding to zero drift (the `db:training` proof shape). |
| Q-11e | The diligence evidence export: guard inventory, migration ledger, CI history, session logs, gap register | founder addition, 5 September 2026, ahead of an October raise | none expected | **NEW and UNSCHEDULED.** A form a technical reviewer can be handed. **Everything it exports already exists and nothing is assembled**: the guard set is computed by `guards-manifest.test.ts` and described in CLAUDE.md's table, the migration ledger is `packages/schema/drizzle` plus the journal, CI history is the runs API, session logs are `docs/sessions/`, and the register is `docs/GAP_REGISTER.md`. **The rule this row must hold, and the reason it is a build rather than a zip: every count in it is COMPUTED, never transcribed.** A hand-carried number in a diligence pack is the exact failure this repository has filed four times, and it would be read by someone whose job is to find it. No household data of any kind enters the export. | Given the repository at a named sha, when the export runs, then it produces the five sections with every count derived at generation time and the sha stamped on the artifact, contains no household row and no personal name or address, and re-running it at the same sha produces the same content. |
| Q-11i | Read-only investor view of a FIXTURE household, corporate-scoped, fixture-only by construction | founder addition, 5 September 2026, ahead of an October raise | none expected | **NEW and UNSCHEDULED. REPORTED PER THE ROW'S OWN INSTRUCTION, and the answer is a SPLIT rather than a yes or a no.** (1) **The WK-DEV-007 freeze is NOT crossed:** the freeze bars member-facing surfaces, an investor is not a member, and no member reaches this. (2) **No fixture guard is crossed either, because none exists to cross:** the `is_fixture` flag is used to EXCLUDE fixtures from metrics and no guard prevents a route serving a non-fixture household, so "fixture-only by construction" is something this row must BUILD and prove, not something it inherits. (3) **The SHAREABLE LINK half does run against a standing posture and should not ship ahead of it.** A link-reachable surface serving household content is a new authentication shape; every surface serving such content today sits behind auth, and staff surfaces behind a second factor. WK-DEV-010 section 12's vendor link is the direct precedent: the proposed shape is a short-lived signed link plus a per-event verification factor with no account, and vendor links are INTERNAL-SIMULATION-ONLY until the pen test covers the surface. WK-SEC-001 test area 3 is that test. **So the recommended split, founder's call: build the VIEW corporate-authenticated now, which delivers the demonstration value with zero new auth surface, and hold the shareable-link half for after the security test, matching the vendor-link ruling rather than making a second, looser rule for investors.** | Given a corporate session and a fixture household, when the investor view is opened, then it renders the demonstration path's sequence read-only with no action available; and given ANY household whose `is_fixture` is false, then the route refuses, proven in the failing direction against a real non-fixture household id rather than a sentinel. (The shareable-link half runs only if the founder rules it ahead of the security test.) |
| Q-11m | A computed person-column census, so the data-minimization page cannot go stale | `docs/DATA_MINIMIZATION_2026-09-05.md` section 7, which names its own gap; the four existing censuses (erasure, child-data, legal, staff-disclosure) as the pattern | none expected | **NEW and UNSCHEDULED, opened by the document that needs it rather than by a defect.** The Part Four data-minimization page states, provably, that exactly FOUR typed person columns exist in 643 columns across 56 tables, and that there is no phone, address, date-of-birth, health, financial, location or IP column anywhere. **Every negative in that page is already guarded** (judgment-free, client-duration, client-payload-shape, telemetry-discipline, the absent performer column asserted against `information_schema`). **The POSITIVE list is not.** It is a hand-maintained list of columns sitting next to the thing it counts, read on one date, which is the exact drift shape the conventions name and which has now been caught five times in this repository. A person column added tomorrow makes the page false and nothing fires. The unit is the COLUMN, not the table, because a table-level census is what hides `playbook_field`: one table, eighteen columns, one of which is the whole problem. | Given the schema, then every column matching the person-shape detection is either named in the minimization page or carries a written excusal, with a count floor on COLUMNS so a broken detection fails rather than passing vacuously; and given a column named in the page that no longer exists, then the census fails as STALE, which is the reverse direction a coverage list usually lacks; proven red both ways by name and green restored, with the manifest and the CLAUDE.md guard table extended in the same change. |
| Q-11g | The leverage metrics page: M-25, process minutes per verified outcome, households per HOM | founder instruction Part Six item 4, 4 September 2026; `docs/LEVERAGE_METRICS_STATUS.md`, the answer to its own first clause | none expected for two of three; the third is Q-13's | **NEW and UNSCHEDULED. The instruction asked to be told FIRST whether these render, and the answer was delivered as a document rather than as this row's premise: ONE of the three renders.** (1) **Households per HOM RENDERS TODAY** (`lib/capacity-utilization.ts`, on the corporate board), under Ruling 1 as amended by A581: founder and CFO only, refused in the permission matrix rather than hidden in the UI, sorted by household count and NEVER by rate, and no field of a row is a rank. **A leverage page must not become the place those rails quietly lapse**, which is the main risk this row carries and the reason it is not simply a rendering task. (2) **M-25 does not exist**: zero occurrences across `apps`, `packages` and `services`. It is Q-6-2's own acceptance criterion and arrives with it, as a count over the Commitment Ledger, computed and never stored. (3) **Process minutes per verified outcome does not exist, and as of 5 September 2026 it is RULED rather than blocked.** The founder resolved the ExecutionActual conflict AGAINST ExecutionActual (decision 3, `FOUNDER_RULINGS_2026-09-05_Blockers.md`): section 29 wins, there is no universal HOM stopwatch ever, and the seven segments are STRUCK rather than deferred so nobody reopens the question later looking for a better metric. Her stated reason is the doctrine and not the cost: seven segments per visit is precisely the measurement the never-measured doctrine bars, and a weaker E2 metric is preferable to a system that times its operators. **So this third metric is not built, now or later**, and E2 measures visit minutes per household. **So this row is genuinely three rows wearing one number**, and pretending otherwise would produce a page with one real figure and two plausible ones. | Given the corporate board's existing capacity rails, then the leverage page renders households per HOM under exactly those rails, proven by the same both-directions retrieval tests (founder and CFO accept, every other role refuses and computes nothing) rather than by re-implementing the check; M-25 renders only once Q-6-2 lands and is computed rather than stored; and process minutes per verified outcome DOES NOT RENDER AT ALL, with the page saying in words that it is not measured and why, rather than showing an empty chart or a proxy: the metric was ruled out on 5 September 2026 and a page that left a space for it would invite somebody to fill the space. |
| Q-11t | The Foundation Reset workflow | preparation batch item 12, 4 September 2026 (Part Six item 5 defers to it); WK-SVC-004 | **likely yes**, unallocated | **NEW and UNSCHEDULED. Opened here because Part Six item 5 says to open it if the preparation batch has not, and it has not.** Every household begins with a Reset and it carries its own certification under WK-SVC-004. **No queue item builds it and no mechanism in the tree corresponds to it**: there is no reset object, no certification, and the closest thing is `pnpm db:training`, which resets a FIXTURE board for the next trainee and is a seeding tool rather than a service workflow. **BLOCKED on an intake rather than on engineering: WK-SVC-004 is not in this repository. OWNED AND DATED 5 September 2026 (founder ruling, blockers document): RETRIEVAL, hers, this week.** So the condition is a named task with an owner rather than an open absence. `docs/library/` holds ten `.docx` and none is it, checked against the census. Under the standing intake rule a document outside the repository is not a build authority, so this row opens BLOCKED with the condition named, per the standing authority that missing specs do not stop the run. **One thing worth settling when it unblocks, because it decides the shape:** whether the Reset's certification is a record ABOUT A HOUSEHOLD or a credential HELD BY A PERSON. The second reading collides with Q-16's credential object and with the certification doctrine, and choosing it by accident is how a household workflow grows a person-scoped record nobody decided to keep. | Given WK-SVC-004 intaken, then a household's Reset exists as a first-class record with its certification, a household cannot be marked Reset-complete without it, and the certification names what was verified rather than that a step was clicked; proven in both directions, including that an incomplete Reset refuses rather than warns. |
| Q-11v CLOSED 5 Sep 2026 | The staff-side member-view render, read-only and corporate-scoped | founder instruction Part Six item 6, 4 September 2026 | none expected | **NEW, and REPORTED AS LARGELY BUILT rather than opened as new work, which is the honest reading.** `(corporate)/oversight/[householdId]/preview/[role]` is the CEO master view: it previews a household through the client or HM projection from the corporate seat, read-only by construction (no form posts, no actions imported), running the SAME server-side filters the real surfaces run with the client payload guards live. It grants nothing new, since corporate already sees the full record on the drill-in and a preview is a strictly NARROWER projection, which is why it needs no additional audit. **What this row therefore covers is FIDELITY, not existence**, and the 5 September flag ruling is the case that proves the distinction matters: the member labels landed on the client page first and the preview kept rendering the raw enum, so for a few hours the surface whose only purpose is to show what the member sees was showing something else. Fixed in the same change by moving the member's reading of the flag into one shared module both surfaces call. **The residual risk was structural: nothing enforced that the preview and the real page render the same thing**, and a section 4 check reads the preview to confirm the client projection, so a divergence is a check that quietly stops checking its own subject. **CLOSED 5 September 2026 by founder instruction (before the COO begins testing).** The 26th journey loads BOTH surfaces for one household and compares the member-visible pairing of each field's title with the tag it carries, sorted so layout order is not asserted, since the two pages lay fields out differently by design and that is not a fidelity question. It asserts its own input is non-trivial FIRST, because comparing two empty sets is equality and is how a fidelity check silently stops checking. Proven red by reintroducing the exact historical drift (the preview rendering `String(f.flag)`), with the mutation confirmed landed at its single site first; the failure named both sides, `Gas shutoff location => CRITICAL` against `=> Needs attention`. Green restored, 26 journeys. | Given a household and a member-visible fact, then the corporate client preview and the real client page render it identically, proven by asserting the two surfaces against each other rather than each against a fixture; and given a member-facing string that changes, then a divergence between the two fails rather than passing, which is the direction a fidelity check usually lacks. |
| Q-11w DEFERRED with a trigger | `time_entry.tz` NOT NULL | the G-116 ruling set, whose one remainder this was; founder amendment 5 September 2026 | yes, a one-line ALTER | **DEFERRED, NOT BLOCKED, and the distinction is the founder's own amendment.** It was recorded as blocked on her confirming that no pre-0060 offline command is still queued on a device; she withdrew that framing on 5 September because she cannot know what is on the COO's phone or the test handsets, and a row blocked on an unknowable fact is a row that never moves. **Her reasoning, kept because it is the general shape and not the special case: the constraint costs NOTHING today on a table with few rows and no member impact, while getting it wrong costs a failed drain and a lost visit record from a device nobody remembered. That asymmetry says tighten it when the device population is KNOWN rather than now.** **THE TRIGGER, recorded here so it fires rather than being remembered: the founding cohort's 30-day mark, when every device in the fleet is one the company issued.** At that moment the unknowable fact becomes a knowable one and the row is a one-line ALTER. **No query and no device check is needed in the meantime**, and the row explicitly does not ask for one: this was opened as a ROW rather than left as a remainder in a WORK_QUEUE paragraph precisely because a trigger held in prose is the remembered form the amendment exists to avoid. Nullable is the honest state until then; NULL on a `source = 'manual'` row means an untrustworthy clock time, and on a `visit_close` row means only that the zone was not recorded. | Given the founding cohort at its 30-day mark and a fleet of company-issued devices only, then `time_entry.tz` is altered NOT NULL and every write path is confirmed to supply it, proven by attempting a zone-less insert and watching it refuse; and given any row that predates the constraint, then it already carries a zone or the ALTER names it rather than failing halfway. |
| Q-11x | Referral becomes an EVENT: referrer, referred household, date, channel | founder ruling, 5 September 2026, on `SEED_EVIDENCE_INSTRUMENTATION_2026-09-05.md` measurement 3 | **yes** | **NEW and UNSCHEDULED, opened by founder ruling.** Today `household.referral_source` is a six-value enum COLUMN ON THE HOUSEHOLD, written by `recordReferral` and overwritten in place; the prior value survives only inside an audit row's `detail.from`, which is a log rather than a queryable series. So the system can answer what share of households came from referrals and cannot answer which member referred whom, or when, or whether a referral converted. **The founder's ruling and her reason: an attribute that records an EVENT erases the difference between the current value and the history, which is the same class as the producer rule, and E4 is priced on a question the current shape cannot answer.** The attribute is RETAINED only as a derived convenience if anything reads it, which is a census this row runs rather than an assumption it makes. **The hard part is not the table, it is the REFERRER**, and the row must meet it head on rather than discovering it at build time: a referrer who is an existing member is an `auth_user` with a household, and a referrer who is not is a record about a person who is not a client, which **may not ship before REQ-077** (the standing merge rule, W-15, G-56). **RULED 5 September 2026 (founder): SCOPE TO MEMBER REFERRERS NOW; a non-member referrer waits for REQ-077.** Her reason is that E4 measures referral share AMONG MEMBERS, so the narrow version answers the question the company is priced on, and the wide version would buy a capability nobody is measured against at the cost of shipping a non-client record ahead of its erasure path. **What the row must therefore do rather than merely avoid: make the narrowing STRUCTURAL, not conventional.** A nullable `referrer_user_id` that happens to be filled only with members today is the shape that quietly becomes general later; the referrer is a member of the referring household by construction, and a referral from someone who is not is UNREPRESENTABLE until REQ-077, refused rather than accepted-and-ignored. Same-PR obligations apply in full: a referral event is a new data category, so erasure treatment, CHILD_DATA, the legal README and both privacy copies land with it, and the erasure question has its own edge, since a referral event names TWO households and erasing one must not erase the other's record of it. | Given a referral, then an append-only event records the referrer, the referred household, the date and the channel, and a second referral from the same member creates a second row rather than overwriting the first, proven by reading both back; given a household erased, then its own referral events go and the OTHER household's record of a referral it made survives with the erased party unlinkable, proven in both directions on real rows; and given the existing `referral_source` column, then the census in this row states what still reads it and it is either derived from the events or removed, never left as a second source of truth. |
| Q-11y CLOSED 5 Sep 2026 | `db:seed` cannot produce a fixture household | preparation batch item 6, which found it by loading its own deliverable | none | **FOUND 5 September 2026 by loading the three fixture workbooks through the real importer rather than assuming the shape was right.** `load-seed.ts` inserts `id`, `name` and `tier` only; `household.is_fixture` is `NOT NULL DEFAULT false`, so **every seeded household lands as NOT a fixture**, verified by reading the flag back on all three. **Why it matters more than it looks: `is_fixture` is what excludes a household from fleet roll-ups, the reconciliation knob, the capacity calculation, the covenant figures and every metric.** So the three fixture households, loaded exactly as intended, would be counted as real households in every corporate number, and those numbers would look entirely plausible while being wrong. That is the G-113 shape: a fixture modelling a state the application would not produce, with every reader downstream reasoning from it and no guard firing. Note the existing households are consistent with this and do not contradict it: Fernbrook is a DEMO and is correctly `false`; the households that ARE flagged (Smoke Test, Trainor, HO Twin) were created by other scripts that set it. **RULED AND FIXED 5 September 2026** (founder, `FOUNDER_RULINGS_2026-09-05_Items5and6.md` item 6): `is_fixture` is a REQUIRED ARGUMENT on the loader, never a default, the same pattern as `rateLimit`'s failure mode. **Her reasoning, recorded here rather than only in the code, because the rule without it is arbitrary and the next person meets the rule first: a default lets a household become non-fixture WITHOUT THE CALLER SAYING SO, and that distinction is the entire purpose of the flag.** A real household will go through `db:seed` one day, which is exactly why the caller must state which kind it is loading. **And what it would have cost had it shipped unnoticed: three fixture households counted as real in fleet roll-ups, the reconciliation knob, the capacity calculation and every covenant figure.** The deliverable was correct and the loader was wrong, and **only loading it rather than trusting it surfaced that**, which is the general lesson worth more than the fix. Built: `--fixture` or `--real` required, no default, refusing with both options named and the reason; proven refusing (exit 1, zero rows written), accepting both ways (`--fixture` writes true, `--real` writes false), read back by query. | Given a seed file loaded through `db:seed`, then the fixture flag is set from an explicit caller decision and never inherited, proven in both directions by loading one file each way and reading the flag back BY QUERY rather than from the loader's success line; and given a load that states nothing, then it refuses rather than choosing, since a household silently entering the fleet numbers is the failure this row exists for. |
| Q-11z | The trigger YAML target shape, defined ahead of WK-APP-002 | founder instruction, 5 September 2026 (`FOUNDER_RULINGS_2026-09-05_Items5and6.md` item 5), authorized as its own instruction after item 5 was withdrawn | none | **DELIVERED 5 September 2026, and the caveat is the row's most important line. THE SHAPE IS DERIVED FROM THE SCHEMA, NOT FROM WK-APP-002**, which is not in this repository and has never been read here. Every required field comes from `trigger_rule`'s own columns and from the `definition` jsonb the engine actually reads (`TriggerRuleRow` in `packages/trigger-engine/src/engine.ts`); the five judgment fields come from the preparation batch's own list. **So nobody may later read this as having been validated against the seven life-domain sections.** If the document does not fit when it arrives, **that is a finding about the shape and not a reason to bend the document** (the founder's own words). The likeliest mismatches are named in `docs/triggers/SHAPE.md` in advance so they are recognised rather than worked around: a section needing more than one THEN ordering, a condition that is not a field binding, a rule spanning two families. **Two of the five judgment fields have no vocabulary anywhere in the tree** (`lead_time`, `suppression_class`), so the validator accepts any value and requires neither, deliberately: inventing a unit for lead time or a suppression vocabulary would be choosing a taxonomy. The other three check against real enums. **A blank is required to be PRESENT and null** rather than absent, so an unanswered judgment field is visible instead of forgotten. **AND THOSE TWO BLANKS STAY OPEN** (founder, 5 September 2026): **a vocabulary may arrive WITH WK-APP-002 rather than needing to be invented before it.** If the document implies one, the validator gains it then, and that addition is itself **a finding about the shape** under the same ruling that governs any other mismatch. So the absence is not a gap waiting to be filled by whoever next reads the file; it is a question the document may already answer, and inventing an answer first would foreclose reading it. No new dependency: the stack carries no YAML parser, so the validator reads the subset the form uses and REFUSES what it cannot parse rather than guessing, since a partial parse that silently dropped a rule would be worse than no validator. | Given the worked example, then the validator parses it, reports the six expected blanks and exits 0; and given each of a missing `pack_key`, a stage outside `pipeline_stage`, a missing `offset_days`, and a judgment field omitted rather than nulled, then it refuses naming the rule and the reason, all four proven. Given WK-APP-002 when it arrives, then the first check is whether the document fits the shape, and a mismatch opens a row against the shape. |
| Q-11k | The D7 guard does not walk the member archive | preparation batch item 10, which found it while laying out freeze packet item A (`docs/FREEZE_PACKET_ITEM_A_LAID_OUT_2026-09-05.md`) | none | **OPEN, BUILDS IN QUEUE ORDER rather than waiting on Ruling A** (founder ruling, 5 September 2026). Her reason, and it is the general form of the finding: **a rule enforced only by the list that defines it is restated, not enforced.** The row's own sentence is what she is naming: the archive's member-scope list and the test asserting it are THE SAME DECISION CHECKED AGAINST ITSELF, so a mistake in the decision passes both. It holds under EVERY shape of Ruling A, which is why it does not wait for one. `client-duration.test.ts`'s `clientSurfaceFiles()` walks the `(client)` route group plus three named copy builders (`apps/web/src/lib/mail.ts`, `apps/web/src/lib/push.ts`, `packages/mail/src/index.ts`). `packages/schema/src/household-archive.ts` and `packages/schema/src/export-household.ts` are in NEITHER set, so a duration entering the member archive is invisible to CI. **Today that is not a live leak**: the archive holds every duration-carrying table out through `CORPORATE_ONLY`, and the archive's own tests assert that list. What is missing is the SECOND opinion: the list and its assertion are the same decision checked against itself, and the guard that exists to catch exactly this class does not look. The shape-dependent half, stated so it is not read wider: under shape (b) the enforcement gap becomes load-bearing, because which jsonb keys to drop inside a payload is a decision no schema-driven walk can see at all. Opened under the standing authority that a guard finding is a row rather than a question. **BUILT 5 September 2026, and NOT the way the row assumed.** Adding the archive files to `clientSurfaceFiles()` would NOT have caught the risk, which is the finding the build rests on: that scan looks for duration IDENTIFIERS in a file's source, and admitting a duration-carrying table to `MEMBER_SCOPE` means writing that table's NAME into a list, with no duration identifier anywhere in the edit. A text scan would have read the archive, found nothing and passed. So the check is a COMPUTED SET COMPARISON: tables carrying a duration-typed column, intersected with the member scope, must be empty, both sides derived. Proven red two ways with each mutation confirmed landed first (`estimate_snapshot` planted into MEMBER_SCOPE, named in the failure; the duration detection disabled, refusing on its own precondition rather than passing vacuously) and green restored. **THE FIRST RED PROOF DID NOT GO RED and that is recorded rather than tidied**: it planted `visit_command`, which carries its hours inside a jsonb payload and no duration COLUMN, so the check correctly stayed green. The author had picked the one table the header documents as invisible to it, which confirms the documented limit is real rather than theoretical and is why the proof was re-run against a table with a real duration column. CLAUDE.md's guard row extended in the same change, including that limit. | Given the member-scope archive path in the D7 guard's walked set, when a duration-typed identifier or D7 vocabulary term reaches a member-scope table or projection, then the guard fails naming it; and given today's tree, then the guard passes, both proven by name with the mutation confirmed landed first. |
| Q-11l | The vault reveal audit records the ATTEMPT and never the outcome | G-53; founder ruling 5 September 2026 (`docs/FOUNDER_RULINGS_2026-09-05_G53_G13.md`) | one, additive: the outcome column and its CHECK | **OPEN and UNBLOCKED.** The taxonomy that blocked it is ruled: FOUR CLOSED VALUES, `delivered` (decrypted and returned), `denied` (refused on authorization before any decryption was attempted), `not_found` (no vault item for the reference), `failed` (decryption attempted and unsuccessful). **The reasoning goes in the code BESIDE THE ENUM by instruction**, not only in the register: "who viewed this" is asked by counsel and by members, so the answer must separate a person who SAW a value from one who tried and got nothing, and separate a refusal from a broken record. Collapsing `not_found` into `failed` hides a data-integrity problem inside an access log; collapsing `denied` into either makes a refusal look like an error. **THE VOCABULARY IS CLOSED: a fifth outcome is a report to the founder, never an addition**, because a vocabulary that grows silently makes old rows mean something different from new ones, and the trail's whole value is that a row from last year and a row from today mean the same thing. **The audit invariant is untouched**: the row is still written BEFORE the decrypt and the outcome is a SECOND write, so a failed second write leaves the conservative attempt row standing and the trail keeps assuming they saw it. Until this lands, any audit export answering "who viewed this" reads as "who was authorized to view this and attempted it". Marked blocks-E1 by the register triage: counsel's packet calls the audit insert load-bearing. **BUILT 5 September 2026 (0068), and THE ROW'S PREMISE WAS STALE, corrected here rather than silently outgrown.** The reveal has recorded an outcome since commit `42176a9` earlier in this run, as a second `audit_event` of kind `s3_reveal_outcome`. Three things were actually wrong: the vocabulary was not the ruled one (`no_vault_item`/`decrypt_failed` rather than `not_found`/`failed`); **`denied` had no producer and an authorization refusal left NO TRACE AT ALL**, returning 403 before writing anything, so the trail could say who attempted and never who was turned away; and nothing closed the set, since the outcome lived in `detail` jsonb where a fifth value was one keystroke. Now: a typed enum, a nullable column, and a CHECK tying column to kind in both directions. Proven with the constraint and enum printed first: a fifth value refused BY THE ENUM, an outcome row with no outcome refused, an ordinary row carrying an outcome refused, four values accepted; and at the route, five tests with the `denied` one proven red first by removing the producer while leaving the 403. The change log now says WHICH outcome in four readings rather than delivered-or-not. **Owed at the next deploy: 0068 applies (68 to 69), founder runs the header's precondition census against production first.** **A `denied` row STANDS ALONE, and that is correct rather than a missing write** (founder note, 5 September 2026, recorded on the row so it is not re-reported later). The other three outcomes are a SECOND row behind an attempt row, because the attempt row is written before the decrypt; `denied` is refused on authorization before any attempt exists, so there is nothing for it to pair with. A later reader counting outcome rows against attempt rows will find `denied` unmatched and could report the asymmetry as a hole in the audit invariant. It is not: the invariant binds the decrypt, and no decrypt was reached. | Given a reveal that returns a value, then the row reads `delivered`; given an authorization refusal, `denied` with no decryption attempted; given a reference with no vault item, `not_found`; given a decryption that throws, `failed`; and given the outcome write itself failing, the attempt row stands unchanged and the export still reads as attempted. All five proven by name, and the CHECK proven refusing a fifth value in SQL. |
| Q-11o | `db:seed` accepts a stated `--fixture`/`--real` and may silently not apply it | found 5 September 2026 while fixing the CI break the required argument caused (G-132); ruled the same day | none | **RULED AND BUILT 5 September 2026.** The founder's ruling: **the loader REFUSES when the stored flag differs from the stated one, naming both values and the household. It does not overwrite, and it does not proceed silently, which is what it did before.** A mismatch is a decision for a person, and the person is told what the conflict is. Not overwriting is the load-bearing half: flipping a real household to fixture would erase it from every fleet number, the reconciliation knob, the capacity calculation and every covenant figure, and flipping the other way would count a synthetic household in all of them. **Proven on the live instance rather than on a sentinel**: Fernbrook Demo stores `is_fixture = false` while being a fixture household, so `db:seed -- --fixture` now refuses naming both values and the household id, `--real` proceeds, and the row was read back after the refusal to confirm nothing was written. **Fernbrook Demo's wrong flag is a live defect the FOUNDER corrects herself as a deliberate one-row change with the reasoning recorded; it is expressly not fixed in the loader**, which is the same distinction the refusal exists to keep. | Given an existing household whose stored `is_fixture` disagrees with the flag the caller states, when `db:seed` runs, then the run does not complete silently: the chosen shape's behaviour (refusal, correction or loud report) happens and names both values. Given agreement, the run is unchanged. Both proven with the row's state read back by query rather than from the exit code. |
| Q-11q | A queue row that contradicts itself is not detected by anything | G-133, found working Q-10 in queue order 5 September 2026 | none | **OPEN.** Q-10's prerequisite cell read `WK-STD-028 (not in repository)` while its status cell read `**NEW.**`, three cells apart on one line, and had done since the 3 September re-cut. Every other check here compares one artefact against another; **nothing compares a queue row against itself**, and a row is read a cell at a time by someone looking for one thing, so the contradiction survives until somebody builds from it. It cost nothing this time only because that row was blocked for three other reasons as well, which is luck rather than a control. **The detection is a same-row comparison rather than a judgment**: where a prerequisite cell says a named input is not in the repository, the status cell must say blocked, gated or corrected, or carry a written allowlist reason. Both the marker words and the not-in-repository phrasings are taken from the file's own existing text rather than invented, which is what keeps this a consistency check and not a new vocabulary. **The limit belongs on the row**: it reads PROSE, so it catches a STATED contradiction and nothing else, and a row can be wrong in ways no same-row comparison can see. **BUILT 5 September 2026, migration-free** (`packages/schema/src/queue-row-consistency.test.ts`, session log `docs/sessions/2026-09-05_Q-11q.md`). The rule is three clauses, each with its reason in the guard's header: the status opener carries a marker, or the Standing cell names the absent input, or the Spec cell carries a disposition beside the absence. Both vocabularies are asserted to be IN LIVE USE, which caught dead vocabulary on the first run. **It found three things it was not written for** (G-133 addendum): two rows mis-split by an unescaped pipe, one of them the Q-16 row whose columns had shifted so that a cell-indexed read returned the wrong cell as its status and made this session's own pre-run prediction wrong; three genuinely contradicting rows, corrected under the standing authority with the original claims left visible (Q-7, Q-12, Q-14); and, while wiring the CLAUDE.md table row, **G-134**, that the manifest's table coupling was a second hand-written list and had never bound a newly added guard, now derived and proven red. **Q-7's correction is the one worth repeating: that row was reported blocked in queue order earlier the same day and the report never reached the row.** Proven five ways with each mutation confirmed landed first: red on a planted contradicting row, green on the same row marked blocked, red on a broken parse through the row-count floor, red on a deliberately mis-split row, and green on the tree.  | Given a row whose prerequisite names an input not in the repository and whose status claims it is ready, then the guard fails naming the row; given every row today, then it passes; given a blocked row with the same prerequisite, then it passes. All three proven with the mutation confirmed landed first, and the census carries a row-count floor so a broken parse fails instead of reporting a clean zero. |
| Q-11j | Household Green's workbook is FILLED, with the estimated pre-fill done from open sources | preparation batch item 7; founder ruling 5 September 2026 on the pre-fill refusal | none (the workbook is data) | **FOUNDER-SIDE, and deliberately not a build.** The empty workbook is delivered (`tooling/seed/household_green_workbook.json`, 258 fields in the exact import shape, targeting the EXISTING Green row so filling it updates that household rather than creating a second one). **The estimated pre-fill from public sources was REFUSED and the founder agreed, and the REASONING is recorded here rather than only the outcome, because the outcome alone reads as laziness and the reasoning is the rule: a plausible wrong value marked `estimated` READS AS RESEARCHED, INVITES CONFIRMATION RATHER THAN ENTRY, AND TURNS A MARKER MEANT TO FLAG A REAL DERIVATION INTO A DISGUISE.** Confirming a plausible wrong answer is easier than noticing it, and the subject is a real family's home. **Nobody attempts it, the founder included, until the sources are actually open**: she fills Green herself with the parcel record and the school assignment in front of her, and the `_estimated` marker then means what it says. The mechanism is built and empty rather than absent, so a real derivation can be recorded and distinguished the moment somebody can make one. **Green is a REAL household and loads with `--real`** (Q-11y); the symmetry is in CLAUDE.md beside the required-argument pattern. | Given the workbook filled from open sources, then `_estimated` is true on exactly those fields whose answers came from a public record rather than from the household, and every one is confirmed against the household before it is trusted; and given the load, then `household.is_fixture` reads false, verified BY QUERY rather than from the loader's success line, which is how Q-11y's defect was found in the first place. |
| Q-12 | Cascade implication sets and intersections; `latest_safe_start` derived on commitments; collision detection (`shadow`) | Inference Cascade Spec (not in repository); WK-APP-004 (bundle); RFC-ATTR-01 Amendment 1 §A1.3 | **yes** (`latest_safe_start`, RULED Part C §2.4: a materialized value stamped with its derivation, recomputed by the worker on outbox events, read-only to every actor, never hand-edited, with a logged corporate-admin recompute action; this matches the repo's stored-derived precedent, `registry_entry.derived_year` beside its `derivation_source`) | **NEW** (year-two, `shadow`). Nothing implied is ever recorded as confirmed (Amendment 1 §A1.2). **CORRECTED 5 September 2026 under the standing queue-row authority, found by Q-11q's guard on its first run. THE ORIGINAL STATUS READ SIMPLY `NEW` (year-two, `shadow`) and stays visible above as wrong rather than deleted.** **BLOCKED on the Inference Cascade Spec, which is absent**, and the tier is not the reason: a year-two row that nobody will reach for months still tells a reader it is ready, and Q-10 proves the cost arrives when somebody finally picks it up. WK-APP-004 is a bundle document held founder-side, so the row's only in-tree input is RFC-ATTR-01 Amendment 1 section A1.3, which governs how an inference would be RECORDED and says nothing about what the cascade infers. | Given a fact confirmed, when the cascade runs, then implied facts land as suggestions, predicted events and staged preparations are logged to the corporate view only, every item is dismissible, and nothing is recorded as confirmed by the system. Given two facts that intersect, then the intersection anticipation exists and neither single fact alone produces it. Given two commitments collide, when either is created or moved, then the collision is detected before it reaches the member and surfaces once, as one decision. |
| Q-12b-1 | `expected_event` with `reconciliation_status` and the six launch patterns (`shadow`) | intake BENCHMARK_ADOPTION §2; RFC-ATTR-01 Amendment 1 §A1.3 (Ruling 2 §4: own migration) | **yes** (`expected_event`) | **NEW** (split from the package's Q-12b under Ruling 2 §4's one-migration-per-session rule). **BUILT 5 September 2026 (migration 0069), session log `docs/sessions/2026-09-05_Q-12b-1.md`.** `expected_event` with the seven-value `reconciliation_status` and the six launch patterns, both vocabularies adopted VERBATIM from the spec; only the pattern KEYS are minted, and the migration header carries each key beside the sentence it transcribes. Producer per column in the header: a corporate action on the drill-in writes the expectation, the daily sweep writes the reconciliation columns, and `matched_event_id` ships with NO PRODUCER and says so, because nothing in this tree emits a signal that MATCHES an expectation. **`matched` is therefore a status the sweep never writes today**, stated rather than left to be found on a green summary. **THREE FINDINGS SHAPED THE BUILD, each read from the tree rather than assumed.** (1) A sweep cannot create a `decision_record`: `routed_by` is NOT NULL to `auth_user` and inventing an actor is what G-66 refused, so the candidate decision lives on the expectation row, personless, in the `time_segment` posture. (2) `decision_record.audience` admits only hom / corporate / founder, so nothing here can reach a member even by accident; "the household is never made to check" is construction, not discipline. (3) **`candidate_decision_refs[]` is deliberately NOT built as an array**: a Postgres array carries no foreign key, so it could not hold the composite `(household_id, id)` tenancy guarantee and a cross-tenant id would be representable; the refs arrive as a reverse join at promotion. Reported, not silently dropped. **ROUTING BY MATERIALITY IS DERIVED**, not invented: `decision_right.materiality` is already on record, so the rights of a materiality are a fact, and `routeCandidateByMateriality` composes the existing `routeByDecisionRights`. **One composition call, APPROVED 5 September 2026 with a stronger reason than the tiebreak it was reported under** (founder): every-ceiling-permits is the only reading that DOES NOT SILENTLY CREATE AUTHORITY, because below-the-highest-ceiling would let a wide right granted in one area widen another. A household that permits a large spend on, say, emergency access would thereby permit the same amount on anything else of that materiality, which nobody granted. The tiebreak framing was about keeping a meaning intact; this is about not minting authority, and it is the reason that governs. Originally reported under the standing tiebreak: where several rights share a materiality, a candidate auto-executes only if EVERY one permits it, which keeps `auto_execute`'s existing meaning intact rather than minting "below the highest ceiling". An unclassified expectation proposes. **SHADOW held structurally**: the sweep writes nothing into `attention_record` (those reach the previsit brief) and the journey asserts the client preview's EMITTED MARKUP carries no expectation, no pattern key and no status. Erasure blanks the free text to a marker so the whole-or-absent candidate CHECK survives, structure kept (the work_item class); erasure and legal censuses proven RED first, then green; the archive census and the staff-disclosure guard also fired red and are answered (CORPORATE_ONLY with its reason; an allowlist entry for `recorded_by`, **reported as an allowlist addition and as the FOURTH staff-attributed table since G-13 became a hiring precondition**). Seven SQL refusals by name with the constraints printed from `pg_constraint` first, three accepting shapes, each case in its own transaction after the first attempt's setup error made every case read ERROR (the preconditions rule catching itself). Four integration tests on a real Postgres including the auto_execute path and the empty-attention-table assertion; the journey caught its own ambiguous selector on first run. Suite 11/11 uncached, typecheck 13/13, 35 journeys green. **Owed at the next deploy: 0069 applies (69 to 70).** **Shadow enablement still does not exist (Q-11s)**, so this computes wherever an operator records an expectation and nowhere else.  | Given an expected event's window passes with no matching signal, when the sweep runs, then a `missing_expected` result opens with candidate decisions routed by materiality, and the member is never asked to check. |
| Q-12b-2 | `changeset` propagation: safe-automatic and review-required sets (`shadow`) | intake BENCHMARK_ADOPTION §2; RFC-ATTR-01 Amendment 1 §A1.3 (own migration) | **yes** (`changeset`) | **NEW** (same split). **BUILT 5 September 2026 (migration 0070), session log `docs/sessions/2026-09-05_Q-12b-2.md`.** `changeset` and `changeset_effect`, both spec vocabularies verbatim (`safe_automatic`/`review_required`, `invalidated`/`recomputed`), the composite `(household_id, changeset_id)` FK so a cross-tenant effect is unrepresentable, and three corporate acts with three separate authors (record, classify, apply). **THE ACCEPTANCE CRITERION IS PARTLY MET AND THE PARTS ARE NAMED RATHER THAN BLURRED.** Met: a source change propagates, and dependent open work dated on or after it is recorded as possibly wrong with a reason on each row; the propagation rule is written in the module rather than inferred, and work already completed or dated before the change is excluded, both asserted. **NOT met, each with its reason:** "stale work is RETIRED" needs a tenth `work_requirement` status, and that CHECK carries nine which 0051 recorded as the full adopted lifecycle, so adding one is a semantics change to a shipped primitive and is the founder's; "recomputed" has no producer, since nothing here recomputes work and claiming it did would be false; "safe changes apply automatically" has an apply path and no automatic classifier, which is a SAFETY TAXONOMY brought as a decision (`docs/DECISION_CHANGESET_CLASSIFIER_2026-09-05.md`, five candidate classes each stated by what a machine would be allowed to do without a person); and "exactly one genuine tradeoff reaches the member" cannot be built at all under the digest freeze and the shadow flag, so `member_tradeoff` ships with NO PRODUCER. **The lock horizon (`changeset_lock_horizon`) ships NULL and stays null until observed data sets it** (founder instruction), the `mail_webhook_silence` posture; null is quiet rather than permissive, proven in the unit test. **G-135 IS THIS ROW'S OWN FINDING AND IT IS THE ONE TO READ:** the first version of `changeset_applies_only_when_safe` used `=` against a nullable column, so `NULL = 'safe_automatic'` was NULL, the expression was `false OR NULL`, and A CHECK PASSES ON NULL. It accepted a changeset applied while unclassified, the exact row it exists to refuse, while the claim that NULL is not permissive sat in four places. Only a refusing-direction proof case found it; `IS NOT DISTINCT FROM` is the fix and the local migration was rolled back and regenerated rather than patched forward. Seven SQL refusals by name and three accepting shapes, each case in its own transaction per the G-129 remedy; four integration tests on a real Postgres; the journey proves nothing applies while unclassified or review-required and that the client preview's emitted markup carries none of it. **The journey also found two of its own defects**: a hydration race, and an `Apply` count that was page-wide where the drill-in carries three such buttons, which is the wrong-unit class in a locator. Suite 11/11 uncached, typecheck 13/13, 36 journeys green. **Owed at the next deploy: 0070 applies (70 to 71).**  | Given a source change (soccer moves Saturday to Sunday), when propagated, then dependent work is recomputed, stale work is retired, safe changes apply automatically, and exactly one genuine tradeoff reaches the member. |
| Q-12b-3 | `fallback_plan` on repetitive operational choices (`shadow`) | RFC-ATTR-01 Amendment 1 §A1.3 (own migration) | **yes** (`fallback_plan`) | **NEW** (same split). Builds beside `preference_rule` (0057), which is prose operating facts and is not this. | Given the preferred option fails, when the fallback runs, then the approved substitute is used within authority before anyone is asked. |
| Q-12c | A ruling recorded where the question was asked, and never carried to the row a build reads | G-133 addendum 3 (5 September 2026), opened as a row under the standing guard-finding rule; the instances are Q-7 and WKT-017 | none | **OPEN, and NARROW BY DESIGN.** Two instances in two days make the shape structural rather than a lapse: a question is asked in one artefact and answered in another, and nothing connects them. Q-7's ruling lived in a turn report; WKT-017's lived in the UNCERTAIN review sheet while the DRAFT CATALOG, which is what the freeze, the provisional flip and the Inventory loader read, still said `UNCERTAIN (T-052)`. **Only the document half is guardable and this row is only that half.** The two Task Inventory documents are machine-comparable: the sheet marks a row RULED and states its verdict, the draft carries that row's `MAPS_TO` cell. **What is expressly NOT claimed:** a ruling delivered in conversation and written into no artefact leaves nothing to compare, so Q-7's half stays procedural (a report that names a row updates that row in the same change, or it is a claim about intention). Sized small deliberately: the pattern is wide and the mechanism is narrow, and a guard that pretended to cover the wide version would be worse than none. | Given a row the UNCERTAIN sheet marks RULED whose draft `MAPS_TO` still reads `UNCERTAIN`, then the guard fails naming the row and both documents; given the tree today, with all eight verdicts applied, then it passes; given a row the sheet has not ruled, then it passes. All three proven with the mutation confirmed landed first, and a row-count floor on each document so a broken parse fails rather than reporting a clean zero. |
| Q-12d | An undiagnosed client-side failure on the corporate drill-in, order-dependent, silent in the server log | found 5 September 2026 building Q-12b-2's journey; opened by founder instruction the same day | none, until the cause is known | **OPEN, UNDIAGNOSED, and opened as an INCIDENT IN WAITING rather than as a flaky test** (founder's framing, and it is the right one). **The symptom:** reading the newly recorded changeset row from the POST-ACTION re-render of `/oversight/[householdId]` fails with Next's dev overlay showing `Runtime TypeError: network error`, a CLIENT-side failure, with NOTHING in the dev server log. **The shape:** it passes when the journey runs alone, and fails when the full journey suite has run before it, on a warm dev server and on a freshly started one alike, so it is ORDER-DEPENDENT rather than load-dependent or stale-server-dependent. Both were tested. **What is known:** the write itself always succeeded, confirmed by SQL poll every time, so the failure is in the render or the RSC fetch that follows the action's redirect, not in the action. **What is NOT known:** anything else. No cause is proposed, because a plausible one recorded here would read afterwards as a diagnosis. **The current mitigation is a MITIGATION and is labelled as one on the test itself**: the assertion now reads from a clean `page.goto` rather than from the post-action render, which proves the same fact and stops exercising the path that fails. **Proving the write by query is not an explanation**, and the risk this row carries is that the same path is what an operator uses after every corporate action on that page: if it can fail for them, it fails silently and they see a stale page rather than an error. **Why it is not sized:** the first step is reproduction with the server-side dev overlay disabled and the browser network panel recorded, which is a diagnosis session rather than a build. | Given the full journey suite run in order, when the changeset journey reads the drill-in from the post-action re-render, then it renders the new row; and the cause of the failure is named in the register with evidence rather than inferred. Until then this row stays open and the mitigation stays labelled. |
| Q-13a | `validity_class`: the eight-value lifecycle vocabulary as a column | RFC-ATTR-01 §2.7 and Amendment 1 §A1.3 (own migration) | **yes** (`validity_class`) | **NEW** (split out so Q-13's runtime work carries no migration). | Given the column lands, when a value is written outside the eight-value vocabulary, then the CHECK refuses; NULL means unassessed. |
| Q-13 | Verification with materiality, hard-stops and just-culture analytics, M-26; `validity_class` runtime behavior; Household Operational Pressure from operational evidence only (`shadow`) | Verification spec (A130, not in repository; row runs on its acceptance); RFC-ATTR-01 Amendment 1 | none (uses Q-13a) | **NEW** (year-two, `shadow`). | Given a `safety_access` field past its verification interval, when the sweep runs, then a hard-stop appears on the HOM briefing; `convenience` fields never hard-stop; a real finding honestly surfaced scores as strongly as a clean audit. Given a `life_stage_bounded` fact past its context, when read, then it is flagged stale before reuse. Given a heavy week by open loops, deadlines and collisions, when pressure is computed, then optional prompts defer, decisions batch, and no stress, emotion or health is inferred (CI guard). |
| Q-13b | `ownership_trace` (`conceive`, `plan`, `execute`) on registry items and commitments; feeds M-27 | RFC-ATTR-01 Amendment 1 §A1.3 (Ruling 2 §4: own migration) | **yes** (`ownership_trace`) | **NEW** (the one Ruling 2 §4 attribute with no package queue row; entered on Part A's authority). | Given a registry item or commitment, when the household hands a stage to Well Kept, then the trace records which of the three, and M-27 computes from it. |
| Q-14a | `dueness` on appliances, pars and recurring work | RFC-ATTR-01 Amendment 1 §A1.3 (own migration) | **yes** (`dueness`) | **NEW, GATED: ships only after the close flow captures condition inputs** (intake BENCHMARK_ADOPTION §3). Until then recurring work runs on doc-cited intervals with a `launch-calibrated` comment. | Given the condition inputs exist, when computed, then dueness derives from interval, use, condition at close, season, prior failures, quantity and confidence, and an unknown is `unknown`, never a guess. |
| Q-14 | Task stacking within the day; portfolio load sheet as a computed view; recall matching; weather and freeze layer (`shadow`) | Task Stacking spec (A128, not in repository); Portfolio and Team Ops (not in repository); intake COMPETITIVE_FEATURE_INPUTS §4, §5 | none | **PARTLY.** The CPSC recall job is built@8fa115a (weekly, deduped, tested; REQ-047). Remaining: task stacking, the load sheet, and the weather/freeze layer (no external_signal mechanism exists; the W-7-era weather note remains unbuilt). Cross-household batching does not exist and must not (year-three, membrane-gated). **CORRECTED 5 September 2026 under the standing queue-row authority, found by Q-11q's guard on its first run. THE ORIGINAL STATUS READ SIMPLY `PARTLY.` and stays visible above as incomplete rather than deleted.** **BLOCKED for task stacking, and one of the two absent specs is PARTLY RECOVERED.** The adopted Workload Forecasting brief (`docs/library/`, in the tree) names `WK Task Stacking & Errand Optimization` in its source-reconciliation table and lists its contents in one line: earliest-by, due-by, weak-then-learned duration, effort/context, dependencies, privacy membrane, and a later optimizer. **That is a summary of a spec and not the spec**, the weaker half of the WK-SEC-001 recovery pattern, so it sizes the row without unblocking it. `Portfolio and Team Ops` is absent with nothing recovered, checked inside every `.docx` part. The CPSC recall half is built and unaffected. | Given a HOM's book, when the week is planned, then date-certain anchors, flexible pool and ballast queue are computed; the capture budget is measured per household and shown to corporate; cross-household batching does not exist (CI guard). Given a freeze forecast for the household's ZIP, when the sweep runs, then the seasonal trigger fires in window. |
| Q-15 | Guided / Normal / Expert mode logic and mode history | intake SPEC_MODE_LOGIC.md §1-3 | **yes** (`hom_household_mode`, signer_1 != signer_2 in schema) | **NEW.** No mode or certification tables exist in the tree. | Given a HOM's evidence on a household, when promotion criteria are met, then the mode advances with provenance and both signatures; demotion on a high-consequence error is automatic and logged; briefing density follows mode. |
| Q-16 | Cohort record, scenario evaluation form, doctrine-change propagation by consequence class | intake SPEC_MODE_LOGIC.md §4; WK-TRN-009 (unwritten; WK-TRN-009a draft exists, G-110) | none per the package; **disagreement recorded**: an append-only cohort record is a table and a table is a migration; settled at the session, reported not silently fixed **SCOPE EXTENDED 5 September 2026 (founder addition): THE CREDENTIAL IS A FIRST-CLASS OBJECT, never inferred from records.** Issuance carries TWO SIGNATURES (the SPEC_MODE_LOGIC signer_1 != signer_2 rule applies here too, and is already the schema-level shape that ruling names), the CURRICULUM VERSION it was certified against, the RE-DEMONSTRATION EXPIRY, and SUSPENSION and REVOCATION as states rather than deletions. **The TRAINER credential takes the same shape**, which WK-TRN-009 defines separately and which G-110 records as never written: the document is a prerequisite of this row, not of the object. Why first-class matters here rather than as tidiness: a credential inferred from records cannot be suspended, cannot state what it was earned against, and cannot survive the curriculum changing under it. | **NEW.** Gated in part on the WK-TRN-009 document existing (founder task, G-110). **RECLASSIFIED 5 September 2026 (founder ruling): this is NOT A RETRIEVAL. The document does not exist and must be WRITTEN, it is the COO's program being written down, so it belongs to the COO and the founder together, and it is on the 25 SEPTEMBER AGENDA.** Recorded as a founder writing task with that date rather than as a missing document, because the two have different remedies and only one of them can be chased. **Item 9 note, 5 September 2026 (G-131): "the evaluation form" names TWO instruments.** The twelve-domain competence rubric (`rubric_anchors.csv`, one HOM per domain, anchors 2 to 5) and the five-item scenario form (SPEC_MODE_LOGIC §4, one scenario run, pass / repeat / module review) are different records of different units. This row builds the second. Both shapes are rendered in `docs/EVALUATION_FORMS_REVIEW_SHEET.md` with seven blanks that are the COO's, the first being whether a scenario run feeds a domain score at all. **The anchor-1 gap is RULED and closed the same day**: the scale starts at 1, and 1 means the domain was not demonstrated at all, whether refused, absent or not attempted, which is a different finding from a 2 (attempted and below standard) and a trainer needs both. Added rather than renumbered, so nothing already scored changes meaning; the anchor lives in `docs/FOUNDER_RULINGS_2026-09-05_Item9.md` beside the frozen CSV. | Given a doctrine change tagged `high_consequence`, when published, then affected HOMs receive a re-demonstration gate; `editorial` changes are silent; every certification check is recorded the day it happens by the observer who watched it. |
| Q-17 | Scenario delivery in-product: the Synthetic Training Household with scripted events, trainer controls, scoring capture; doubles as the AI evaluation harness | intake FIXTURES.md §3, §5; **WK-TRN-007 (the scenario categories A to G) NOT IN THE TREE, G-131** | none | **PARTLY.** The Trainor training household seed is built@43d1f2b (resettable, three identities, the buildable half of the WK-DEV-010 §10 scenario list; offline visit, disputed outcome, AI misclassification and TrainingState stubbed by name). Remaining, and it is most of the row: the 30-scenario bank on the F-2 base, scripted event delivery, trainer controls, scoring against the five-item form, the eleven abuse scenarios, and the `training=true` flag reconciliation with `is_fixture`. **ITEM 9 LANDED 5 September 2026, two sheets and one blocker.** `docs/EVALUATION_FORMS_REVIEW_SHEET.md` and `docs/SCENARIO_STUB_BANK.md` are GENERATED from the frozen sources by `tooling/review/training-forms.mjs` (counts asserted against the counts the sources state in words; proven red three ways). **The scenario bank cannot be completed here: WK-TRN-007 does not exist**, checked four ways including inside every `.docx` part, and only categories C and G are named anywhere, so A, B, D, E and F would have to be invented (G-131). The sheet fills the twelve deviation slots from the twelve deliberate traps as a stated PROPOSAL, lists seven candidates against six integration slots without choosing which to drop, and leaves all twelve procedure slots empty with their candidate sources named. **And this row's own acceptance criterion is one of two instruments**: it says the five-item form (SPEC_MODE_LOGIC §4), batch item 9 says the twelve rubric domains, and they have different units, so a builder reading either sentence alone builds the wrong one. **NEITHER SUPERSEDES THE OTHER** (founder ruling, 5 September 2026, recorded on this row so the next reader sees why both exist rather than reading the newer sentence as replacing the older): this row's five-item form records ONE SCENARIO RUN, the batch item's twelve-domain rubric records ONE HOM ON ONE DOMAIN, and they are different records of different units. Both are rendered separately and unfused; the join, which scenario exercises which domain, is the COO's first blank. Remedy for the missing document follows the WK-TRN-009 ruling: a COO writing task with the 25 September date, not a retrieval. **The missing document is a PATTERN, not an incident** (same ruling): WK-TRN-007, WK-TRN-009 (G-110) and WK-QA-000's five-dimension review (G-108) are all cited and absent, because the certification program's sources are largely unwritten and live in the COO's practice. Writing them is the FOUNDING-COHORT CRITICAL PATH rather than a documentation chore, on the 25 September agenda in that form; none is to be drafted here. | Given a trainee account, when a scenario runs, then events fire on schedule, the trainee's actions are captured against the five-item evaluation form, safety scenarios allow no partial credit, and nothing from a training household reaches a member surface. |
| Q-17b | `ai_behavior_version`, evidence envelope, reversibility classes, handoff packet contract; release pipeline through Household Zero | intake SPEC_MODE_LOGIC.md §4a | **yes** (`ai_behavior_version`) | **NEW.** | Given an AI behavior change, when proposed for promotion, then it carries a version record, passes the scenario suite and abuse cases with no partial credit, runs on Household Zero first, and can be rolled back in one action; every proposal carries its evidence envelope. |
| Q-18 (R) | Reconciliation; shadow-mode audit; E2 promotion plan; M-series review; the s4-kind-versus-catalog reconciliation | all | none | **NEW.** Founder ruling 4 Sep 2026 (the Q-3b acceptance): the shipped s4 event kinds stay OUTSIDE the family catalog until THIS session reconciles them; no renames before it, and none assumed by it. | Given all `shadow` features, when audited, then each has a promotion criterion tied to the E2 test and a measured baseline of process minutes. |
| Q-19 | Trigger library conversion consumed: `docs/triggers/*.yaml` loaded into the versioned library | WK-APP-002 (converted; founder task) | none **`trigger_rule.stage` HAS A NAMED OWNER AND A NAMED MOMENT (founder ruling Part One item 4, 4 September 2026): stages are authored onto library rules as part of THIS conversion, not in a separate session.** That is the route out for the third of Q-5's three stage columns, which shipped inert with no producer; it stays inert until this row runs. | **NEW, gated on the founder's trigger-table conversion.** Trigger rules already exist in-app (`trigger_rule`, versioned, attributed); the loader extends that mechanism, never a second one. | Given the yaml, when loaded, then every trigger validates against the schema, the three launch cascades fire on fixtures, and corporate can edit dates in-app. |
| Q-20 (R) | Hardening and pen-test remediation; managed KMS migration when funded; final E1 reconciliation | WK-SEC-001; Tech Schedule (not in repository) | none | **NEW, gated on the assessor engagement and the staging standup.** | Given the assessor's report, when remediated, then the retest passes and thirty stable worker days are logged. |

## Corporate and founder tasks outside any session (re-mapped)

The ruling's "three founder tasks that gate the queue" and the
package's six corporate tasks are the same list at two grains; the
count difference is recorded in the Q-0b session log, the list is
preserved here in full per Part B task 4, with what each gates re-mapped
to the re-cut ids:

- **Fixture content** (founder and COO): gates Q-8 and the Q-17
  remainder. Named by the ruling as queue-gating.
- **A2P/10DLC registration and SMS/voice provider selection**: gates
  Q-9. Named by the ruling as queue-gating.
- **WK-APP-002 conversion to the trigger table** (founder): gates Q-19.
  Named by the ruling as queue-gating.
- **Apple Developer Program**: gates native-device testing (already the
  standing pacing item), not a queue row.
- **Managed KMS choice**: gates the Q-20 KMS migration.
- **Security assessor engagement**: gates Q-11's handoff and Q-20;
  staging standup (ADR-007) is its venue and is also founder-side.
- **RFC-001 enum sign-off**: DONE by the 3 September ruling (RFC-ATTR-01
  Amendment 1 §A1.1).

---

## Exit condition, and what it triggers

**This section did not exist when ruling D asked for it.** Ruling D
(4 September 2026, `docs/FOUNDER_RULINGS_2026-09-04_NoDependency.md`)
says to record the retainer trigger "in the queue's exit-condition row",
and the queue had no such row: the exit condition was carried in the
standing authorization and in the founder's head rather than written
where it would be noticed at the moment it is met. Reported rather than
quietly invented, and then created, because the whole point of the
ruling is that the condition should be met at the moment it is true
rather than remembered later.

**The condition.** The standing authorization of 4 September 2026 runs
Q-6 through Q-17b, with the three instruments and the loaders. It
completes when every row in that span is built or explicitly closed and
its own exit condition is met.

**What happens then, by ruling D:** the senior engineer engagement moves
from the BUILD RETAINER to REVIEWER ON CALL. The move is triggered by
the condition, not by a date.

**Who checks.** Whoever closes the last row in the span says so in that
session's log and in this section, in the same change. It is a sentence,
and the reason it is written here is that a trigger nobody is told to
look for is a date in disguise.

**The venue for the condition's named test is a FIXTURE HOUSEHOLD, never
a live one** (founder ruling Part One item 5, 4 September 2026). The
reason is stated so it is not softened later: the exit condition must not
depend on founder content landing first. A fixture is the venue, and the
test's OUTPUT is itself informative, because what it cannot produce tells
the founder which content is still missing for it to produce real
results. That also keeps the condition inside the standing fixtures rule:
no real household data, Fernbrook DEMO and the Smoke Test Fixture, and
the Trainor training household.
